# Carbon Opportunity Workbench v0.2 specification

Status: working offline decision-support implementation. It grants no production, scientific, security, rights, qualification, submission, reuse, or deployment authority.

## Scope and precedence

The implementation extends the existing five-view v0.1 workbench. It preserves the 64 source opportunities, their exact source cells and hypotheses, six fit checks, role-specific reference architecture, MMS distinctions, one-draft-per-opportunity behavior, manual export, and lack of a receiving service.

Authority precedence is deliberately narrow:

| Order | Source | Role |
|---:|---|---|
| 1 | Current Carbon repository contracts | Authority |
| 2 | CPES evidence at `ca904dfee93d3574df4e56b99981a6ed3b138e80` | Displayed research findings |
| 3 | EXAM-PROTECT-WORKBENCH-01 | Bounded application integration |
| 4 | User input/import | Unreviewed scenario assertion |

The unmerged PR #140 documents are historical design input. The later pinned CPES conclusion controls the UI: `RETAIN_A` is current, A is DEVELOPMENT only, B is a conditional future research option, C is sensitivity only, and repeated precise hidden-bank feedback is a negative control only.

## Evidence adapter

`tools/import_cpes_evidence.py` validates the raw evidence-index hash, all eight listed byte lengths/hashes, member schemas, source/study identities, 30 ordered unique attack IDs, 17/8/5 layers, the five blocker IDs, detached replay structure/totals, and the 32/33/46.875% negative control. Structural disagreement fails the build; it is never repaired by inference.

The generated browser record retains research baseline, missing implementation revision, harness/protocol/file identities, packaging revision, integration marker, and delivery status as separate provenance. C-03 Linux worker and C-EP3 Apple M3 observations stay separate. The five required quantities remain null/unknown.

Browser evidence claims use one closed schema and maximum sizes. Exact duplicates deduplicate; same study/version with different content conflicts; the built-in study cannot be overwritten. A matching user manifest/hash verifies bytes only, and never authorship, honesty, review, rights, adequacy, qualification, or platform approval. Web Crypto unavailability is reported as unavailable.

## Challenge review

The six-check layout remains. P1–P8 appear inside Exam, with source label, state, source reference, method, reviewer note, and unresolved assumptions. The five blockers—AT-09, AT-16, AT-19, AT-22, AT-30—carry independent applicability records. `NOT_APPLICABLE_ASSERTED` is a user assertion.

Derived states are:

- `UNASSESSED`: no scoped protection review has begun.
- `PROTECTION_BLOCKED`: a touched review has a required control/evidence gap or applicable unresolved claim.
- `READY_FOR_DOMAIN_REVIEW`: the packet is assembled; this is not approval.

Record completeness and unresolved risks are separate outputs. Old Exam notes are preserved but cannot alter the protection result. No averaged fit/security score exists.

## Economics

The immutable current baseline is one adequate fresh pack per separately admitted job under A. The old v0.1 cohort calculator is retained only as a labeled legacy hypothetical sharing scenario.

For an explicitly homogeneous fixed group:

```text
A work = b × R
B work = R + H
group saving = (b − 1) × R − H
saving/candidate = group saving ÷ b
```

For `b=1`, B overhead is charged and break-even division is not performed. Equality is not an advantage. Each quantity retains value, unit/scope, and observed/estimated/assumed/simulated/unknown status. Missing R, H, b, unit, or scope makes arithmetic unknown. Unknown demand or field-dependent evidence keeps any computed relation conditional. Different resource classes, scopes, environments, and timing boundaries cannot be automatically composed.

Aggregate accounting charges full incurred reference/overhead work to incomplete groups and divides by eligible completions. Zero completions produce undefined cost/completion. Upfront implementation/qualification fields never change recurring calculations or recommendation. Candidate reconstruction/inference/obligations and required failures/audits remain charged at their true scope.

The dynamic view consumes the pinned detached Python replay output; no scheduler is ported to JavaScript. It shows work, completed and unfinished comparisons, completed-work delay, groups, and scope-specific caveats. Membership is recomputed per overhead; the 38/39 versus 36/36 counterexample is a golden vector. C is not selectable as policy.

## Migration and exports

`carbon_workbench_draft_v0.2` and `carbon_workbench_workspace_v0.2` are closed additive schemas. v0.1 workspaces are accepted only at their exact source/schema identity. Source records, shortlist, fit checks, brief, reference roles/notes, next test, studies, and review note are copied. Protection starts unassessed. Cohort/reference inputs become a visible legacy record, and A is derived separately from old assumptions where possible. A migration receipt records input digest availability and semantic changes.

Invalid imports do not mutate the current session. Parsed derived results are never trusted; they are recomputed. Authority fields are fixed at unreviewed/not-qualified/not-sent/not-granted.

Client and Engineering JSON/Markdown exports are projections of the same analysis record. The client form omits its contact field from client output. The Engineering packet retains exact study/control/attack references, six evidence conditions, assumptions, and review questions. Both embed caveats; neither is executable or transmittable. Files are unencrypted.

## Safety and operation

JSON parsing rejects duplicate members, prototype-pollution keys, excess size/depth, nonfinite and unsafe integers, Boolean numeric coercion, unsupported fields/versions, malformed identity, and forged authority. Imported text is escaped. Bundled JSON escapes `<` and Unicode separators before insertion into script nodes. CSP disables every source by default and allows only build-hashed inline assets; connections, forms, objects, and bases are disabled.

The standalone artifact works under `file://` and an approved `127.0.0.1` static host. No analytics, URL fetch, solver, evaluator, telemetry, credentials, cloud sync, public service, send action, answer publication, or sharing activation is present.

## Next evidence

The deterministic checklist requests: compatibility identity/reference role; compatible dispatch demand; same-identity adequate reference/evidence cost; B lifecycle overhead; scientific field/comparison ownership; and a service envelope. The first missing item produces a question, missing fields, owner, smallest observation/experiment, and decision relevance. Completing the list assembles a review request only.

See `REQUIREMENT_TEST_MAP.md` and `OWNER_REPORT.md` for acceptance and remaining unknowns.
