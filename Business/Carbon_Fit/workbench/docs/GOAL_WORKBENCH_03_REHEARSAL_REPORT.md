# GOAL-WORKBENCH-03 operational rehearsal

## Outcome

The accepted Carbon Goal-to-Challenge Workbench v0.3 baseline was rehearsed through three fixed synthetic/public DEVELOPMENT journeys. The workbench preserves a supported source-owned authoring result, detects a same-physics goal mismatch, and keeps an unsupported physics/rights request bound to a precise source-owner restart event. It does not call Grok, a CRM, a calendar, a solver, training, a protected exam, qualification, registration, or launch.

The rehearsal also closed one concrete planning gap: structural coverage is now checked in both directions. A material requirement must have a complete measurement/authoring trace and case coverage; a case family without a requirement or without a complete trace is visible in the engine, UI summary, and exports.

## Frozen source and baseline

- Application baseline: merge `32c0465d87864e3ba01c0f846be7dca91708c7e4`, accepted PR #162 head `00b197ccd4324a400c8efb9f1073f340de6dec65`, acceptance run `34801089908` attempt 2.
- Operating source: `Carbon_Grok_Master_Plan_v1_9.docx`, all ten rendered pages visually inspected, including the page-4 diagram.
- Source DOCX SHA-256, verified before rendering: `799108791ec951ebfd51b2d56c35f70df51ebc559929a4991cc118efd13981b5`.
- Rendered PDF SHA-256: `925ff210a40bbb9bd5136d95e8ea937beac5c775e472df5258ab6d4736cec2cb`.
- The source DOCX disappeared from its supplied Downloads path after rendering; the recorded digest is the original observed `shasum` result, not inferred from the PDF.
- Machine-readable protocol: `data/goal_workbench_03_rehearsal_protocol_v1.json`.
- Source conformance map: `data/grok_v1_9_conformance_v1.json`.
- Deterministic result: `data/goal_workbench_03_rehearsal_record_v1.json`.

The v1.9 plan is operating input, not scientific or execution authority. The current repository contracts remain authoritative. The external DOCX/PDF bytes are not copied into the release bundle; the public-safe conformance map and observed digests are.

## Journey A — supported Burgers Dynamics

The direct synthetic intake binds one material requirement to a mandatory measurement trace, one EVAL case family, the source generator/reference proposal, and the exact public Burgers DEVELOPMENT template. The real fixed local C-AUTH1 bridge emitted:

- semantic status `INTENT_PRESERVED`;
- active goal `Dynamics`;
- source profile `carbon.goal-authoring.canonical-json.v1`;
- proposal digest `sha256:4d2f4d84dfbf82553c3d51dcd62212adaa2c4595598ce528513bd49d91fd80be`;
- all source capability flags false.

The design-bound measurement/score diagnostic request remains `NOT_EXECUTED`. A synthetic interface response reattached to the exact request/design and the launch projection stopped at `MANUAL_OWNER_HANDOFF_REQUIRED` with native launch `UNAVAILABLE`.

## Journey B — Front Resolution mismatch

The same Burgers bridge accepted the closed source input but emitted active `Dynamics`. The workbench retained the actual output, returned `INTENT_MISMATCH`, and generated a C-AUTH1 extension request for `periodic_viscous_burgers_1d_v1 / Front Resolution`. No qualification or launch state changed.

This is the required negative control: compiler success does not establish fulfillment of the intended active objective.

## Journey C — unsupported physics and rights

The synthetic `customer_heat_transfer_v1` / `CLIENT_RESTRICTED` scope was not coerced into Burgers or relabeled as synthetic rights. It produced a source-owner extension request and a manual handoff with:

- one named lead;
- one correction pass maximum;
- no routine polling;
- a precise stop condition;
- an observable restart event bound to the same request and design revision.

The blocker response imported once, an exact replay deduplicated, and the record stopped `BLOCKED` / `NOT_LAUNCHED`. Timeout-to-never-started inference is expressly forbidden by the rehearsal contract.

## CPES and evidence result

Variant A remains the DEVELOPMENT baseline. Variant B was not activated. AT-09, AT-16, AT-19, AT-22, and AT-30 remain source-backed unresolved claims. Qualified same-identity reference cost, compatible dispatch demand, B overhead, field-dependent evidence, and service limits remain unknown.

Change-impact controls were exercised explicitly:

| Change | Re-review impact |
|---|---|
| Population | CPES economics |
| Score/evidence depth | CPES economics |
| Reference role | CPES protection and economics |
| Disclosure | CPES protection |
| Commercial/editorial note | no automatic CPES invalidation; unknown review impact remains visible |

No cost result can clear mandatory science or protection requirements.

## Grok v1.9 conformance

The workbench represents one named lead, one working record with role-specific projections, one correction pass, a precise blocker/restart event, no routine polling, timeout uncertainty, manual/unavailable route truth, R1/S1 C-PILOT-01 scope, H1's reserved-decision role, and least-privilege N1 output. No source divergence was found in these mapped rules.

No Grok account, CRM, calendar, notification, or native launch connector was verified. The plan's two-task ceiling, 1+1 construction pilot, possible six builds, and proposed owner-attention allowance remain pilot operating facts, not scientific defaults.

## Friction observed

- Material fields re-entered across the three deterministic records: `0`.
- Handoffs prepared/exported: `3` / `3`.
- Responses imported: `4`; exact duplicate responses deduplicated: `1`.
- Correction passes: `1`; poll events: `0`.
- Human active minutes and wait duration: `NOT_MEASURED`.

These are rehearsal mechanics, not founder-time, throughput, scientific-quality, or commercial-value claims.

## Interface decision

Working: browser-local job/design record, fixed local C-AUTH1 Burgers bridge, exact-bound manual response import.

Manual: source-owner handoff delivery, measurement/score diagnostic return, owner decision.

Unavailable: Grok account connector, CRM/calendar connector, native launch status interface.

The recommended next integration is a source-owned measurement/score diagnostic response adapter. It is the earliest common evidence gap on the supported path. Its minimum contract is a versioned request-bound result carrying source revision, implementation layer, per-example execution status, limitations, artifact digest, and no qualification promotion. This recommendation does not authorize implementing C-05 or changing official score science.

## Reproduce

```sh
node Business/Carbon_Fit/workbench/tools/run_operational_rehearsal.cjs --grok-plan /path/to/Carbon_Grok_Master_Plan_v1_9.docx
node --test Business/Carbon_Fit/workbench/tests/test_workflow.cjs Business/Carbon_Fit/workbench/tests/test_rehearsal.cjs
```

## Local acceptance evidence

- 93 Node engine/workflow/rehearsal checks passed. The suites overlap and are not a scientific or security total.
- 24 Python C-AUTH1/bridge checks passed.
- 15 source, evidence, schema, deterministic build, and package checks passed.
- 106 Hub decision records passed focused validation.
- 27 goal-flow and 40 inherited CPES checks passed against the actual generated HTML in Google Chrome. These are separate browser journeys.
- Changed Python files passed Black 26.5.1 and Ruff 0.16.3 checks; changed JavaScript files passed syntax checks.

Final CI run, PR head, and merge state belong in the delivery record after exact-head acceptance. Safari/WebKit and every manual/unavailable integration above remain unexecuted unless separately reported.
