# GOAL-WORKBENCH-06 source-owner decision packet

Decision ID: `GOAL-WORKBENCH-06-C-AUTH1-CONTRACT-V1`

Owning domains:

- Workbench consumer: `SYSTEM/BUSINESS-AUTHORITY`
- Native source contract: `WAVE-C/C-AUTH1` / scientific integration

Status: `DELIVERED_VERIFIED_UNACKNOWLEDGED`

Verified destination: <https://github.com/carbonphysicsai/Carbon/issues/42#issuecomment-5675346832>. Its exact raw Markdown was read back through the GitHub API and matched the posted bytes at `sha256:04952e91e9fda00b36e85ac8862cb601bb9992c124546eec9b7fb56e2c63d72c`. This proves delivery of the decision content to the issue, not acknowledgment, acceptance, selection, funding, or scientific authority.

Historical correction: comment [`5674263522`](https://github.com/carbonphysicsai/Carbon/issues/42#issuecomment-5674263522) contained only the literal local-path token `@/private/tmp/gw06-owner-notification.md`. It is preserved as `DELIVERY_MALFORMED_CONTENT_NOT_DELIVERED`; it did not deliver this packet. The complete sequence and byte-level receipt are in `source_assessment/delivery/v1/`.

## Decision requested

Accept, change, or reject contract `1.0` and source profile `burgers-dynamics-public.v1` for one exact public question: can the accepted C-AUTH1 path express the sealed Burgers/Dynamics design, what exact C-05 fixed-case context is relevant, and what applicability questions remain unanswered?

The packet is in `source_assessment/v1/`; the normative document is `GOAL_WORKBENCH_06_SOURCE_ASSESSMENT_CONTRACT.md`. The detached fixture response is generated by `GOAL_WORKBENCH_06_TEST_PRODUCER`, not by Harsh or another source owner, and remains non-authoritative.

## Exposed source facts and consumer meaning

| Source field | Consumer meaning | Producer cannot assert |
|---|---|---|
| C-AUTH1 semantic receipt `INTENT_PRESERVED`, emitted active goal `Dynamics` | Exact public request compiled to the named DEVELOPMENT Dynamics semantics | Exam adequacy, customer suitability, scientific qualification |
| Native proposal template/goal/query/measurement/reference declarations | Technical field-level capability coverage for the named source snapshot | Unstated physics, unsupported goals, qualified thresholds or floors |
| C-05 fixture ID, request/result digests, association, four measurement IDs, six physics IDs | Historical context for one exact public EVAL case/query | Fresh execution, 72-member campaign, target-population adequacy |
| Null uncertainty/limits and unresolved source decisions | Those quantities remain unknown/unresolved | Pass/fail, score, qualified floor, uncertainty coverage |
| Claimed issuer and role | A producer statement preserved as bytes | Authenticated operator or accepted owner authority |

Request digest and exact source hashes are recorded in `source_assessment/v1/manifest.json`; exact tested values must be taken from that generated manifest rather than copied from this prose.

## Origin route and missing trust inputs

Detached validation can establish exact fixture bytes, the accepted local authoring artifact/semantic facts, and the pinned C-05 association. It cannot authenticate a live source owner. A production route therefore remains unavailable.

The owner must identify or accept:

1. the consumer-side verifier owner;
2. the permitted issuer-to-assessment-domain mapping;
3. the trust-anchor source and its update/custody rules;
4. verification failure behavior (recommended: fail closed without partial workspace mutation);
5. one public source-issued fixture or an explicit decision that the test-authored fixture remains conformance-only.

The recommended choice is to accept the field semantics and exact source mapping, keep origin status unverified for this demonstration, and require a separately accepted issuer/scope verifier plus public source-issued fixture before any production consumer is selected. The alternative is to revise the named request/response fields or keep the manual external-linked handoff indefinitely.

## Partial, stale, conflict, and review behavior

- A partial/unsupported/blocker response leaves all unaddressed reason IDs pending.
- A response for an older or different snapshot cannot update the current target.
- Same response identity with different bytes rejects.
- Conflicting responses require explicit reconciliation; array order is irrelevant.
- Supersession retains the earlier response and must match request, source, reason, and scope.
- A technical applicability fact cannot remove scientific/rights debt or a prohibition.
- Applicability confirmation never implies qualification.

## Three acceptance questions

1. **Field semantics:** Does the request expose the minimum correct C-AUTH1/C-05 fields, and does the response distinguish technical capability, exact evidence relationship, scoped applicability, and unanswered questions without changing source meaning?
2. **Issuer and scope verification:** Which issuer identities may answer which exact C-AUTH1 assessment domains, who owns the consumer verifier, and what repository-controlled trust anchor should establish that mapping?
3. **Fixture/source mapping:** Does the public example correctly map the accepted authoring output and `C05-PUBLIC-EVAL2-PERTURBED` as one historical fixed-case DEVELOPMENT context, with all stated limitations and no rebinding or campaign claim?

If any answer is “change”, name the smallest schema field, profile rule, source mapping, or fixture assertion to revise. Engineering conformance, source-interface acceptance, and scientific qualification are separate decisions.

## Public/disclosure and authority ceiling

The package uses only public/synthetic repository records. It does not transmit customer data, fetch external links, run numerical work, grant rights, expose protected answers, change CPES, qualify an exam, create a ScoreInput, or launch. A source-interface acceptance would permit only a later bounded consumer proposal; it would not itself supply scientific qualification or rights.

One post-delivery check found no eligible exact owner response. Next boundary: after all three questions have exact source-owner answers and the accepted trust inputs/public fixture exist, propose a separate bounded production consumer. Do not implement that consumer in GOAL-WORKBENCH-06 or GOAL-WORKBENCH-06A.
