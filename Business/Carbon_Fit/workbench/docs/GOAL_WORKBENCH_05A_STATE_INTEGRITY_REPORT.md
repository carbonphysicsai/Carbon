# GOAL-WORKBENCH-05A state-integrity report

Status: implemented for repository acceptance. This is a corrective successor to accepted GOAL-WORKBENCH-05; the prior release and checksums remain historical evidence.

## Review basis

The supplied post-merge review archive was safely inventoried before extraction. Archive SHA-256: `85aa3633410ea41cad9029a5e864e42ca2134c4ff0ec9eb5caef638acd4e2f78`. Its pinned `routing.js` matches merge `3681f7fb10be0c6e278f53d59ff9b022099ef12d`, blob `4d6aa65bca629faa351e2f7d89cd6c4ec934ed1e`, 27,726 bytes, SHA-256 `cacc140b1e9f9904a2360ba7a798d6789619154b54a27ccdf4faf8adb18accf0`.

The review probe reproduced F1–F6 against that exact module. F7 was confirmed as a guarded projection hazard: the v0.5 importer already rejected the state, but direct construction could reach the incorrect projection. The archive did not run the browser, exact C-05 reader, repository gates, numerical runtime, or protected work.

## Repairs

- F1: scientific and rights review reasons are cumulative. An editorial change records an unchanged relationship without replacing the assessment.
- F2: child and grandchild revisions retain origin identity, `invalidated_by`, prohibitions, and unresolved reasons. Parent content remains immutable apart from the expected seal transition.
- F3: `PROHIBITED` remains effective when rights context changes; the proposed context creates a separate review reason.
- F4: the supported field inventory now covers scope, requirements, traces, cases, generator, measurement/score, reference, reconstruction, authoring, rights, disclosure, and deployment paths. Unknown fields reject rather than inherit a favorable default.
- F5: a result is evidence, not activity. One explicit `current_action_ref` binds workflow state, next action, attention bucket, and provenance; ambiguous history requests reconciliation.
- F6: native provenance is not a writable generic label. Exact registered C-05 byte validation creates its verified binding. Links remain locators and manual imports remain assertions.
- F7: applicability confirmation can only project `APPLICABILITY_CONFIRMED_NOT_QUALIFIED`; it cannot produce `QUALIFIED_SCOPED`, rights, eligibility, approval, or launch.

The machine-readable before/after inventory is in `data/goal_workbench_05a_transition_evidence_v1.json`.

## Migration and owner demonstration

Workspace v0.6 reads v0.1–v0.5. Contradictory v0.5 carry labels are reconciled from retained parent/change/invalidation evidence. Insufficient history remains unassessed or review-required; migration appends a receipt and never fabricates an owner decision. Export/reimport revalidates privileged metadata atomically.

The browser demonstration imports the exact eligible retained C-05 fixture, shows the completed observation as source-executed and unresolved, applies a material output change followed by an unrelated commercial edit, revises and exports, then reloads the workspace with the review reason and origin intact. A clean editorial-only path remains cheap. The linked `D-QUAL-PREP-01-FOLLOWUP-01` example remains waiting and is not acknowledged, selected, funded, executed, or approved.

## Coverage and limits

Automated coverage uses the public/synthetic Workbench paths only: pure routing/workflow functions, workspace v0.1–v0.6 parsing and migration, exact C-05 fixture verification, deterministic build/package checks, authority/invariant checks, and generated HTML interaction at desktop and narrow widths with zero external requests.

Local focused results before repository CI: 165 JavaScript engine/workflow/C-05/routing/state/rehearsal tests; 18 source/schema/package tests; 24 authoring-bridge and source-owned C-AUTH1 tests; and three generated-Chrome suites with 40, 29, and 19 checks. Counts are reported per suite and establish no scientific or protected-security percentage. The default Python lacked `python-docx`, the default environment lacked pytest, and sandboxed Chrome could not launch; the same source tests passed under the bundled document runtime, the pinned temporary pytest runner, and the approved headless-browser route. Safari/WebKit was unavailable and is not claimed.

This repair does not authenticate remote operators or issue scientific decisions. It does not implement or dispatch the source-owner response interface, execute a solver/campaign, change C-05 source bytes, change Wave C selection, choose rights, qualify uncertainty, score, publish answers, activate a network, or launch.

## Next decision

Prepared, not implemented, not dispatched: the actual capability/evidence owner may review a small request-bound response contract with separate capability coverage, applicability scope, origin verification, and non-qualification semantics. Restart only when that owner accepts the contract and a public fixture. See `GOAL_WORKBENCH_05_NEXT_INTEGRATION.md`.
