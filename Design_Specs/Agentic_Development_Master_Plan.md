# Carbon Agentic Development Master Plan

**Status:** OWNER-CANONICAL long-horizon development plan.  
**Purpose:** connect the bounded implemented foundation, active Wave B, post-Wave-B C0/C1/C2 network and real-vertical work, post-D parallel E/F/G lanes, the H/I launch-critical branch, and Carbon's longer-term generalized construction, qualification, commercial, and Physics Intelligence architecture.
**Authority:** this document owns long-horizon sequencing and dependency interpretation. `Design_Specs/Build_Out.md` remains the detailed current implementation sequencing authority for the active P0 waves.  
**Non-permission rule:** later waves are not authorized for implementation merely because they are described here.

---

# 1. Development doctrine

> **First prove the judge. Then prove the portfolio. Then deepen the physics. Then widen the search. Bring industry in throughout.**

Engineering doctrine:

> **Abstract the interface now; do not broaden execution freedom now.**

Agentic doctrine:

> **Carbon can widen what participants are allowed to discover without changing who controls the grade.**

Security doctrine:

> **Construction and official evaluation are separate security domains.**

---

# 2. Current implementation state

The canonical `.agent/WAVE.md` records the authoritative ticket evidence.

At the historical constitutional reconciliation, the table below recorded the
Wave-A state. Current selection authority is `.agent/WAVE.md`; as of the
`OWNER-NET-01` planning base, Wave B is active with B-04 selected
`in_progress`, and this master-plan update does not change that selection.

| Ticket | State | Constitutional interpretation |
|---|---|---|
| A-1 | done | orientation / audit provenance |
| A0 | done | stable package/role substrate |
| A1 | done | CI/evidence substrate |
| A2 | done | P0 `TrainingStrategy` schema; future subtype of broader construction strategy |
| A3 | done | Challenge registry + qualification identity/hash gate |
| A4 | done | seed/domain separation; necessary but not sufficient for semantic decontamination |
| A5 | done | bounded scoring engine + fixture pack; not production qualification |
| A6 | done | internal card store + disclosure projection boundary |
| A7 | done | submission identity/FSM/fee/infra-vs-science separation |
| A8 | done | bounded fixture-only TrainEval execution seam; no scientific/production authority |
| A9 | done | bounded seven-tool in-process miner MCP; no transport/production authority |
| A10 | done | bounded fixture leaderboard projection; no official/public-production authority |
| A11 | todo | logs/metrics/redaction/failure tags |
| A12 | todo | invariant suite / Wave-A closeout |

No bounded component is superseded merely by broader design. Future work
wraps/extends/migrates explicitly. Described C/H/I work is planned only and is
not implementation permission.

---

# 3. Wave A — bounded protocol skeleton

Goal:

> prove the software authority boundaries before real scientific execution.

## A8 — TrainEval fixture execution

Scope:

- exact fixture-official request/handle composition;
- deterministic synthetic backend;
- private outcome types;
- A5-only score construction;
- explicit infrastructure/strategy/result separation;
- no public card publication inside A8;
- mechanically non-emission-capable;
- no generic `mock | official` mode;
- no arbitrary miner code execution.

Constitutional gate:

> A8 may exercise lifecycle and contract shapes. It may not be treated as
> evidence that the scientific backend, sandbox, reference process, or
> production scoring path is qualified.

## A9 — MCP surface

Integrate:

- info;
- prior;
- scaffold;
- dry validation;
- estimate;
- submit;
- submission result;
- later ratified, nominally separate research/practice paths.

Hard rule:

> Free/practice signal should reward transferable improvement on the declared
> physics distribution. It must not create material incremental ability to
> infer protected realized cases, mixture, margins, or unresolved ordering
> after controlling for performance on evaluator-held shadow cases unavailable
> to the attacking agent.

## A10 — public leaderboard

Public projection only. No hidden seed, internal measurement vector, private score material, or scientific authority beyond the registered public result state.

Future compatibility:

- Challenge leaderboard is not the same object as a persistent frontier record;
- frontier promotion is a later layer.

## A11 — observability

Structured:

- state;
- failure class;
- runtime;
- retry;
- environment identity;
- redaction;
- evidence IDs where public-safe.

Never expose the hidden exam.

## A12 — invariant suite

Wave A closes only when tests demonstrate the core authority boundaries, not merely happy-path functionality.

---

# 4. Wave B — science-ready authoring skeletons

Goal:

> make the exam authorable without pretending it is already scientifically qualified.

Components:

- generator API;
- canonical case representation;
- nominally separate practice/research execution path;
- scaffold/prior infrastructure;
- Validation Dossier structure;
- qualification manifest schema;
- reference runner abstraction;
- `MeasurementContract` plumbing;
- initial `PhysicalSystemSpec` binding;
- explicit `CandidateOutputContract` hooks;
- `CandidateAssemblyContract` and public `ParameterCatalog`;
- deterministic Strategy compilation into a `ResolvedConstructionPlan`;
- `ChallengeInteractionManifest` capability discovery;
- architecture ratification followed by an exact bounded Wave B service-protocol gate;
- one shared implementation of the B-07S-ratified nominal primitives followed
  by composition and conformance for its exact closed operation set;
- nominal fixture-only practice tasks, paired comparison, research records,
  and receipts;
- immutable PriorPack models, TEST_ONLY fixture staging, and bootstrap
  source-eligibility/publication schemas with negative activation seams;
- an autoresearch utility and conditional hidden-exam leakage gauntlet.

Add constitutional objects where not already represented:

```text
InstanceDistributionContract
SamplingPlan
ReferencePolicy
MeasurementContract
CanonicalChallengeCase
CandidateAssemblyContract
ParameterCatalog
ResolvedConstructionPlan
ChallengeInteractionManifest
ResearchTask
ExperimentRecord
ResearchReceipt
PriorPack
PriorPolicyBundle
PriorPublicationReceipt
PriorDisclosureLedger
PublicEstimand
```

Humans/scientific owners still supply actual population semantics, solver adequacy, measurement qualification, thresholds, and LIVE judgment.

Wave B keeps the ratified Strategy v1 input declarative. The catalog/compiler
seam may register a fixed hybrid backbone or learned component inside a
Challenge-owned assembly contract, but it does not authorize arbitrary
participant code or construction graphs. Real reconstruction and authenticated
remote research transport remain Wave C.

---

# 5. Wave C — post-Wave-B network and real single-Challenge integration

Goal:

> prove localnet/network foundations, execute one real candidate through one
> protected scientific path, then prove temporary direct testnet weights
> without claiming scientific qualification, frontier, settlement, or
> mainnet authority.

Real Bittensor integration begins only after Wave B.

## C0 — network foundation → G2 `LOCALNET_READY`

```text
NET-0  topology / threat model / chain boundary / temporary test policy
NET-1  pinned ChainAdapter / metagraph / wallet / UID / chain errors
NET-2  hotkey-authenticated application transport / replay protection
NET-3  candidate commitment / availability / hotkey binding
NET-4A StructuralLocalnetWeightIntent / TestnetWinnerWeightIntent /
       TreasuryRoutingWeightIntent
NET-4B compiler / constraints / no-winner sink / readback / receipts
NET-5  reproducible localnet E2E harness
NET-6  node / runtime / images / secrets / recovery / observability
```

Bittensor identity/discovery and authenticated transport wrap the Carbon Miner
MCP. Scientific/evaluation modules remain SDK-independent behind a narrow
adapter. G2 is structural/local integration only.

## C1 — real scientific vertical

Required chain:

```text
registered Challenge
→ exact real-path population / SamplingPlan identity
→ real generator + protected truth path
→ producer-independent reconstruction
→ protected evaluation
→ MeasurementContracts
→ Score Pack
→ signed EvaluationReceipt / ExperimentRecord
```

Required implementation work:

- real TrainEval production adapter;
- sandbox/container execution;
- resource controls;
- validator orchestration;
- backend/reference adapters;
- signed evidence receipts/provenance and append-only commitments;
- authenticated MCP end-to-end research/submission/result path;
- secondary execution, validator-disagreement handling, and free-rider
  simulation; and
- Carbon policy-event/typed-intent integration without final frontier
  economics.

## C2 — temporary direct-weight testnet → G3
`TESTNET_ALPHA_DIRECT_WEIGHTS`

```text
C-W1 TestnetWeightEligibilityEvent
C-W2 winner-only expiry / supersession / explicit non-paying sink
C-W3 publication / validator agreement / readback / recovery
C-W4 complete Testnet Alpha Report
```

A Challenge-local result decides only whether a new eligible leader exists;
raw score magnitude never sets weight magnitude. Only exact real C2
provenance may create an expiring `TESTNET_ONLY`, `NON_LIVE`, `NON_SETTLING`
event and `TestnetWinnerWeightIntent`. With no active eligible winner, the
approved non-paying sink is active and participant miners are zero. Exact
window duration and sink identity/custody remain human/security/economic-owned.

G3 proof is hotkey → authenticated MCP → committed candidate → orchestration
→ protected exam → score → signed receipt → testnet event → typed intent →
on-chain weight → readback → expiry/supersession → no-winner sink. Its ceiling
is `NON_LIVE`, `NON_SETTLING`, `NOT_FRONTIER_QUALIFIED`, and
`NOT_MAINNET_ELIGIBLE`.

STOP-SHIP if:

- generator error is material at candidate-resolution scale;
- a candidate exploiting generator/reference bias can outrank one following
  the qualified physical reference;
- task omits a causal input;
- all realistic strategies are scientifically inadmissible;
- winner flips under ordinary reconstruction/evaluation variance;
- hidden material reaches miner-facing transport;
- candidate bytes/hotkey/UID/receipt identity are not exactly bound;
- fixture/mock/practice/estimate/PriorPack/scaffold/partial/failed/contested
  evidence can enter C2;
- no-winner behavior can preserve a stale participant reward; or
- a publisher can accept raw scientific results, score dictionaries, or a
  caller-selected authority Boolean.

---

# 6. Wave D — human scientific qualification

Goal:

> earn the right to call the first exam LIVE.

Human-owned:

- physical envelope and population;
- SamplingPlan adequacy;
- truth/reference hierarchy;
- generator conformance judgment;
- measurement qualification;
- scientific thresholds;
- uncertainty/resolution policy;
- disclosure adequacy;
- Launch Bar decision;
- exact LIVE activation.

First authoritative Burgers direction remains fixed-viscosity unless science review changes it prospectively.

---

# 7. Wave E — Landscape and evidence memory

Waves E, F, and G are parallel lanes that may proceed after Wave D. They do
not block the D→H→I launch-critical branch.

Goal:

> learn from authoritative experiments without allowing learned memory to become scientific authority.

Landscape may:

- ingest structured ExperimentRecords;
- represent method/context/outcome relationships;
- surface hypotheses;
- propose candidate Challenge additions/retirements/versioning;
- propose information-value experiments;
- provide priors and search orientation under leakage controls;
- provide validator scheduling, safe-prefetch, and capacity hints under
  leakage and fairness controls.

Landscape may not:

- define scientific truth;
- dynamically rewrite current Score Packs;
- change a live candidate's registered cases, sampling law, stress mass,
  reference fidelity, measurement depth, gates, or weights;
- change current Challenge-set allocation mid-epoch;
- alter a current frontier decision after observing results;
- expose hidden exam structure.

Doctrine:

> **Landscape proposes; registered contracts decide; independent experiments adjudicate.**

---

# 8. Wave F — product qualification / specialist system

Goal:

> separate search success from deployable product evidence.

Objects:

```text
ProductQualificationPack
ProductBatteryRecord
QualificationRecord
FastPhysicalModel identity
runtime/deployment identity
answerability / escalation policy
requalification triggers
```

Rules:

- rank nominates; evidence qualifies;
- exact artifact/environment/context identity matters;
- adaptation/material change can require a new qualification identity;
- internal learned relations are not validated merely by end-to-end success;
- component qualification does not automatically compose.

---

# 9. Wave G — commercial/private/sponsored engagement plane

Goal:

> make Carbon usable by real enterprises without moving business authority into the judge.

Integrate the `Business/` canon via:

```text
CommercialEngagementSpec
CommercialRightsPolicy
CustomerDisclosurePolicy
ReferenceAccessMode
DeliverableContract
CustomerAcceptancePlan
CommercialEconomicsRecord
```

Priority private modes:

1. customer-hosted reference service;
2. controlled private/VPC evaluation;
3. later air-gapped deployment when qualified.

Products may include:

- Evidence Audit;
- Challenge Feasibility;
- Sponsored Discovery;
- Model Development;
- Qualified Model;
- Lifecycle/Requalification;
- Enterprise Evidence Platform.

Business payment never changes the scientific ruler.

---

# 10. Wave H — frontier promotion and Challenge portfolio

Wave H follows Wave D on the launch-critical branch; Waves E/F/G are not
predecessor gates.

Goal:

> distinguish ordinary Challenge ranking from verified frontier advance.

Objects:

```text
FrontierBaseline
FrontierRecord
LeaderReplacementPolicy
FrontierPromotionExam
FrontierAdvanceEvent
ChallengeSetEpoch
```

Required semantics:

- Challenge-specific scores remain non-comparable by default;
- screen ranking nominates contenders only;
- incumbent and contender use common fresh promotion evidence when required;
- decision states include `SUPERIOR`, `NOT_SUPERIOR`, `INDETERMINATE`;
- unresolved equivalence does not create an arbitrary paid winner;
- at most one paid frontier event per Challenge/window under the registered policy;
- incumbency itself earns no new performance reward;
- a leaderboard lead, G3 direct-testnet event, or testnet weight cannot create
  a `FrontierAdvanceEvent`.

---

# 11. Wave I — treasury and network settlement

Goal:

> settle verified scientific entitlements without forcing scientific accounting into normalized validator weights.

Objects:

```text
TreasuryReceiverSet / TreasuryVault custody boundary
TreasuryAccrualLedger
SettlementObligation
TreasuryRoutingWeightIntent
PayoutIdentity
SponsorRewardPolicy
ValidatorAssignment
ValidatorExecutionReceipt / ValidatorAuditReceipt
ValidatorServiceObligation / ValidatorServiceSettlement
```

Wave I is mainnet-critical and decomposes as:

```text
I-00 treasury receiver / custody / economic contract
I-01 SettlementObligation + immutable accrual/scientific-economic ledger
I-02 TreasuryRoutingWeightIntent + treasury publication
I-03 exactly-once miner settlement
I-04 validator execution / audit economics
I-05 direct-testnet-to-treasury migration + settlement soak
```

Production routing is:

```text
validator weight publication
→ TreasuryReceiverSet
→ TreasuryVault / custody boundary
→ TreasuryAccrualLedger
→ SettlementObligation
→ per-Challenge payout
```

The chain vector contains no raw candidate score, hidden measurement,
Challenge-specific score magnitude, scientific threshold, or payout amount.
Exact custody and economic values remain owner/security/economic inputs.

Must prove on localnet/testnet before production:

- no unintended normalization redistribution;
- no unintended miner-burn/starvation pathology;
- no duplicate payout;
- treasury outage preserves scientific event state;
- payout requires exact Challenge/frontier/event identity;
- governance cannot silently alter scientific merit;
- validators cannot claim Carbon-controlled execution/audit compensation
  without valid assignment/receipt evidence;
- direct-winner and treasury modes never overlap or double-benefit one event;
- UID/key rotation preserves accounting and retries remain exactly once;
- rollback returns to a non-paying state; and
- mainnet has no automatic fallback to direct-winner weights.

---

# 12. Wave J — model-family neutrality

Goal:

> prove Carbon evaluates the registered physical job rather than rewarding a preferred architecture ideology.

Expand reconstruction adapters for qualified families such as:

- neural operators;
- reduced-order models;
- hybrid/symbolic-numeric models;
- other fast physical model families.

Candidate comparison uses registered input/output/task contracts.

No family gets automatic scientific privilege merely because it is mechanistic, symbolic, neural, or fashionable.

---

# 13. Wave K — generalized Agentic Construction Discovery

Goal:

> widen search from bounded training strategies toward construction methods while preserving the independent judge.

Evolution:

```text
TrainingStrategy
        ↓
ModelConstructionStrategy
        ↓
registered richer strategy graph / composition
        ↓
ConstructionProgram
```

Agent capabilities may include:

- architecture choice;
- model composition;
- loss/objective construction;
- optimizer/training curriculum;
- solver/model hybridization;
- representation selection;
- data-generation proposals;
- staged reconstruction procedures.

Hard boundary:

- construction program cannot directly access official evaluator internals;
- construction program cannot define mandatory scientific measurements used to prove itself;
- output must enter an independently controlled reconstruction/evaluation plane.

---

# 14. Wave L — generalized ReconstructionProtocol

Goal:

> replace the P0 assumption “fresh retraining is the only rebuild mechanism” with an explicitly registered producer-independent reconstruction contract.

Architecture:

```text
construction producer
        ↓
registered ModelConstructionStrategy / ConstructionProgram
        ↓
ISOLATED RECONSTRUCTION WORKER
        ↓
ConstructionReceipt + candidate artifact
        ↓
hard security boundary
        ↓
OFFICIAL EVALUATOR
```

`ReconstructionProtocol` owns:

- accepted construction representation;
- deterministic/randomness domains;
- resource limits;
- environment pinning;
- artifacts/receipts;
- failure attribution;
- reproducibility expectations;
- allowed external dependencies;
- isolation requirements.

Arbitrary participant code is not a default capability; freedom expands only after security and scientific qualification.

---

# 15. Wave M — engineering system/product lifecycle

Goal:

> qualify exact deployable artifacts/systems and preserve evidence over change.

Support:

- deployment adapters;
- model portfolios/routers;
- answerability/abstention/fallback;
- digital-twin/control context;
- system composition;
- drift/change intake;
- requalification;
- envelope expansion;
- customer/auditor evidence projections.

System-level success does not automatically validate every internal component.

---

# 16. Wave N — prospective Physics Intelligence

Goal:

> determine whether accumulated Carbon evidence can improve future scientific/engineering decisions.

Candidate decision tasks:

- choose method family;
- select construction intervention;
- predict transfer;
- prioritize simulations/experiments;
- identify high-risk regimes;
- choose qualification evidence allocation.

Qualification requirement:

```text
historical fit
!=
prospective decision lift
```

Physics Intelligence becomes a stronger scientific/commercial product only when prospective tests show that it improves registered decisions against baselines.

---

# 17. Commercial/business development parallel lane

Business development runs in parallel with scientific build, but cannot skip scientific gates.

Recommended company sequence:

```text
Evidence Audit / Feasibility
→ Sponsored Discovery
→ Model Development
→ Qualification
→ Lifecycle
→ Enterprise Platform
→ API / OEM / Frontier Market
→ Physics Intelligence when earned
```

Commercial modes may initially be `OFFCHAIN_ONLY`, later `NETWORK_ELIGIBLE`, and only become `NETWORK_REQUIRED` when privacy, science, reliability, economics, and participant liquidity justify it.

---

# 18. Cross-wave evidence gates

Every wave declares separately:

```text
SPECIFIED
IMPLEMENTED
TESTED
SCIENTIFICALLY QUALIFIED
SECURITY QUALIFIED
NETWORK QUALIFIED
COMMERCIALLY VALIDATED
PRODUCTION QUALIFIED
```

A later state cannot be inferred from an earlier one.

Examples:

- implemented != production-qualified;
- tests passing != scientific qualification;
- paid pilot != repeatable product;
- design-specified network advantage != empirically demonstrated network leverage.

---

# 19. Agent implementation rule

Coding agents should only execute the active authorized ticket/wave.

For every ticket:

1. read `CONSTITUTION.md`;
2. read `.agent/INVARIANTS.md`;
3. read current `.agent/WAVE.md`;
4. read the domain owner specifications;
5. read this Master Plan only for future-compatibility constraints relevant to the ticket;
6. do not implement later-wave abstractions unless the current ticket explicitly requires a compatibility seam;
7. stop if a future constitutional law conflicts materially with the active runtime contract and no migration decision exists.

---

# 20. Closing architecture

Launch sequencing is:

```text
WAVE B unchanged
→ C0 → G2
→ C1 → C2 → G3
→ D → G4
→ H → I → G6
→ G7 treasury-routed mainnet mechanism
```

Waves E/F/G may proceed after D in parallel and do not gate H/I. G5 may be
reached with economic activation off.

```text
DEFINE THE PHYSICAL JOB
        ↓
QUALIFY THE EXAM
        ↓
PEOPLE + AGENTS COMPETE
        ↓
PRODUCER-INDEPENDENT RECONSTRUCTION
        ↓
PROTECTED OFFICIAL EVALUATION
        ↓
ADMISSIBILITY
        ↓
CHALLENGE RANKING
        ↓
COMMON FRONTIER PROMOTION WHEN REQUIRED
        ↓
VERIFIED FRONTIER ADVANCE
        ↓
SETTLEMENT OBLIGATION
        ↓
TREASURY-ROUTING WEIGHT INTENT
        ↓
EXACTLY-ONCE PER-CHALLENGE TREASURY SETTLEMENT
        ↓
PRODUCT QUALIFICATION / LIFECYCLE
        ↓
EVIDENCE MEMORY
        ↓
PROSPECTIVE PHYSICS INTELLIGENCE
```

> **The search surface may widen indefinitely. The scientific authority boundary may not.**
