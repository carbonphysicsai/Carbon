apiVersion: apps/v1
kind: Deployment
metadata:
  name: Carbon-validator
  labels:
    app: Carbon-validator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: Carbon-validator
  template:
    metadata:
      labels:
        app: Carbon-validator
    spec:
      containers:
        - name: validator
          image: Carbon/validator:latest
          imagePullPolicy: IfNotPresent

          resources:
            requests:
              nvidia.com/gpu: 1
            limits:
              nvidia.com/gpu: 1

          env:
            - name: Carbon_USE_BENCHMARK
              value: "true"
            - name: BT_NETUID
              value: "107"
            - name: BT_NETWORK
              value: "finney"
            # Add more env vars or secrets as needed

          volumeMounts:
            - name: pdebench-data
              mountPath: /data/pdebench
              readOnly: true
            - name: Carbon-code
              mountPath: /workspace/Carbon

      volumes:
        - name: pdebench-data
          hostPath:
            path: /mnt/data/pdebench   # Change to your actual data location
            type: Directory
        - name: Carbon-code
          hostPath:
            path: /opt/Carbon     # Mount your code if developing
            type: Directory

      nodeSelector:
        accelerator: nvidia-tesla   # Adjust based on your cluster nodes
