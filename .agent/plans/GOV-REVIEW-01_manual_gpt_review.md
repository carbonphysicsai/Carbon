# GOV-REVIEW-01 plan — manual Codex/GPT review governance

**Ticket:** GOV-REVIEW-01
**Delivery mode:** `SINGLE_TICKET_PR`
**Separate contract PR reason:** `NOT_APPLICABLE` — this ticket is itself the bounded governance deliverable
**Exact base:** `938f94d3cf0ae8ff092b52659c39f0952ec352da`
**Exact base tree:** `ca050935b1b4881931d438e47a7a69a98bd5fcfa`
**Primary Hub map_ref:** `SYSTEM/DEVELOPMENT-SEQUENCING`

## 1. Authority and classification

Preserve B-04 and Wave B. Record the human-selected review process and classify
the already changed live ruleset against still-Greptile-bound repository
authority as `MIGRATION_REQUIRED`. Preserve historical Greptile completion
records while superseding every live/prospective dependency on that service.

## 2. Review contract and enforcement

1. Define one closed receipt schema binding review model, fresh read-only
   context, complete PR diff, exact head/tree, finding counts, dispositions,
   zero unresolved findings, and `PASS` outcome.
2. Require the receipt in an `APPROVED` GitHub review by a non-author human.
3. Validate the live PR, exact head/tree, review state, author separation,
   receipt framing, and finding arithmetic with hostile-input-safe code.
4. Trigger the protected-base `GPT review gate` from review submission,
   editing, and dismissal events without executing pull-request code.
5. Require one approval, dismiss stale approvals on every push, require last-
   push approval and resolved threads, and retain strict normal merge only.

## 3. Governance delivery and bootstrap

Create one governance PR from exact main. Run the detected scope and focused
ruleset/review tests. Before merge, post the exact-head review receipt through
a distinct human approving review. Use the documented one-time bootstrap only
because the default branch cannot run or require the new protected-base review
workflow before this governance contract exists on main.

After normal guarded merge, require exact-main `Merge gate`, then invoke the
guarded ruleset tool from clean exact main using the merged governance PR.
Verify live rules, settings, checks, approval count, stale-review dismissal,
last-push approval, resolved threads, merge-only policy, and zero bypass actors.

## 4. Hub and active ticket reconciliation

Update Hub source because the recurring delivery boundary changes. Pin Hub
authority to the committed authority snapshot, append one decision event,
regenerate deterministically, and leave Wave B/B-04 selection and maturity
unchanged. After this governance ticket closes, synchronize PR #75 with the new
main and resume B-04 under the replacement review contract.

## 5. Validation

Run focused review/ruleset tests, delivery and diff hygiene, strict quality,
compilation, Hub render/drift/validator/routes/browser checks, delivery
preflight, detected-scope CI, exact-head `Merge gate`, exact-main checks, and
guarded live-ruleset verification. Do not run redundant full CPU lanes outside
the one scope-selected exact-head acceptance pair.
