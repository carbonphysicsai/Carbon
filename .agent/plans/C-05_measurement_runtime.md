# C-05 Burgers measurement runtime plan

**Decision:** `OWNER-C1-BURGERS-ALPHA-01`
**Status:** bounded implementation accepted in PR #157; scientific and
protected admission remain open
**Base:** C-04 PR #154 merge
`0cd91bfa6d30f81739ff75e46888f6f1387bd1de`
**Primary Hub map_ref:** `WAVE-C/C-05`

## Scope

Keep B-05's standard-library authoring package and A5 scoring engine unchanged.
Add `carbon.measurement_runtime` as a separate numerical/process package over
immutable public DEVELOPMENT predictions and C-04 candidate-primary outputs.
Every result keeps scientific limits, numerical/reference floors, uncertainty
policy, qualification and ScoreInput absent. A successful computation is raw
qualification-candidate evidence only.

The exact candidate operators are the four C-AUTH1 Burgers proposal quantities:
field-phase RMS, fourfold periodic trigonometric maximum compression, peak
viscous dissipation and bracketed/right-censored energy half-time. The runtime
also retains initial-condition, periodicity, conserved-mean, maximum-principle,
energy-dissipation and weak-local-PDE defects. Definitions, normalizations,
censoring, precision and implementation identity are content-bound.

## Ordered implementation

1. Bind exact candidate source/environment/plan/replica/artifact, public case,
   candidate-primary reference request/policy/environment/artifact, measurement
   contract/environment/implementation, physical scales, grid, times and output
   semantics in one closed request digest.
2. Stage read-only finite float64 candidate/reference copies by bytes, never by
   caller path. Reject changed, mismatched, nonfinite or shape-invalid fields.
3. Run one fixed measurement entry point under the accepted C-03 profile after
   the separate reference worker has terminated. Export a single bounded raw
   report, remove the container, then validate in a separate capped process.
4. Retain raw values, normalization scales, normalized values, censoring and
   unresolved decisions. Emit no ScoreInput, scalar score, gate pass, rank,
   receipt, archive acknowledgement or network/reward effect.
5. Bring forward only D-05 prerequisite calculations: for the exact two recipes,
   three replicas and twelve public cells, separate within-case reconstruction
   variability from across-case variability and record paired rank reversals.
   No count sufficiency or scientific target is inferred.
6. Require exact-head Linux service acceptance for the fixed worker, effective
   controls, resource observations, post-cleanup association and exact replay.

## Stop boundary

D-03/D-04 must qualify the reference; D-05 must supply accepted applicability,
floors, uncertainty, thresholds and a decision-resolution target. Until then the
runtime is structurally incapable of producing score input. Protected admission
also requires the commissioned independent security assessment. C-06 may later
consume only this explicit non-official result after the bounded merge.

## Accepted delivery

Exact head `8dbee54dcd5bdea3a76b22812955e31fbe95e8da` passed required run
`34789621325` and normally merged as
`e3324691666da6b8987764048d2bfff45e0578b4`. The Linux service lane ran 14
tests in 214.70 seconds with exact image/config
`sha256:034960220cc3aad1a6c44b0b79bf1a12e4b66de2878bc3391ec7701dfe45c04d`.
This closes only the bounded engineering slice. D-03/D-04/D-05 acceptance and
all protected, score, archive, network, reward, production and LIVE authority
remain absent.
