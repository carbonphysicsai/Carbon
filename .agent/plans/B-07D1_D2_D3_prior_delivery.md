# B-07D1 -> B-07D2 -> B-07D3 combined prior delivery plan

**Tickets:** B-07D1, B-07D2, B-07D3
**Branch:** `agent/b-07d-prior-delivery`
**Starting base:** `3d48b3569a8ecc68e15f8b4a151a10c804896f52`
**Starting tree:** `bd37c136e00f90f8eed101f2f55df44e2bf54d5e`
**Primary Hub maps:** `WAVE-B/B-07D1`, `WAVE-B/B-07D2`, `WAVE-B/B-07D3`

## Eligibility and authority

Fetched `origin/main` is the verified PR #94 merge. Its accepted head
`5c1f2551aaf3f3d23ed838050db19cd98bf95dd3` passed run `34069874204`,
including Delivery preflight, Development Hub validation, Canonical
environment, and Merge gate. The completion comment confirms the bounded
B-05/B-07C delivery and leaves B-07D1 next. Their remaining conditional status
text is `DOCUMENTATION_LAG`, not authority to replay old ceremonies or change
working measurement/practice code.

The owner directs one branch and one PR for B-07D1 -> B-07D2 -> B-07D3 under
OWNER-DX-03. Each ticket keeps its identity, Definition of Done, stable evidence,
and maturity statement. Dependency satisfaction is tested inside this candidate.
This grouping grants no public publication, qualified ingestion, scientific,
security, rights, production, or LIVE authority.

## Reuse classification

- **KEEP:** B-07A/B-07S `carbon.research` wire types, refs, canonical encoder,
  errors, provider protocols, and closed operation names.
- **WRAP:** B-02B `ParameterCatalog` lookup/validation, B-07B prior-resolution
  pinning, and frozen A9 `PublishedPrior` for private offline compatibility tests.
- **REPAIR:** domain validation absent from the shared wire-value constructors,
  without changing ratified wire fields or canonical encoding.
- **REPLACE:** no active implementation. Historical noisy/generated prior paths
  remain quarantined and are not imported.

## Working contract and slices

1. **B-07D1:** add internal exact index, authorization/publication receipt,
   lifecycle/audit, signer-seam, and compatibility-projection records around the
   shared pack/ref vocabulary. Persist immutable canonical pack bytes and
   append-only metadata. Enforce catalog registration, one-lever anchors,
   estimand/counterevidence pairing, source/class ceilings, aggregate provenance,
   identity acyclicity, atomic index replacement, history, supersession, and
   withdrawal. Run focused D1 tests before D2.
2. **B-07D2:** add a constructor-injected synthetic-record-only publisher,
   immutable evidence snapshots, deterministic aggregation/coarsening, lineage
   caps, joint-cell suppression, lag/window rules, exact candidate checks, and a
   durable cumulative disclosure ledger. Commit ledger delta, private exact-hash
   authorization receipt, and fixture snapshot atomically. Public branches are
   validators/specification seams only and cannot activate. Run D2 and D1-D2 tests.
3. **B-07D3:** add separate concrete public and fixture providers, exact/active
   reads against one snapshot, receipt and withdrawal validation, and deterministic
   catalog/pack-only alignment. Adapt exact returned refs to B-07B's existing
   `PriorResolution` so task start pins immutable inputs. Run combined boundary,
   invariant, resource, race, and installed-wheel tests.
4. Reconcile each ticket/evidence record separately, material decisions, Wave
   registers, implementation ledger, and Hub source. Regenerate derived Hub
   surfaces and run the required validation suite.
5. Inspect the completed diff, run full native diagnostics, push one ready PR,
   require its applicable automated acceptance and Merge gate, and normally merge
   the unchanged tested head using an expected-head guard. Post one completion
   comment for all three tickets and leave B-07E todo/unstarted.

## Human-reserved boundaries

Real public vocabulary/content, estimands, cohorts, cadence, lag, coarsening,
privacy budget, diversity/utility/leakage thresholds, rights determinations,
named public approvers, production keys/custody, qualified official ingestion,
security acceptance, publication/withdrawal authority, and every production or
LIVE state remain absent and fail closed. Fixture values are structurally
synthetic and cannot become production defaults.
