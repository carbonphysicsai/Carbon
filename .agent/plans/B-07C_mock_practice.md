# B-05/B-07C combined delivery plan

**Tickets:** B-05 prerequisite reconciliation and B-07C nominal mock/practice
execution
**Status:** owner-authorized combined candidate in progress
**Branch:** `agent/b05-closeout-b07c-practice`
**Exact base:** `84f46cc2c847ac44b4950cab80b05dbc7a2b7c85`
**Primary Hub map_ref:** `WAVE-B/B-07C`
**Affects:** `WAVE-B/B-05`, `WAVE-B/B-07D1`

## Owner-directed sequencing and packaging

The owner selected B-05 prerequisite completion followed by B-07C ahead of
the recorded B-07D1 selection and directed one branch, one PR, one automated
acceptance, and one normal merge. B-05's substantive dependency is verified
inside this candidate before B-07C implementation. B-07D1 remains `todo` and
unstarted. This exception changes engineering sequence and delivery packaging
only; it grants no scientific, security, rights, network, commercial,
production, qualification, or LIVE authority.

## Conflict and reuse classification

- B-05 runtime and focused tests: `KEEP`; its implementation is present and
  the remaining historical ceremony is `DOCUMENTATION_LAG` under OWNER-DX-03.
- B-07A/B-07S wire types and canonical codec: `KEEP`; B-07C adds no wire
  record, operation, error, enum, alias, or bound.
- B-07B lifecycle, idempotency, retries, cancellation, private records, and
  receipts: `KEEP + REPAIR`; pass the already-ratified immutable task bindings
  to the private executor, enforce the resource-calibration no-scope rule,
  retain the resolved training policy needed for mock draws, and project only
  registered finding definitions plus executor-supplied numeric bands.
- B-02B resolved plan/compiler and B-02C observed resource refs: `WRAP` through
  exact nominal adapters; do not duplicate either owner.
- A4 mock entropy and role derivation: `KEEP + WRAP`; official,
  fixture-official, and qualification contexts are rejected by exact type.
- A5/A6/A7 official authority: `EXCLUDE`; practice constructs no Score Pack,
  `ScoreInput`, score, card, official submission, or official receipt.
- Real practice population, adequacy, uncertainty, disclosure, sandbox, and
  rights decisions: `NEW_OWNER_DECISION_REQUIRED` for real operation and kept
  explicit/fail-closed; synthetic fixture policy remains authorized.

## Implementation slices

1. Verify the current B-05 Definition of Done against current code and focused
   tests; repair only an actual bounded defect.
2. Repair the private B-07B executor/record seam without changing the v2 wire
   contract.
3. Add `carbon.practice` domain-owned scope, measurement-pack, mock-pack
   registry, scaffold provider, nominal executor, aggregate private record,
   mock-only entropy derivation, and B-02C receipt adapter.
4. Exercise scaffold compilation plus all four task kinds, exact pins,
   paired common cases, freshness/idempotency, failure classes, disclosure,
   installed-wheel isolation, and affected predecessor invariants.
5. Reconcile decisions, tickets, Wave status, evidence, current implementation
   ledger, and Hub source/generated projections. Run focused and applicable
   canonical acceptance, then ship one normal expected-head merge.

## Bounded maturity ceiling

The candidate may earn `SPECIFIED`, `IMPLEMENTED`, and `TESTED` for B-05's
verified measurement-authoring scope and B-07C's in-process synthetic fixture
scope only. `SCIENTIFICALLY_QUALIFIED`, `SECURITY_QUALIFIED`,
`NETWORK_QUALIFIED`, `COMMERCIALLY_VALIDATED`, `PRODUCTION_QUALIFIED`, LIVE,
official evaluation, frontier, settlement, weight, and emission remain `NO`.
