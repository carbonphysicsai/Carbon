# B-06 stable evidence — Validation Dossier and qualification manifest

**Tracked evidence class:** startup and first-slice evidence  
**Starting main:** `2500e51042f39a31f5056c74ce2ac5065657ec2a`  
**Starting tree:** `89763523576cef09f40fd8a205aa86d169d679de`  
**Primary Hub map_ref:** `WAVE-B/B-06`  
**Maturity:** bounded working engineering candidate only

## Owner-directed transition

The owner explicitly directed B-06 to begin after PR #87 merged and accepted
the merged B-05 implementation as this ticket's dependency despite outstanding
routine B-05 delivery steps. B-06-D0 records this narrow exception. B-05 is not
described as having a completed review, approval, CI, exact-main, or external-
receipt predicate. The exception does not alter future delivery governance or
grant scientific, security, production, qualification, or LIVE authority.

## Exact startup and one-time external observation

- `git fetch origin main` resolved `origin/main` to exact commit
  `2500e51042f39a31f5056c74ce2ac5065657ec2a` and tree
  `89763523576cef09f40fd8a205aa86d169d679de`.
- The existing B-05 checkout remained on
  `e2d7461e9935ba58f9946c87e67dcceba5f8f2a8` with the same repaired tree.
- A new branch/worktree `agent/b-06-dossier-manifest` was created from the
  exact verified main without altering the B-05 checkout or other worktrees.
- PR #87 was observed once as `MERGED`, merge commit `2500e510...`, head
  `e2d7461e...`. Delivery preflight, Development Hub, and canonical environment
  checks were successful. `GPT review gate` was failed. `Clean dev-container
  image` and main CI were still in progress at observation time. No polling,
  wait, or retrigger followed.

The review entries visible on PR #87 were comments reporting an unavailable
trial review service, not an approval receipt. This record does not infer that
the missing delivery predicate occurred.

## Authority and reuse inspection

The current Constitution, agent instructions, invariants, Wave registers,
B-06 ticket, delivery/delegated-decision protocols, Hub maintenance contract,
Generator Validation, Evidence and Envelope Standards, Launch Bar,
Science/GTM integration and owner decision, Build Out/overlay, scientific
canon, A3 registry, and B-02A/B-03/B-04/B-05 contracts and code seams were read.

The selected `carbon.qualification` package reuses A3 `ChallengeKey` and
tagged digest grammar plus B-02A strict value validation. A3's existing
qualification manifest, registry store, artifact gate, and LIVE lifecycle are
unchanged in Slice 1. B-03/B-04/B-05 outputs remain evidence refs only and
cannot self-qualify.

## Baseline

The canonical wrapper was invoked once and exited 2 because Docker or the
exact Carbon Dev Container is unavailable. This is an infrastructure pause,
not a passing canonical result, and was not retried.

Native Python 3.11.11 using the existing repository-pinned B-05 development
environment ran:

```text
python -m pytest -q tests/cpu/test_registry.py \
  tests/cpu/test_b05_measurement_contract.py \
  tests/invariants/test_b05_measurement_boundaries.py
```

Result: `242 passed in 1.14s`. This macOS result is diagnostic only.

## First coherent slice

The first slice is limited to exact D1–D12 identities, typed evidence and
signer-artifact refs, explicit requirement/completeness/status states, exact
B-06 signer-role bindings, prospective version/supersession, structural
fixture propagation, and deterministic canonical bytes/digest/ref/decode.
The working contract records the later qualification-manifest/active-registry
comparison seam without implementing it.

Implemented files are `carbon/qualification/enums.py`, `errors.py`, `refs.py`,
`model.py`, `canonical.py`, and the package public surface. Focused tests cover
exact slots/titles, canonical evidence ordering and duplicate rejection,
missing/placeholder states, MMS and cross-slot substitution, rationale rules,
signer-role/artifact substitution, cross-Challenge and supersession mismatch,
strict round trip/tamper/duplicate-key rejection, fixture propagation, import
direction, protected refs, and absence of qualification/registry/LIVE APIs.

Native diagnostic result after the slice:

```text
262 B-06 plus directly affected B-05/A3 CPU and invariant tests passed in 1.12s
108 B-06 plus code-authority/package/upstream-surface tests passed in 7.93s
Ruff 0.16.3: all selected B-06 Python files clean
Black 26.5.1: all selected B-06 Python files formatted
```

The required non-gating lead notification mentioning `@harshaa765` was posted
to issue #42 as comment `5554513577`. No response is required for bounded
implementation. Canonical final-tree validation, complete-diff review,
delivery approval, merge evidence, and completion receipts remain absent until
they actually occur.

## Second structural slice

The authority trace found no later ratification of the dependence amendment.
`Generator_Validation.md` commit `40c58a1578c6d16ded4ec147561455df66859697`
is the ratified v2.0 architecture. Commit
`74f8edb04b3b806f4edc75de3ba8c4c6273815fb` changes it to v2.1 and labels
the dependence additions an `OWNER-RATIFICATION PROPOSAL`; the matching
scientific-canon v4.1 additions carry the same pending status. The active B-06
ticket and merged B-05 nominal types authorize an exact structural seam, not
scientific acceptance. B-06-D5 therefore exposes only
`OWNER_RATIFICATION_PENDING` for that submanifest.

Slice 2 implements:

- an independently framed deterministic `DossierEvidenceManifest` identity,
  exact origin-preserving supersession ref, digest, and canonical round trip;
- exact slot-specific B-02A subject bindings for claim scope, physical system,
  target population, SamplingPlan, generator/distribution conformance,
  reference qualification policy, candidate output, and representation, plus
  exact public B-05 measurement identities;
- explicit evidence-to-claim mappings with a closed non-substitution matrix,
  including source-backed narrow MMS/convergence/reference roles and no
  customer-context, product-qualification, or LIVE claim path;
- a statistical scope binding exact estimand, sampling/resampling/independence
  units, case/stratum, interval method, dependence/applicability,
  reconstruction interactions, decision-resolution coverage evidence,
  stopping, missing-cell, and evidence-set identities without numeric policy
  or scientific evaluation;
- prospective/realized evidence accounting with distinct generator,
  reference, infrastructure, candidate, timeout, invalid, non-applicable,
  corrupted, excluded, and valid dispositions;
- opaque D11 disclosure/blinding/decontamination/role-separation bindings and
  D12 limitations bound to exact affected evidence, claims, and scope; and
- fixture propagation, honest incomplete/placeholder states, exact required
  subject graphs, canonical ordering, tamper rejection, and protected record
  representation.

B-03/B-04 reverse-import invariants initially exposed two boundary violations
when the draft imported their runtime ref modules directly. The implementation
was repaired to use the existing B-02A `DistributionConformanceRef` and
`ReferenceQualificationPolicyRef` seams that B-03/B-04 already populate. No
invariant was weakened. The first expanded run was therefore an actual failed
diagnostic (`811 passed, 2 failed`), not a success. After repair:

```text
77 B-06 plus B-03/B-04/B-05 boundary tests passed in 2.85s
815 expanded B-02A/B-03/B-04/B-05/B-06/A3/package/authority tests passed in 135.45s
44 final focused B-06 CPU/invariant tests passed in 0.60s
Black 26.5.1: 11 selected files unchanged
Ruff 0.16.3: all selected files passed
git diff --check: passed
```

The sandboxed Black/Ruff check first failed to resolve PyPI after retries; the
same pinned commands then passed with approved network access. Tool pins were
not changed. The earlier Docker/Dev Container absence had no changed
environment evidence, so canonical Docker validation was not retried.

Supplemental campaign evidence classes and explicit mappings are represented,
but specialized acquisition/result schemas for MMS observed order, mutation,
analytic anchors, witness convergence/disagreement, generator-oracle
adversarial studies, and measurement-floor studies remain later B-06/B-E1
work. Qualification-manifest construction, signer verification, active A3
comparison, and complete synthetic integration remain later B-06 slices.

## Maturity ceiling

```text
SPECIFIED: WORKING ENGINEERING CANDIDATE
IMPLEMENTED: ONLY FOR LANDED B-06 SLICES
TESTED: NATIVE DIAGNOSTIC / FIXTURE STRUCTURAL SCOPE ONLY
SCIENTIFICALLY_QUALIFIED: NO
SECURITY_QUALIFIED: NO
NETWORK_QUALIFIED: NO
COMMERCIALLY_VALIDATED: NO
PRODUCTION_QUALIFIED: NO
LIVE / REVIEW / MERGE AUTHORITY: NO
```
