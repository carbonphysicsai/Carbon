# B-07E evidence — exact resource inspection and fail-closed forecast seam

**Tracked evidence class:** implementation and engineering tests
**Starting main:** `258a35d91f45a1125879123bddccc52428d003b2`
**Primary Hub map_ref:** `WAVE-B/B-07E`
**Maturity ceiling:** bounded SPECIFIED / IMPLEMENTED / TESTED only

## Implemented behavior

`StaticResourceInspectionProvider` compiles the exact request through B-02B,
checks the constructor-pinned Challenge/catalog/compiler/policy/resource-class
identities, delegates resource admissibility and declared ceilings to B-02C,
and deterministically projects only exact public plan requirements. It queries
no private record, Landscape, card, official result, reference, scorer, or
hidden evaluator source.

`UncalibratedResourceForecastProvider` accepts no calibration input and always
returns a deterministic `UNRESOLVED` result after exact static validation. Its
zero-valued structural wall-time placeholder is explicitly paired with closed
`SUPPORT:UNRESOLVED` semantics and is not a runtime prediction. A separately
constructed `TestOnlyCalibratedResourceForecastProvider` exercises synthetic
model/version provenance, calibration windows, resource/hardware/environment
scope, distribution, horizon, uncertainty, stale/mismatch/miscalibration, and
malformed-output validation. TEST_ONLY material has no ordinary-provider or
production-capable construction path.

The five concepts remain distinct: B-07D3 prior alignment, exact static
inspection, non-binding forecast, future Wave-C quote/admission, and B-07B
observed-resource receipt. A9 `estimate` is unchanged. No conversion to A5,
rank, qualification, frontier, treasury, settlement, or emission exists.

## Verification

Native focused and affected diagnostics report:

```text
18 B-07E CPU and invariant tests passed
75 installed-distribution and fresh-wheel tests passed
133 complete invariant tests passed
285 B-02B/B-02C/B-07B/B-07C/B-07E affected tests passed
21 unchanged A9 estimate tests passed
46 B-07A/B-07D3 protocol and prior-provider tests passed
Ruff 0.16.3 and Black 26.5.1 checked the changed Python files
```

The tests cover deterministic static projection, exact refs, cross-Challenge
substitution, B-02C ceilings, missing calibration, unsupported distributions,
stale models and windows, miscalibration, model/scope and hardware mismatch,
non-finite/malformed material, exception redaction, protected-information and
forbidden-outcome vocabulary, quote/receipt confusion, TEST_ONLY construction,
import boundaries, and installed-wheel use.

The canonical wrapper was invoked once and failed closed because Docker is
unavailable on this host. Native results are diagnostics; the ready-head
GitHub acceptance and Merge gate remain the canonical shipping predicate.

## Human-owned inputs and conditional completion

Real calibration datasets, support and miscalibration criteria, hardware
normalization, production model qualification, quote/admission, prices, quotas,
reservations, capacity, and operations/economic policy remain absent. The
ordinary forecast therefore remains `UNRESOLVED`, by design. No scientific,
security, network, commercial, production, qualification, or LIVE state is
earned. B-07E bounded completion becomes authoritative only after the unchanged
candidate passes applicable acceptance and normally merges. B-07F remains
`todo` and unstarted.
