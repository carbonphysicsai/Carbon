# B-07B stable evidence — research tasks, records, and receipts

**Tracked evidence class:** bounded local runtime, conformance, and conditional
closeout evidence
**Starting main:** `30c87a83e821d5fbd27bc87c4620173d712496eb`, tree
`f6120747cc433c8eade4e0c02b28b1c61fa99fb4`
**Branch:** `agent/b-07b-research-records`
**Primary Hub map_ref:** `WAVE-B/B-07B`

## Scope and contract

B-07B implements the three-method `ResearchTaskProvider` behind the exact
B-07A/B-07S wire types. A constructor-bound local requester session owns random
service/requester identity, linearizable idempotency and task state, immutable
start pins, cancellation and terminal-commit ordering, three-attempt provider
retry, captured poll replay, and domain-separated terminal receipts.

Private `ExperimentRecord` values preserve exact contract and resolved-plan
identity, strategy lineage, independently computed paired surface difference,
execution identity, evidence role/class/context, scientific failure category,
resource observations, aggregate refs, and explicit retention/reuse scope.
They are not wire records. Public findings come only from a constructor-owned
allow-list and terminal receipts contain no private record alias or executor
exception text.

## Native diagnostic evidence

The isolated Python 3.11.16 environment records these final candidate checks:

| Scope | Result |
|---|---:|
| B-07B lifecycle and authority boundaries plus B-07A/B-07S regressions | `84 passed` |
| complete invariant suite | `123 passed` |
| installed-wheel and outside-tree optional-dependency isolation | `69 passed` |
| complete CPU suite | `4,377 passed, 2 skipped` |
| Ruff 0.16.3 and Black 26.5.1 on changed Python | clean |
| Development Hub render/check/validation/routes/smoke | recorded in final candidate checks |
| `git diff --check` | clean |

The executable evidence covers concurrent duplicate linearization and conflict
non-mutation; failed-resolution non-insertion; exact compilation, prior, and
resource pins; parent lineage and authoritative resolved-surface difference;
allowed and forbidden transitions; queued/running/terminal cancellation;
poll-sequence capture and byte replay; retry cap and unchanged attempt inputs;
receipt preimage identity; queue/worker infrastructure separation; meaningful
negative finding retention; local requester isolation; request mutation
isolation; missing-rights exclusion; MMS non-substitution; absent epistemic
default; disclosure allow-listing; raw-exception suppression; frozen wire and
operation registries; v1/scoring/TrainEval separation; and wheel installation
outside the source tree.

## Validation availability

Docker remains absent on the host, as already recorded by B-07A, so the pinned
Linux canonical wrapper is locally unavailable and the probe was not repeated.
Native results are diagnostic rather than canonical Linux evidence. The single
ready PR's pinned acceptance jobs and Merge gate are the automated acceptance
authority.

## Maturity ceiling and conditional closeout

The repository candidate coordinates B-07B `done` and B-07D1 next/unstarted,
but that transition is authoritative only after one unchanged ready revision
passes applicable automated acceptance and Merge gate and normally merges with
the expected-head guard. B-07C remains todo because B-05's ordinary delivery
record is incomplete and the earlier owner exception was narrow to B-06.

```text
SPECIFIED: YES — inherited ratified v2 task/receipt contract
IMPLEMENTED: YES — bounded local B-07B lifecycle and private records only
TESTED: YES — exact recorded runtime/conformance scope only
SCIENTIFICALLY_QUALIFIED: NO
SECURITY_QUALIFIED: NO
NETWORK_QUALIFIED: NO
COMMERCIALLY_VALIDATED: NO
PRODUCTION_QUALIFIED: NO
LIVE: NO
```

Task success and receipt existence do not establish scientific success. Rights
authorization is not supplied by this ticket; missing permission remains local
and excludes learned aggregation. Security acceptance, real identity,
authenticated transport, B-07C practice execution, B-07D prior publication,
B-07G dispatch, official v1 submission/scoring, production, and LIVE remain
unavailable or owned elsewhere.
