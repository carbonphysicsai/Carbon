# B-07A stable evidence — shared v2 protocol core and discovery

**Tracked evidence class:** bounded runtime, conformance, and conditional
closeout evidence
**Starting main:** `a0e6635d9c12743db96b9fd774a2d8b584d8fe0a`, tree
`d13530e3f4efc8564f054626d79fd23142d15e9c`
**Branch:** `agent/b-07a-interaction-manifest`
**Primary Hub map_ref:** `WAVE-B/B-07A`

## Scope and contract

B-07A implements the shared `carbon_research_v2` nominal core exactly once,
the ratified `ChallengeInfo` and `InteractionManifest` resources, exact
historical discovery, and a bounded local-only adapter for the two B-07A
operations. It preserves the twelve-operation vocabulary while all downstream
operation implementations remain explicitly unavailable.

The B-07A ticket's older `ChallengeInteractionManifest` wording is reconciled
as documentation lag: `InteractionManifest` is the only wire type and no alias
or second schema exists. Existing A2/A3/A7 and B-02A/B/C/B-05 identities are
kept, not structurally duplicated.

## Native diagnostic evidence

The isolated Python 3.11.16 environment passed:

| Scope | Result |
|---|---:|
| B-07A protocol/discovery runtime, B-07A invariant, installed-wheel/import | `111 passed` |
| B-07S specification invariants, unchanged | `9 passed` |
| B-07A plus affected A9/B-02A/B-02B/B-02C/B-05 regressions | `403 passed` |
| Ruff 0.16.3 and Black 26.5.1 on changed Python | clean |
| `git diff --check` | clean |

The executable evidence covers:

- exact nominal type and subclass/lookalike rejection;
- canonical call/reply/resource round trips and stable content hashes;
- malformed, duplicate, unknown, trailing, non-finite, Boolean/integer,
  collection, depth, text, and byte-bound rejection;
- exact Challenge/ref consistency, historical retrieval, absent Challenge,
  unavailable capability, namespace separation, and error precedence;
- hostile provider outputs rejected without partial disclosure or exception
  text;
- package/import and existing v1/shared-domain regression coverage;
- unchanged B-07S specification invariant assertions.

The installed-wheel fixture builds `carbon-0.9.0`, installs it outside the
source checkout, and imports the new public modules with optional dependencies
blocked. The affected regression selection exercises the unchanged A9 v1 MCP
skeleton and the reused B-02A/B-02B/B-02C/B-05 nominal identities.

## Validation availability

The task inherited the already-recorded absence of Docker, so the pinned Linux
acceptance image is locally unavailable and that infrastructure probe was not
repeated. Python 3.11.16 and the pinned development tools were provisioned into
an isolated native macOS environment. Native focused, package/wheel, and
regression results are diagnostic evidence only; the ready PR's pinned CI is
the automated acceptance authority.

## Maturity ceiling and conditional closeout

The repository candidate may coordinate B-07A `done` and B-07B next/unstarted,
but that transition is authoritative only after one unchanged ready revision
passes its applicable acceptance and Merge gate and normally merges with the
expected-head guard. GitHub supplies dynamic head/run/merge identities and the
brief OWNER-DX-03 completion comment.

Only the following can be earned:

```text
SPECIFIED: YES — inherited ratified v2 contract
IMPLEMENTED: YES — bounded B-07A shared core and discovery only
TESTED: YES — exact recorded runtime/conformance scope only
SCIENTIFICALLY_QUALIFIED: NO
SECURITY_QUALIFIED: NO
NETWORK_QUALIFIED: NO
COMMERCIALLY_VALIDATED: NO
PRODUCTION_QUALIFIED: NO
LIVE: NO
```

The full dispatcher/composition, task lifecycle, domain providers/stores,
practice/scaffold execution, prior publication/retrieval, resource forecasting,
remote transport, credentials, real identity, signing, pricing, settlement,
weights, and production deployment remain outside this ticket.
