# B-01F evidence — Development Throughput Hardening

**Ticket:** B-01F
**Tracked evidence class:** stable, pre-final-tree evidence
**Branch:** `agent/b-01f-development-throughput`
**Starting base commit/tree:** `79293d5b65efef8553c0583ba6cf9bc5d0922ff6` /
`62ccbe40d5df0c8c45403609dc14cc7ca892bb25`
**Decision:** `OWNER-DX-01`
**Plan:** `.agent/plans/B-01F_development_throughput_hardening.md`
**Primary Hub map_ref:** `SYSTEM/AGENT-EXECUTION`
**Hub impact:** `HUB_UPDATE_REQUIRED`; `map_structural`; affects
`SYSTEM/GOVERNANCE`, `SYSTEM/DEVELOPMENT-SEQUENCING`, `SYSTEM/CI`,
`SYSTEM/PR-MAINTENANCE`, `SYSTEM/DEVELOPMENT-HUB`,
`SYSTEM/DEVELOPMENT-HUB/VALIDATION`, `SYSTEM/MATURITY`, `WAVE-B`,
`WAVE-B/B-01F`, `WAVE-B/B-01G`, and `WAVE-B/B-04`
**External completion receipt:** use
`.agent/templates/EXTERNAL_COMPLETION_RECEIPT.md`; final location is dynamic
and must remain outside this reviewed tree

## 1. Starting authority and B-04 preservation

PR #72's external completion receipt established the exact-head checks and
Greptile result, normal reviewed-tree-preserving merge, and exact-main checks
before this branch was created. Those dynamic identities stay external. This
tracked B-01F record uses only its exact starting base at the top and the
substantive B-04 maturity earned by that receipt.

B-04 therefore retains:

```text
SPECIFIED: YES — BOUNDED ENGINEERING CONTRACT
RATIFIED_ENGINEERING_CONTRACT: YES
IMPLEMENTED: NO
REFERENCE_QUALIFIED: NO
SCIENTIFICALLY_QUALIFIED: NO
SECURITY_QUALIFIED: NO
PRODUCTION_QUALIFIED: NO
LIVE: NO
```

B-01F temporarily precedes only B-04 runtime implementation. It does not
demote, rewrite, or reopen the merged B-04 contract.

## 2. Authority set and durable decision

The bounded authority set is `CONSTITUTION.md`, `AGENTS.md`,
`.agent/INVARIANTS.md`, `.agent/WAVE.md`, `.agent/WAVE_B.md`,
`.agent/DELEGATED_DECISION_PROTOCOL.md`, `.agent/DELIVERY_PROTOCOL.md`, the
B-01F ticket/plan, `agent_pack/EXECUTION_PROTOCOL.md`,
`docs/development/ENVIRONMENT.md`, and the Development Hub maintenance
contract.

`OWNER-DX-01` selects:

- one single-ticket PR by default, with a contract-first initial commit and
  vertical implementation/test slices;
- exact-head required checks plus Greptile as the routine correctness gate;
- normal expected-head merge and exact-main verification without another
  owner prompt solely for a clean merge;
- stable tracked evidence plus an external dynamic completion receipt;
- strict delivery hygiene, a canonical wrapper, path-aware CI, stable
  `Merge gate`, live PR-declaration validation, and bounded Hub fan-out; and
- B-01F before B-04 runtime, with B-01G queued but non-blocking.

Human-reserved science, security acceptance, rights, economics,
qualification, `LIVE`, launch, deployment, and production authority is
unchanged and fail closed.

## 3. Expected manifest and exclusions

The exact final manifest is the base-to-final-head Git diff carried by the
external receipt. The fixed manifest plus required one-time semantic Hub
source/event reconciliation and renderer-determined output set are recorded in
the plan. Hard exclusions are:

- all `carbon/**` runtime implementation;
- B-04-D1 through B-04-D10;
- `Design_Specs/Reference_and_TruthAsset_Contract.md`;
- B-04 runtime/tests/fixtures/solver/Julia/measurement/scoring/Dossier work;
- B-01G implementation; and
- B-05, B-06, B-07S, or later scientific implementation.

## 4. Validation and invariants

The final candidate must supply exact results for:

| Gate | Required evidence location |
|---|---|
| delivery-hygiene positive/negative fixtures | exact-head CI / PR |
| path classifier representative manifests | exact-head CI / PR |
| canonical wrapper direct/container/dry-run/fail-closed construction | exact-head CI / PR |
| live PR body/head and stale-declaration cases | Development Hub check |
| Hub renderer/validator/fan-out/route/browser behavior | exact-head CI / Hub check |
| full canonical acceptance | exact-head CI |
| clean dev-container acceptance | exact-head CI |
| ruleset artifact and apply-tool `--dry-run` | exact-head CI / PR |
| `git diff --check` and exact manifest audit | exact-head CI / PR |
| `Merge gate`, Greptile, threads, merge, exact-main | external completion receipt |

A missing or failed result stays missing or failed. No tracked candidate may
claim its own final head, final tree, merge commit, or exact-main result.

## 5. Development Hub fan-out acceptance

The regression changes one semantic selected-ticket transition and compares
semantically affected outputs. The expected affected set is nine generated
surfaces: the two entry pages, Markdown overview, Hub README, generated index,
start orientation, the affected Wave B page, and the two affected ticket
pages. The test does not impose a universal numeric limit and does not hide a
legitimate cross-cutting change. B-01F's authority changes do require one final
coherent Hub data/event/current-position reconciliation and render after all
non-Hub commits settle; include every output whose semantic bytes truly change
and no byte-identical rewrite.

## 6. Conditional completion predicate

The candidate `done` state and B-04 runtime selection become authoritative only
when the exact final head/tree passes every scope-required exact-head check and
`Merge gate`, exact-head Greptile succeeds, every valid finding is repaired,
unresolved thread count is zero, no applicable block remains, normal merge
uses the exact expected head, ordered parents and reviewed-tree equality pass,
fetched `origin/main` equals the merge, and exact-main `Merge gate` succeeds.
The completed normalized external receipt, with no required `PENDING` field,
must then be posted at its declared external location before the prepared
closeout and B-04 selection take effect.

Dynamic completion fields remain `PENDING` here by design. Their authoritative
values belong in the external receipt. Failure of any predicate leaves B-04
runtime paused and does not authorize a dependent ticket.

## 7. Candidate maturity ceiling

```text
SPECIFIED: YES — BOUNDED DEVELOPMENT-SYSTEM CONTRACT
IMPLEMENTED: CONDITIONAL — exact final candidate and completion predicate
TESTED: CONDITIONAL — exact recorded engineering acceptance only
SCIENTIFICALLY_QUALIFIED: NO
SECURITY_QUALIFIED: NO
NETWORK_QUALIFIED: NO
COMMERCIALLY_VALIDATED: NO
PRODUCTION_QUALIFIED: NO
LIVE / LAUNCH / DEPLOYMENT AUTHORITY: NO
```

No B-04 runtime or later-ticket implementation is evidence of this ticket.
