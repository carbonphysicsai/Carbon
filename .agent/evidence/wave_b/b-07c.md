# B-07C evidence — fixture-only mock practice

**Tracked evidence class:** combined-candidate implementation and focused tests
**Starting main:** `84f46cc2c847ac44b4950cab80b05dbc7a2b7c85`
**Primary Hub map_ref:** `WAVE-B/B-07C`
**Delivery:** one branch and PR with prospective B-05 reconciliation
**Maturity ceiling:** in-process synthetic fixture only

## Authority and dependency order

The executor read the root authority set, B-05 and B-07C tickets, current
service and research contracts, A4/A8/A9 boundaries, B-07A/B-07B shared types
and lifecycle, B-02B construction ownership, B-02C resource policy, B-05
measurement authoring, and the Development Hub maintenance contract.

The B-05 audit passed before B-07C implementation began. Decision
`OWNER-B05-B07C-01` records the owner-directed combined-delivery exception;
`B-07C-D1` records the bounded executor architecture. PR #94 subsequently
accepted and normally merged this scope; B-07D1 then became eligible.

## Implemented fixture path

`carbon.practice` adds exact nominal `PracticeScopeStatement`,
`PracticeMeasurementPack`, `MockPracticePack`, `PracticeAggregate`, a
versioned exact-ref registry, a non-champion scaffold provider, and
`MockTrainEvalService`.

The service compiles the scaffold through the real B-02B fixture catalog and
runs all four B-07S task kinds through B-07B:

- reconstruction rehearsal records structural-only completion;
- single practice records a bounded aggregate and observed range;
- paired practice uses common fresh evaluation cases after B-07B independently
  verifies exactly one resolved-plan difference;
- resource calibration has no practice scope, performs no scientific
  comparison, and emits observed resource facts through the B-02C receipt-ref
  seam.

B-07B remains the sole task, replay, lifecycle, cancellation, retry, private
record, and receipt owner. Its private attempt and record now retain immutable
task bindings and the exact resolved training-sampling policy required to use
the compiler's registered abstract randomness purposes. Public finding
projection combines a trusted registered definition with only a finite numeric
band from the executor; raw executor text and caller authority labels are not
accepted.

## Isolation and negative boundaries

The executor derives exact A4 `MockContext` draws from a private process root
and B-07B task identity. Distinct tasks receive fresh cases; replay of the same
task is reproducible without resampling. Training, evaluation, and reference
roles are separate, and paired evaluation cases are common. Official and
fixture-official contexts, packs, seeds, and references have no accepted
nominal path.

The package imports no A5 scoring, A6 publication/card, A7 TrainEval, fees,
leaderboard, chain, or MCP transport surface. Measurement use requires an
exact Challenge-bound `PracticeMeasurementPack`, permitted-rights identity,
and separate non-authoritative configuration. Reference and measurement
failures remain typed scientific non-results. Infrastructure and resource-limit
failures remain non-scientific. Case-level draws, artifacts, references, and
exceptions remain private.

## Current focused verification

Current native diagnostics report:

```text
48 B-07C/B-07B CPU and invariant tests passed in 8.84s
85 B-07C plus installed-wheel/outside-tree tests passed in 5.19s
Ruff 0.16.3: changed Python files clean
Black 26.5.1: changed Python files formatted
```

The counts overlap and are not canonical acceptance. Applicable ready-head CI
and `Merge gate` remain the completion predicate; their run identities belong
in the PR, not this tracked pre-merge record.

The current canonical wrapper was invoked once with `--full` and failed closed
because Docker is unavailable on this host. That is an infrastructure fact,
not a passing result; the attempt was not repeated. Native diagnostics remain
non-canonical, and pinned GitHub CI is the acceptance authority.

## Explicit ceiling and conditional closeout

The fixtures use synthetic population, generator, reference, measurement,
resource, and environment material. `OBSERVED_RANGE_ONLY` is explicitly not a
confidence interval. No real population relationship, adequacy decision,
numerical floor, tolerance, uncertainty or disclosure policy, qualification,
security acceptance, production sandbox, rights decision, network/commercial
authority, or LIVE operation is implemented or claimed.

B-07C is `done` only in this bounded fixture scope: PR #94 accepted head
`5c1f2551aaf3f3d23ed838050db19cd98bf95dd3` and normally merged it as
`3d48b3569a8ecc68e15f8b4a151a10c804896f52` after run `34069874204` passed
all applicable jobs and Merge gate. A completed task remains operational
completion, not scientific success.
