# B-05 stable evidence — measurement and Score Pack authoring

**Tracked evidence class:** startup and pre-final-tree evidence  
**Starting base:** `f1a429de37290b3c7615ca051661a1d727528f78`  
**Starting tree:** `3e25bd65508c5c11d8d67558f9bd699808fc57a9`  
**Primary Hub map_ref:** `WAVE-B/B-05`  
**External B-01H completion receipt:** PR #78 comment `5548725328`  
**Maturity:** bounded working engineering candidate only

## Activation evidence

- `git fetch origin` resolved the expected exact main commit and tree; no pull
  or reconciliation was needed.
- The external receipt reports reviewed B-01H head
  `a4e2e56469f783513e4d5596753f3a973b40f03d`, normally merged as the exact
  starting base above with tree preservation, exact-head and exact-main gates,
  complete-diff review, distinct approval, zero unresolved findings/threads,
  and `NEXT_SELECTED_TICKET: B-05 in_progress but NOT STARTED`.
- Existing worktrees were inspected and preserved. B-05 began on a dedicated
  branch/worktree from the exact selected base.
- The tracked pre-receipt conditional language is retained as historical
  evidence. This file and the coordinated authority update record the later
  external predicate rather than rewriting that history.

## HoH pilot evidence

The B-05 requirements manifest binds the current ticket blob
`8ea74d4de44ab3f775efd2507c9d3b8328a15ed0` and SHA-256
`6d355400d2348709d64f9df19dfd4fc295be010684f028f5bf0a826d3045c18e`.
Its eleven B-05 requirement mappings contain empty verification-command
lists. It is therefore navigation evidence only and cannot produce a VERIFIED
requirement.

The installed supported executable is
`/Applications/ChatGPT.app/Contents/Resources/codex`, reporting
`codex-cli 0.151.0-alpha.7.2`. Running:

```text
python3 scripts/dev/hoh.py probe-codex --codex-executable /Applications/ChatGPT.app/Contents/Resources/codex
```

exited 2 with the fail-closed result that the installed CLI could not prove
the required projection-only filesystem boundary. No private role context was
supplied. B-05 neither modifies nor weakens the harness; work proceeds through
the ordinary single-agent path and harness availability remains a separate
infrastructure fact.

## Authority and seam inspection

The required authority set was read before implementation. Current code
supports these decisions:

- B-02A already owns exact Challenge-bound authoring refs, evidence roles,
  strata, canonical primitives, and fixture-origin discipline.
- B-04 owns reference/truth identity, typed failure, applicability,
  conditioning, uncertainty representation, and positive admission.
- B-02C exposes exact build/replicate/reuse/stop/receipt facts but explicitly
  has no scientific-sufficiency authority.
- A5 consumes only its closed validator-authorized scalar `ScoreInput` and
  exact fixture-only `ScorePackPin`; it owns deterministic hard gates and
  scalar aggregation, not measurement computation or policy authorship.

The selected `carbon.measurement` package preserves these boundaries instead
of widening any completed upstream schema.

## Baseline validation

The canonical wrapper was invoked but exited 2 because Docker is unavailable
on this host. This is `PAUSED_INFRA`, not a passing test result.

A native Python 3.11.11 diagnostic environment using pinned pytest 9.1.1 ran
the focused A5, B-02A, B-02C, and B-04 seam tests:

```text
469 passed in 6.83s
```

The repository's lock is Linux x86_64 only, so no claim is made that the
native Darwin environment is canonical. Exact-head GitHub canonical and clean-
container checks remain required before merge.

## First coherent slice

The first slice implements `carbon.measurement` measurement-definition and
qualification-evidence identities, explicit scientific-value and uncertainty
unresolved bindings, exact stratum applicability, the closed evidence-role /
claim compatibility matrix, canonical bytes and nominal refs, fixed fixture
hash vectors, and a bounded fixture-only in-memory store. A5 is unchanged and
the slice imports neither scoring nor resource policy.

Native diagnostic results after the slice:

```text
24 B-05 CPU tests passed
105 B-05 CPU/invariant plus authority/package tests passed
493 B-05 plus affected A5/B-02A/B-02C/B-04 tests passed
Ruff 0.16.3: all changed Python files clean
Black 26.5.1: all changed Python files formatted
```

These counts overlap and are not a canonical final-tree ledger. Package/wheel
tests used the exact repository-pinned build tools in the ignored diagnostic
environment. Later slices and canonical final-tree CI remain required.

## Working decisions and current evidence ceiling

B-05-D1 through B-05-D6 are recorded in `.agent/DECISIONS.md` and specified in
`Design_Specs/Measurement_and_ScorePack_Authoring_Contract.md`. The required
non-gating SciML/statistics/protocol notification was posted to issue #42,
mentioning `@harshaa765`, as comment `5548824971`. No blocking response was
present when development continued.

Current ceiling:

```text
SPECIFIED: WORKING ENGINEERING CANDIDATE
IMPLEMENTED: ONLY FOR LANDED B-05 SLICES
TESTED: NATIVE DIAGNOSTIC / FIXTURE STRUCTURAL SCOPE ONLY
SCIENTIFICALLY_QUALIFIED: NO
SECURITY_QUALIFIED: NO
NETWORK_QUALIFIED: NO
COMMERCIALLY_VALIDATED: NO
PRODUCTION_QUALIFIED: NO
LIVE / REVIEW / MERGE AUTHORITY: NO
```

Canonical final-tree results, review findings, approval, merge identity, and
completion receipt are intentionally absent until they actually exist.
