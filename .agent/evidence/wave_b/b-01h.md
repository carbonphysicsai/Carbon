# B-01H stable evidence — Carbon Iterative Agent Harness Pilot

**Tracked evidence class:** stable, pre-final-tree evidence
**Starting base:** `650b035dae5629ae75b9e3f549b289f28cdbb9ba`
**Starting tree:** `6d3664b3b29189cde8c7ffdeaa5a7c851f530955`
**Primary Hub map_ref:** `SYSTEM/AGENT-EXECUTION`
**Owner decision:** `OWNER-DX-02`
**External completion receipt:** final PR comment using
`.agent/templates/EXTERNAL_COMPLETION_RECEIPT.md`; identity intentionally not
predicted here.

## Stable starting evidence

- A fetch without pull resolved `origin/main` to the expected starting base.
- PR #75 normally merged B-04 as that exact main.
- PR #75 comment `5513643185` records successful exact-head and exact-main
  gates, exact reviewed/merge tree preservation, zero unresolved threads, and
  `NEXT_SELECTED_TICKET: B-05 in_progress but NOT STARTED`.
- The preexisting B-05 worktree and all of its local changes were left
  untouched; B-01H uses a distinct branch/worktree.
- The B-05 ticket base blob is
  `b0a56077ca1a8990f78a339c2cdab50279993e1c` with SHA-256
  `280be2b3285c86e3494500b80236d4191bfd6fab81b5a95f5a3a58790a6ec086`.

## Stable scope and architecture

The exact owned scope, authority ceiling, role boundaries, state flow, tests,
and exclusions are in the ticket and plan. Runtime state is external to the
reviewed tree; tracked content contains schemas, prompts, protocol, fixtures,
tests, durable decisions, and a non-authoritative exact-ticket B-05 mapping.

## Codex surface verification

The local supported surface is `/Applications/ChatGPT.app/Contents/Resources/codex`
reporting `codex-cli 0.151.0-alpha.7.2`. Its verified `codex exec --help` and
`codex sandbox --help` surfaces support strict inline configuration, ignored
user config, explicit permission-profile selection, a fixed working directory,
ephemeral execution, and JSON Schema output. The adapter uses a custom profile
that denies root and ambient temporary-directory reads, restores only Codex's
minimal runtime paths, grants the exact projection and one private runtime
directory, disables command networking and host-skill discovery, uses the
private runtime as `HOME`, and sets approval policy to `never`. The explicit
`CODEX_HOME` remains usable for client authentication but is outside sandboxed
command readability. The adapter intentionally does not pass the
legacy `--sandbox` flag, which official Codex documentation says would replace
the permission profile. A real installed-CLI probe passed projection-read,
read-only, Developer-write, and sibling-sentinel-denial checks for both role
profiles. A generic no-context preflight using the exact `codex exec` role
configuration reported `custom permissions` before any private role context
was supplied. Authorized evidence commands now replay through the executor
evidence seam; Codex uses the same verified read-only/network-disabled profile,
manual replay fails unavailable, and only the explicit synthetic test executor
uses direct subprocess replay. Any isolation or selection failure makes the
executor unavailable.

## Validation state

The required start baseline was invoked through the canonical wrapper and is
locally `PAUSED_INFRA` because Docker is unavailable. Native host output is not
canonical. A pinned Python 3.11.16 diagnostic reports 48 focused tests passing;
Ruff 0.16.3, strict JSON parsing, the real Codex adapter probe, sanitized
Developer isolation, manifest-authorized evidence-command replay, and diff
hygiene also pass. Exact-head GitHub canonical jobs remain required and belong
in the external receipt.

The first fresh complete-diff review found four boundary defects, the next
exact-head review found seven additional protected-policy, Git-mode/import,
regression/evidence, pause-recovery, and Hub-projection defects, the next review
found one lifecycle-incoherent resume path, the next found three adjacent
resume/retry authorization gaps, and the next found that legacy sandbox modes
did not restrict reads outside a role projection. The latest rereview kept that
finding open because only `codex sandbox`, not the effective `codex exec` path,
was probed and also found unsandboxed controller evidence replay. The candidate
repairs all seventeen with focused regressions, including authority ancestry
plus cumulative scope/mode checks, coherent Tester-pause retry, reauthorized
Tester disclosures, exact scope recomputation, final evidence replay on resume,
an actual-exec custom-profile preflight, and read-isolated evidence replay; no
review receipt is closed until the repaired exact tree receives another fresh
complete-diff PASS.

## Maturity ceiling and conditional result

Before the external completion predicate passes:

```text
SPECIFIED: CONDITIONAL CANDIDATE ONLY
IMPLEMENTED: CONDITIONAL CANDIDATE ONLY
TESTED: NO CANONICAL FINAL EVIDENCE YET
SCIENTIFICALLY_QUALIFIED: NO
SECURITY_QUALIFIED: NO
NETWORK_QUALIFIED: NO
COMMERCIALLY_VALIDATED: NO
PRODUCTION_QUALIFIED: NO
LIVE / MERGE / REVIEW AUTHORITY: NO
```

The candidate may prepare B-01H `done` and B-05 pilot selection, but those are
inert until exact-head checks/review/approval/merge, reviewed-tree preservation,
exact-main checks, and the completed normalized external receipt all pass.
