# C-03 bounded development worker evidence

**Status:** implementation in progress; canonical Docker evidence pending
**Baseline:** `83186be004a4087b27b07278da490dad36785acb`
**Primary Hub map_ref:** `WAVE-C/C-03`

## Delivered boundary

The prospective v1 profile and parent/worker implementation contain the
canonical C-02 adapter on one trusted disposable CPU-only Docker host. The
parent binds and validates exact C-01/C-02/B-02C identities, accepts only public
TRAIN fixtures, retains reconstruction as partial work and grants no score,
reference, summary, reward, archive, network or production effect.

C-03-D1 was notified through issue #42 comment
[`5654488786`](https://github.com/carbonphysicsai/Carbon/issues/42#issuecomment-5654488786).
The owner-facing implementation report is
`docs/development/C03_DEVELOPMENT_RECONSTRUCTION_WORKER_REPORT.md`.

C-02 supplies the validated adapter needed to implement C-03. C-03 supplies an
enforced envelope needed to complete the wider C-02 execution contract. C-02
therefore remains open while C-03 is the sole active implementation ticket.

## Local evidence before canonical acceptance

```text
PYTHONPATH=. /private/tmp/carbon-jax-upgrade-venv/bin/python -m pytest \
  tests/cpu/test_c03_development_worker.py \
  tests/cpu/test_c02_reconstruction_profile.py \
  tests/cpu/test_c01_durable_execution.py -q
45 passed (including C-03/C-02/C-01 and dependency-boundary checks)

full invariant lane: 212 passed
science lane: 34 passed
package/wheel/outside-tree lane: 88 passed
Hub newcomer tests: 9 passed
Hub validator unit tests: 81 passed
Hub validation and route checks: passed

ruff 0.16.3 focused Python paths: passed
git diff --check: passed
```

A focused negative path also proves that an envelope mismatch before the C-01
claim removes the unclaimed container and staged bytes while leaving the exact
attempt queued. Once claimed, start ambiguity remains durable and follows C-01
reconciliation rather than this cleanup path.

The local macOS host has no Docker executable. It supplies no enforcement
evidence. The selected canonical Ubuntu runner must build the pinned worker and
exercise real reconstruction, repeat retention, cancellation/restart,
network/filesystem denial and resource-limit probes. Exact run/head/measurement
records are recorded after they occur, never inferred from local mocks.

## Remaining authority

MQ-015 security acceptance, protected inputs, hostile-host integrity,
production limits/repeat policy, accelerator execution, scientific
qualification, official evaluation, archive finalization, public network,
reward and LIVE remain unavailable. The optional `carbon_jax_research` v0.2
archive is absent/unverified and is not a blocker or substitute for the
canonical `carbon_jax_lab` source.
