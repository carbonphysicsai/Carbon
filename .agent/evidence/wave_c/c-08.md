# C-08 bounded authenticated DEVELOPMENT composition

**Decision:** `OWNER-C1-BURGERS-ALPHA-01`, `C-08-D1`
**Status:** implementation candidate; exact-head acceptance pending
**Base:** C-07 PR #163 merge
`44511ac0e18c1f3b66227e1e46a986074d16ee0c`
**Primary Hub map_ref:** `WAVE-C/C-08`

## Candidate implementation evidence

`carbon.miner_mcp` composes the existing NET-2 authenticated gateway and receipt
journal, the exact A9 seven-tool service and C-07/C-01 real-path orchestration
ownership. It adds three versioned association tables inside the NET-2 journal,
with submit and bind intent durable before source mutation. A9 response objects
stay unchanged. The optional C-07 public extension is canonical, copy-on-read,
bounded by NET-2 and contains literal-false eligibility only.

Focused CPU, invariant and change-classification execution passed 69 tests on
the native Mac diagnostic environment. It covers one authenticated submit
effect, durable ambiguity without redispatch, exact controller reconciliation,
real-path-only binding, source-owned cancellation, changed-account rejection,
query-budget preservation, restart and explicit same-attempt resume, C-01 retry
continuity, response bounds and no authority upgrade. Ruff and Black pass for
the changed implementation/test paths.

One first-pass association gap was confirmed and repaired before checkpoint:
result lookup initially bound requester and submission but omitted the
Challenge. The retained query now joins the original submit receipt and requires
exact Challenge/version; the negative cross-Challenge lookup test passes.

`tests/service/test_c08_miner_mcp_service.py` uses the installed Bittensor SDK
signer/verifier, actual NET-2 gateway/journal, A9 service and a real-path
non-LIVE C-07 binding. It is wired into the existing mandatory Linux isolated
service lane. The native Mac JAX environment lacks Bittensor, so that test was
not locally collected and remains pending exact-head Linux acceptance; it is not
skipped in the required lane.

The test inputs are public/synthetic DEVELOPMENT material. They cannot become a
protected exam, official result, archive acknowledgement, score, network event,
reward, security qualification or scientific qualification.

## Retained failed acceptance candidate

Exact-head run
[`34812538603`](https://github.com/carbonphysicsai/Carbon/actions/runs/34812538603)
tested `c4c88bd1c7af11e87d157a85513707b1557d79be`. Delivery preflight and Hub
validation passed, and the worker image was built as
`sha256:012d955a4c041ebce2fe6429e4a591dd508ca72a5452cb05af694b7446956dcf`,
but required service job `103876612461` failed during collection before any C-08
service workload ran. The controller bootstrap omitted the existing locked
`chain` dependency group, so importing `bittensor.keyfiles.Keypair` raised
`ModuleNotFoundError`. This is retained failed candidate evidence, not C-08
acceptance. The repair adds the already pinned group to that controller
environment; it does not change dependencies, the worker image contents or the
scientific workload.

Exact-head run
[`34813201516`](https://github.com/carbonphysicsai/Carbon/actions/runs/34813201516)
tested `e2689449568057ffa511ab6238a641b0cfe4640e`. The repaired Linux service
lane passed all 16 service tests in 203.790 seconds, including the installed-SDK
C-08 authenticated composition test and the retained C-03 through C-07 worker
controls. Its exact worker image/config identity was
`sha256:37e020574ea8e745eb9ad3909e582b7a0c2f2f72e2600a22a30d05fed41bb20f`,
with zero service-test failures or skips. The overall candidate still failed:
canonical and clean-image quality checks rejected an import ordering change in
`tests/cpu/test_code_authority.py` under the repository's explicit Python 3.10
quality target. The ordering is restored and the exact local quality command
now reports zero new Ruff/Black debt. This run proves the locked controller SDK
repair, but remains failed candidate evidence and is not merge acceptance.

## Retained limitations

No public listener, deployment TLS, production quota/fee, protected data,
official result, real archive acknowledgement, independent security acceptance,
scientific qualification, chain action, reward or LIVE path exists. The accepted
C-07 image remains historical evidence; exact-head C-08 acceptance will build a
new image identity and must not reuse that digest for a rebuilt image.
