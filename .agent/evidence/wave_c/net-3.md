# NET-3 stable implementation evidence

Status: selected; implementation and canonical acceptance pending.
Starting main: 97725a1f4c6c8c65234e96770c847622273fc55e (NET-2 PR #121).
Ticket: `.agent/tickets/NET-3_candidate_commitments.md`.
Primary Hub map_ref: WAVE-C/NET-3. Decision: NET-3-D1.
Notification: https://github.com/carbonphysicsai/Carbon/issues/42#issuecomment-5609284142.

## Expected manifest and reuse

New carbon/candidates model/store/service and tests. Extend NET-2 journal with a
transaction seam; extract its unchanged A7 requester projection. KEEP A7/A8/A6
science, admission, publication, private state and score bytes unchanged. Register
the new canonical root with an exact-delta tested CPU classifier exception.
Update ticket/decision/Wave/current maturity then regenerate the derived Hub.

## Candidate diagnostics

Native Windows diagnostic command:
`.local/diagnostic-env/Scripts/python.exe -m pytest tests/cpu/test_net1_chain_adapter.py tests/cpu/test_net2_transport.py tests/cpu/test_net3_candidates.py tests/cpu/test_select_cpu_profile.py tests/invariants/test_net1_chain_boundary.py tests/invariants/test_net2_auth_boundary.py tests/invariants/test_net3_candidate_boundary.py -q`
Result: 139 passed, 15 explicitly skipped (installed SDK and A3 canonical Linux
filesystem integration), 4.52s. Native output is diagnostic, not canonical.

Canonical acceptance uses the NETWORK_FOUNDATION manifest including NET-1/2/3,
existing MCP, A7 submission and A8 fixture regressions; all invariants, package,
quality, authority, Hub and Merge gate remain. A7/A8 implementation changes or
unknown paths would require full regression. No scientific owner code changed.
Run the one applicable acceptance after the complete candidate is ready.

## Assertions and limits

Authenticated bytes and original order; canonical artifact availability;
copied-artifact/wallet dedup; immutable context; exclusive dispatch; idempotent
completed replay; crash/ambiguous fail-closed state; existing publication before
fixture acceptance; exact score bytes; non-scientific infrastructure/refund;
no hidden evaluation projection, no second evaluator and no LIVE authority.
No localnet service was started. A7 active execution recovery remains explicitly
indeterminate after process loss; completed records and commitments are durable.

Completion is conditional on focused canonical tests/required Merge gate passing
on the unchanged expected PR head and normal guarded merge. GitHub run/head/merge
and completion comment are external facts, not invented here. After delivery,
C-REWARD is next. G2 and C1/C2 scientific/launch states remain unearned.

## First canonical run and repair

Run 34410816684 on abd509a passed all 173 invariants and 1,025 focused CPU
checks; two checks failed. CODE_AUTHORITY required alphabetical order for the
new root. The success-path fixture's default entropy produced a mandatory gate
failure, correctly rejected by the implementation. Repair sorts the namespace
(and exact classifier proof) and uses a fixed synthetic passing fixture whose
independently reconstructed gate input is 0.6186188674954336 under unchanged
threshold 1.0. No scientific threshold/formula or runtime code was changed.
The failed run remains failed evidence; acceptance of the repaired head is required.
