# C-10 independent re-execution evidence

**Ticket:** C-10
**Decision:** `C-10-D1`
**Status:** bounded DEVELOPMENT implementation candidate; acceptance pending
**Base:** PR #168 merge `0ee4c9b8db8339740521e2afc72624c97d8e177a`
**Primary Hub map_ref:** `WAVE-C/C-10`

## Candidate evidence

- C-01 now persists one source-linked `REEXECUTION_OF` relation separately
  from its unchanged infrastructure-retry rule. The source must already have a
  recorded result, all material execution bindings must match, attempt identity
  must advance exactly once, and one source cannot mint a second audit.
- `carbon.reexecution.*` implements a closed request/outcome model while the
  exact C-06 `carbon.audit` receipt package remains unchanged,
  hash-chained restart-visible journal, C-07/C-06 composition and bounded
  positive report projections. It implements no execution engine, scorer,
  signer, archive, publication, weight or reward service.
- The prospective launch intent is durably recorded and C-01-claimed before
  numerical dispatch without pretending that not-yet-produced output digests
  are known. The concrete C-07 evidence request is bound afterward to that
  exact intent, attempt and claim. A restart in the claim/journal transition
  reconciles the same claim instead of minting another execution.
- Exact replay produces no new attempt, worker, receipt or outcome. Changed
  request or outcome bytes conflict. Interrupted execution requires the same
  C-01 claim to resume; a C-07 result persisted before C-10 association is
  attached without another numerical execution.
- Exact reconstruction checkpoint bytes and the frozen prediction, reference
  and measurement bytes are reported as DEVELOPMENT reproducibility only.
  Whole reconstruction artifact envelopes remain independently receipt-bound
  and may differ because they contain execution identity and measured timing.
  Any observed scientific-state difference remains unresolved and quarantined
  because no qualified scientific comparison tolerance is available.
- The selected audit budget retains the C-03 per-worker profile, concurrency
  one, three reconstruction workers plus reference and measurement, and zero
  automatic replacements. It is additional metered audit work and does not
  reset the primary attempt or alter the three-replica scientific plan.

## Local verification

```text
.venv-jax-macos/bin/python -m pytest \
  tests/cpu/test_c10_independent_reexecution.py \
  tests/cpu/test_c01_durable_execution.py \
  tests/cpu/test_change_classifier.py \
  tests/cpu/test_code_authority.py \
  tests/invariants/test_c06_audit_boundaries.py \
  tests/invariants/test_c10_reexecution_boundaries.py \
  tests/invariants/test_c1_dependency_contract_boundaries.py -q
129 passed
```

The Docker-backed test collects locally and is mandatory in the Linux
C-03/C-04/C-05/C-07/C-08/C-10 service lane. Native collection is not service
evidence. Exact-head CI, worker image, timing and merge facts will be appended
after the single applicable acceptance.

## Retained failed acceptance

Required run `34877468997` at candidate head
`90cbb6fa3dd44ea22d15df2f7467343bbb19e731` retained a real Linux worker image
`sha256:ba2de3b7301c09e2c18f925bdda8bce076bd639e643d4e60b43f02592a3e40a9`.
The service lane stopped after 16 passes and one failure because the test had
prospectively copied primary reconstruction receipt envelopes to predict the
fresh run's artifact digests. Actual artifact digests correctly differed: the
manifest includes the new execution identity and observed compile/training
timings. The failure exposed a genuine sequencing/comparison defect rather
than a numerical disagreement.

The repair versions the C-10 request/journal contract, separates prospective
launch intent from post-run evidence association, and compares controller-
validated checkpoint trees instead of timing-bearing artifact envelopes. The
failed run is not acceptance evidence; a new exact-head Linux service run is
required.

Required run `34880275746` at repaired candidate head
`6a6d45c8e51f7a662bfe68991d7b6a0e6bafee79` passed the actual Linux
C-03/C-04/C-05/C-07/C-08/C-10 service lane. Its canonical lane nevertheless
reported `5449 passed, 1 skipped, 2 failed`: one permanent closeout assertion
still classified selected C-10 as `future_reserved`, and the unrelated B-07B
learned-aggregation lifecycle case raised a transient reference mismatch.
The B-07B case passed immediately in focused local reproduction without a
source change. The C-10 closeout assertion is repaired prospectively to assert
this ticket's bounded selected status while retaining the future-ticket rule
for D-wave tickets. This mixed run is retained as failed acceptance evidence;
the amended exact head still requires one applicable acceptance.

## Limits retained

Both executions currently declare the same host and administrator trust
domain. Separate containers and scratch demonstrate fresh bounded execution,
not independent administration, malicious-host resistance or an independent
security assessment. The public fixture is not a protected exam and the exact
comparison is not a scientific sufficiency finding. Every official, archive,
publication, network, weight, reward and settlement path remains unavailable.
