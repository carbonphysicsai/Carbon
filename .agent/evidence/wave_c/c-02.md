# C-02 bounded reconstruction evidence

**Status:** `in_progress`; bounded DEVELOPMENT adapter and repeat capability only
**Baseline:** PR #147 merge `dbd7e255f8a1d507b924bd1f82d309007b201b0b`
**Primary Hub map_ref:** `WAVE-C/C-02`

The owner's C-02 continuation assignment authorizes repair of F1–F4 and a
fixed, public-development repeated-build runner. It does not select a
production repeat count, C-03, official science, protected evaluation,
production, network or LIVE behavior.

## Delivered capability

- Prospective v2 artifacts bind the complete outer tree and exact receipt to
  manifest, checkpoint, configurations, source, required and observed
  environments, TRAIN data, full derived key, normalization, optimizer/step,
  status and inference weights before resume or prediction.
- Completed predictions enforce finite exact-shape float32 output after device
  completion, validate fp32 representability and the shared endpoint-excluded
  grid, preserve requested-time order, and reject non-COMPLETE artifacts on the
  ordinary path.
- A frozen plan reuses B-02C `BoundReconstructionReplicate` and supplied C-01
  `ExecutionAttemptRef` identities. Immutable per-replica records retain
  complete, cancelled, failed and reconciliation-required outcomes without a
  second attempt ledger, resampling or evaluation-pack creation.
- FNO and DeepONet each ran a fixed 3-member native-Mac diagnostic: two
  complete members and one retained cancellation. The successful-subset
  dispersion is descriptive only; singleton uncertainty is unresolved.
- Correct Transolver revision and exact source/license digests are retained in
  installed package data. The adapter comparison remains Carbon-authored and
  non-independent.

## Remaining boundary

C-02 stays `in_progress`. Owner-selected production repeat policy, registered
resource enforcement, hostile-worker isolation/security acceptance and real
scientific qualification remain absent. C-03 is still unselected. Its grouped
MQ-015 decision must supply the threat boundary, image/hardware class, exact
CPU/memory/wall/PID/output limits, filesystem/network controls,
cancellation/descendant cleanup, audit evidence and accepted residual risk.

## Local evidence before canonical acceptance

```text
.venv/bin/python -m pytest -q tests/cpu/test_c02_reconstruction_profile.py tests/cpu/test_c02_receipt_validation.py
27 passed

.venv-jax-macos/bin/python -m pytest -q tests/science/test_c02_jax_conformance.py
26 passed

.venv-jax-macos/bin/python scripts/dev/jax_macos_diagnostic.py repeat-demo /private/tmp/carbon-c02-repeat-demo-20260913
FNO: 2/3 complete, 1 cancelled, wall 5.5700 s, process CPU 12.4421 s
DeepONet: 2/3 complete, 1 cancelled, wall 2.1961 s, process CPU 6.2241 s
Exact reopen: no new replicas, attempts, artifacts or predictions
```

Canonical exact-head acceptance and merge evidence are added only after those
events occur; historical PR #146 counts are not reused as this delivery's
acceptance.

## Owner-directed v3 dependency/scaling/Foundax continuation

C-02-D3 keeps Python 3.11 and pins the newest stable mutually resolving CPU
set within Carbon's canonical range: JAX/jaxlib 0.10.2, NumPy 2.4.6, SciPy
1.17.1, Optax 0.2.8, Chex 0.1.92, Equinox 0.13.8, Einops 0.8.2, Foundax
0.2.0 and PyYAML 6.0.3. Linux remains canonical and Apple arm64 diagnostic;
no accelerator dependency was added. Root `uv.lock` and the Apple hash lock
are reproducible under repository-required uv 0.12.7.

Prospective v3 profiles/artifacts and v2 repeat request identities bind an
explicit `carbon.burgers-physical-scaling.v1` record: `L`, `T`, `U`,
`U*T/L`, each case's `nu*T/L^2`, exact unit-system token and a separate TRAIN
RMS numerical normalization. Manufactured tests independently check temporal,
first/second spatial, conservative nonlinear, forced-residual, round-trip and
periodic-integral behavior on non-unit scales, retaining RMS and maximum error
through truncation and float64 roundoff regimes.

Foundax uses its actual `fno1d`/Equinox API behind the existing public `fno`
semantic and an exact `foundax_fno_v1` implementation pin. Real Optax updates,
512 points, full model/optimizer/EMA/RNG/step persistence, exact resume,
target-free inference, requested-time ordering and rejection paths are tested.
jNO and Orbax are not dependencies. Foundax tag/wheel LICENSE bytes are treated
as EPL-2.0 (SHA-256
`209fe24bf55677bbf81c2b0481c1403201fab57b3b4c609971eba4ec8162b99c`)
despite contradictory MIT package metadata.

The only received archive is the prior v0.1 integration bundle, SHA-256
`532c5786ce4ad08e463ecfb03cd8372016173fe9040d381e108d9517cfb084e9`.
No supplied file contained top-level `carbon_jax_research/*.py`, so expected
v0.2 file-set digest
`sha256:1e539a856a35701ec7ff85880ba9e5a84eaf13970a6212e56f13a32b9d65955e`
is explicitly unverified and no substitution is claimed.

Native Apple-arm64 development evidence before canonical acceptance:

```text
doctor: READY; exact ten-package runtime; CPU; Foundax LICENSE digest matched
lab one-update smoke: compile 0.8326 s; train execution 0.0140 s; total 2.0216 s
focused v3 suite: 124 passed
package-boundary suite: 78 passed
Foundax: real 512-point nonzero update; exact partial/resume equality; finite target-free prediction
installed-wheel Foundax: COMPLETE, 2 steps, finite float32 [2,2,16], target-free prediction
manufactured verification: 3 passed; 12-point cutoff truncation then 18/36-point float64 roundoff floor
```

Final exact-head CI, PR, merge and completion-comment identities are recorded
only after those events. C-02 stays `in_progress`: the production repeat
policy, C-03 resource/isolation/security decision and implementation,
scientific qualification, GPU/TPU evidence, protected evaluation, network,
production and LIVE authority remain absent.
