# C-CORE-06: portable logical research state

Programme #209 v3; working contract, implementation and acceptance pending.
Depends on C-CORE-02; authored research integration coordinates with C-CORE-07.
Primary Hub map_ref `SYSTEM/AGENT-EXECUTION`, impact `map_structural`; affects
WAVE-C/C-02 and public research. Governing authority: constitution/invariants,
current delivery/delegation protocols, C-02 reconstruction, C-03 isolated worker,
the resource-policy contract and the owner platform mandate. Root owns shared
contracts and batches Hub updates before acceptance.

## C-CORE-06-D1

KEEP the existing immutable non-pickle checkpoint, validated logical leaf paths,
shapes/dtypes, optimizer/EMA, RNG/progress, physical scaling, recipe/data/source
and numerical environment identities. WRAP these bytes in a prospective public
research bundle that also records observed source placement. Do not change the
vendored checkpoint source or reinterpret historical official reconstruction.

The first supported bundle is a fully addressable, single-device JAX lab state.
Reject sharded/non-addressable state until a tested explicit gather/restore
policy exists. Record global/logical shape independently from placement. Copy
logical arrays, never compiled executables, host handles or mutable caches.
The existing checkpoint reader/shape validation owns array safety.

Restoring public research state into a different backend requires an explicit
new operation binding, the same principal and the expected immutable source
bundle digest. Preserve recipe, data, scaling, RNG, progress, leaf shape/dtype,
source and numerical library versions; undeclared precision changes reject.
Runtime backend and machine placement may differ prospectively. No bitwise
cross-backend training equivalence is promised. The old same-environment resume
API remains strict and unchanged.

The helper moves state inside an already admitted research worker; it never
dispatches work, creates a grant, validates controller ownership or authenticates
an artifact. The controller/workspace supplies the trusted operation/principal
and expected digest through existing admitted research composition. A hash is
integrity evidence, not scientific or ownership authority. Retained outputs stay
MINER_SELF_REPORTED and cannot replace fresh independent evaluation. An imported
bundle alone must not enter the reconstruction service or validator image.

Integrate with the existing public analysis image's explicit file allowlist and
document the Python research workflow. No extra scheduler, ledger or public MCP
operation is needed: the existing admitted `run_python` action executes research
code and records the operation/image/source/input identities. Coordinate image
source changes with the C-CORE-07 owner before writing shared files.

Alternatives: relaxing the established load_checkpoint environment check is
rejected because it silently changes historical resume. Exporting compiled state
is rejected because placement-specific executables and caches cross trust and
portability boundaries. The new bundle/profile can be removed without migration
of historical artifacts. GPU/TPU transport acceptance still requires real granted
hardware; simulated backend metadata tests cannot satisfy it.

## Acceptance and boundaries

Run actual CPU export/reload/continued-training with exact RNG/progress, declared
engineering floating comparison precision, same-backend strict resume regression,
fixed-state predictions, tampering/foreign principal/reused-operation/precision
rejection, malformed bounded metadata and non-addressable-state rejection.
Exercise the public research image workflow and existing output/cancellation
controls where composition is available. Canonical science/invariant/package/
quality/isolated-service and Hub checks govern normal expected-head delivery.
Hardware cross-backend loading/retraining, scientific comparison calibration,
multi-device state and production containment remain explicit open acceptance.

No new campaign, spending, host installation, protected processing, scientific
qualification, score rule, launch, public deployment or chain action is granted.
The accepted CPU execution class remains the only implemented comparison route
until a prospective competition contract selects otherwise; this ticket does
not pool backend scores or choose scientific thresholds.

Local implementation checkpoint: `carbon/reconstruction/portable_state.py` wraps
the unchanged checkpoint reader/writer/restorer. The original exact-resume
baseline passed (1 test); 10 focused portability tests passed in 22.56 seconds.
Actual CPU export/load preserved all leaves and fixed-state predictions exactly,
and continued training matched the existing strict checkpoint resume. Foreign
principal, reused operation, precision change, tampering, unknown metadata and
unbounded/non-array input cases rejected. The cross-backend branch is exercised
only with explicitly labelled simulated source metadata; no accelerator ran.
Exact repository py310 Ruff and Black passed. Public analysis-image integration,
canonical acceptance, Hub and normal merge remain open.
