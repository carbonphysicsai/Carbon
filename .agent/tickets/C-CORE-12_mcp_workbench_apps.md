# C-CORE-12: optional MCP App for existing Workbench studies

Status: implemented with local diagnostics; canonical delivery pending.
Programme #209 section 7.
Base: C-CORE-08 `153494c0ed220ac1fbdbfc25b80799725ab7c368`; normal merge of
C-CORE-10 precedes final integration. Primary Hub map_ref:
`SYSTEM/AGENT-EXECUTION`; impact `map_structural`. Root integration owner batches
Hub registration and owns shared server, HTTP and package configuration edits.

## Authority and bounded decision

Read Constitution, expanded invariants, Wave C, current delivery/delegation,
Hub maintenance, Build-Out and constitutional overlay; business semantics remain
under `Business/Business_Canon.md`, the existing Workbench specification, and
`Scientific_Challenge_Authoring_Contract.md`. The programme owner authorizes this
optional interface after the working CPU/Julia/MCP path. Future agent autonomy,
customer rights, protected evaluation and scientific qualification do not follow.

C-CORE-12-D1: KEEP WorkbenchScience, registered drafts, ResearchToolAdapter,
existing controller/accounting and Workbench numerical validation/plotting.
WRAP the same capabilities/start/status/result/cancel methods in one optional
typed MCP tool and fixed App resource. No new scheduler, solver, Workbench,
execution ledger, model call, customer-data admission or network deployment.
The view only operates an exact existing public-source request. It cannot edit
physical inputs, register a draft, select a grader, grant rights or change limits.

The trusted constructor receives the exact WorkbenchScience instance already
bound to the same ResearchToolAdapter. An additional operator-provided Workbench
authorizer runs on every tool and direct resource access; an ordinary
`carbon:research` token alone never grants client-design/assessment access.
Neither the service nor authorizer can be selected through request fields.
WorkbenchScience retains exact registered draft, role, grant and lineage checks.
No App resource/tool is installed without this explicit composition. Research
CLI defaults remain unchanged. Protected evaluator access remains unavailable.

## Protocol and package seam

Selected released Apps specification: `2026-01-26`, extension
`io.modelcontextprotocol/ui`, resource MIME `text/html;profile=mcp-app`.
Verified official source and registry: `@modelcontextprotocol/ext-apps==2.0.0`
at upstream `352f6ced4d80772e92b4e7a311854481a8d65b04`, TypeScript
`@modelcontextprotocol/client==2.0.0`, Python `mcp==2.2.0`. Exact build dependencies
and transitive package integrity are recorded by the existing pnpm lock format.

The fixed `ui://carbon/workbench-study-v1.html` resource is linked through
`_meta.ui.resourceUri`. The official App SDK owns initialize/initialized,
tool-input/result notifications and host-mediated tools/call. Bundle dependencies
and the reused Workbench renderer into a package asset at build time; no runtime
repository lookup, remote scripts, credentials, direct fetch or arbitrary HTML.
CSP declares empty network/resource/frame/base-domain lists and no permissions.
Plain clients still receive meaningful text and the same structured result.
Optional UI support cannot become a CPU/GPU/TPU admission prerequisite.

The view retains an exact request and uses existing operation identity on
start/retry; reconnect receives the original tool input/result. Explicit controls
read status/results or request cancellation. Display changes never start work.
Missing/unknown values remain unknown, HELD capacity stays distinct from spend,
and cancellation acknowledgement does not establish resource release. Results
remain DEVELOPMENT / NOT_QUALIFIED; no automatic training reuse or draft
qualification. Existing saved-study and source-lineage contracts remain intact.

## Acceptance and limits

Require exact SDK wire tests for optional discovery, meaningful fallback,
malformed/JSON-string rejection, direct authorization, cross-binding rejection,
unchanged request forwarding, and fixed resource metadata. Browser evidence uses
the official App/AppBridge SDK with sandboxed iframe at desktop/mobile sizes,
actual tool-input/results, user controls and denied external network; this is a
deterministic host harness, not a commercial/paid agent-host claim. Preserve
existing Workbench tests and run package/quality/invariant/Hub checks selected by
current delivery policy. Build asset/source identities must agree, and packaging
must work without repository files. Do not label implementation or local harness
tests as scientific, security, customer, or production qualification.

Upstream references:
- https://github.com/modelcontextprotocol/ext-apps/blob/352f6ced4d80772e92b4e7a311854481a8d65b04/specification/2026-01-26/apps.mdx
- https://apps.extensions.modelcontextprotocol.io/api/
- https://registry.npmjs.org/@modelcontextprotocol/ext-apps/2.0.0
- https://py.sdk.modelcontextprotocol.io/advanced/extensions/

The normal MCP lane in `scripts/dev/ci.sh` requires both the App wire suite and
the real server/HTTP composition suite. The default CPU suite verifies source,
asset, license and CSP identities. The desktop/mobile AppBridge harness remains
a separately recorded local diagnostic, with no paid host or deployment claim.

Completion remains conditional on the tested integrated head, required canonical
checks, normal merge and bounded closeout under current OWNER-DX-03 policy.

## Implementation and local evidence

The optional extension, shared exact-instance composition, independent Workbench
authorizer, fixed packaged HTML/manifest/licenses, and reused Workbench validation
and plot are implemented. The default research surface remains App-free. Direct
HTTP access with only research scope is rejected even when the tool is discovered.
Real SDK wire calls use the existing Workbench domain fixture and retain one task
and its original two reference invocations across repeated start/read/cancel.
This is deterministic engineering evidence; its worker values are synthetic.

Local WSL checks passed 14 combined App/Workbench tests, then 18 focused
App/asset/MCP-boundary checks after source changes. The 18 existing scientific
Workbench JavaScript tests passed. Strict isolated Python 3.10 Ruff/Black passed.
The official App/AppBridge 2.0.0 browser harness passed at desktop and 390px mobile
width, with one numerical plot, four controls, no page errors or external
requests, and a CSP-blocked fetch. The view rejected forged qualification.
These are not real commercial agent-host or paid-model observations.

Two concrete findings changed the implementation: literal callback insertion
prevents replacement-token expansion from changing bundled JavaScript/CSP bytes;
independent cancellation state keeps cancel usable during a deferred synchronous
start. The browser exercised start/cancel concurrency, an observed
CANCEL_REQUESTED state, and subsequent completion without inferring release from
the cancellation acknowledgement. Exact HTML LF attributes preserve package
byte verification on Windows. Excluding the local npm dependency namespace from
Python package discovery preserves the intended installed boundary; original
upstream redistribution licenses accompany the fixed bundle.

The fresh offline wheel check passed with installed npm dependencies present:
only the fixed HTML, manifest and license notices were included, with no npm tree,
build sources or unexpanded shell. Outside the checkout, `packaged_app()` returned
the exact resource with optional SDK imports denied. This establishes package
portability for the fixed view, not host deployment or security qualification.

C-CORE-10 integration retained the Tasks/Skills extensions, supervised shutdown,
optional exact Workbench binding and all required service tests. The combined
App/Tasks/Skills/stdio/HTTP and asset/boundary run passed 39 tests in 82.34 seconds.
This native diagnostic does not replace canonical checks or normal delivery.
No accelerator, native Julia, cloud, campaign, protected-data,
scientific qualification or production security evidence is claimed by this App
slice. Existing Workbench saved-file/lineage authority remains unchanged.
