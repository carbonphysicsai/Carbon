# C-CORE-04: public-source scientific studies in the existing Workbench

Programme: #209; owner authority: the integrated v3 mandate recorded in
`.agent/plans/CARBON_CORE_PLATFORM_EXECUTION.md`. Dependency: C-CORE-02 native
CPU science and standard research access. Status: working contract and
implementation in progress; no completion, scientific qualification or
production authorization claimed.

Primary Development Hub map_ref: `SYSTEM/AGENT-EXECUTION`; impact
`map_structural`. Affects `SYSTEM/BUSINESS-AUTHORITY`, Wave-C reference and
protocol owners. The integration owner batches Hub records before acceptance.

## Working contract and C-CORE-04-D1

KEEP the existing Goal-to-Challenge Workbench, its v0.8 workspace, design
revisions, rights states and source-owned authoring contracts. WRAP the
C-CORE-02 `ResearchToolAdapter` and existing research task/controller/ledger
for public Julia reference-feasibility diagnostics. No second Workbench,
scheduler, numerical solver, research loop or accounting authority is added.

The first consumer exposes a local structural physical-definition check and
one explicit numerical study of the admitted public Burgers TRAIN source
case. The service supplies the exact domain, viscosity, Fourier coefficients,
requested times, units and output layout; the browser does not select a
solver, script, hidden case, numerical tolerance or scientific threshold.
Private customer cases, modified numerical inputs and operating-envelope
sweeps remain unavailable until an appropriate profile and grant exist.

The private service resolves job/design/revision to an operator-installed
draft and compares its physical scope with the submitted request. A browser
rights flag or matching hash does not authorize processing. The fixed public
case and existing grant remain service-owned. Public-source observations do
not establish numerical validity of customer design prose.

The study request and response are closed v1 contracts. Each request carries
one stable operation ID, exact draft binding, physical definition and a
physical-scope digest. Digest numeric values use explicit big-endian binary64
hex tags before sorted JSON hashing, avoiding JS/Python decimal-serialization
ambiguity. Connect, adoption and display-only edits do not dispatch studies;
start/status/cancel/result reuse the same operation. Parallel transmissions
cannot multiply its allowance. Unknown remaining budgets stay null.

Results retain method/environment identities, resource observations,
refinement evidence, requested output arrays and limitations as data. The
browser never evaluates solver text or renders it as executable markup.
Incomplete task results are not candidate failures or successful science.
Cancellation remains requested until the existing controller observes its
dispatched work and cleanup.

## Evidence and draft lifecycle

The first slice uses a standalone versioned study bundle explicitly bound to
an existing v0.8 draft rather than broadening the established workspace schema.
The UI saves/reopens that bundle within the existing design view. Imports are
unverified until reread through the private service. Conflicting response
bytes reject; changing study inputs requires a new design revision rather
than silently replacing the retained study.

Changes to physical scope invalidate the current association, while editorial
and display changes do not rerun the task. Late responses retain their origin
but cannot attach to a changed draft. Historical fixture, assessment, accepted
reference and campaign identities remain unchanged. No automatic public
training-data handoff exists.

The standalone build keeps `connect-src 'none'`. A separate private-service
build permits same-origin requests to fixed scientific-service routes, requires
an explicit output directory, and embeds neither credentials nor Julia. Host
authentication, draft registration and grants are supplied by the private
operator service; a deterministic local fixture is not production security
acceptance or deployment authority.

## Scope and acceptance

Expected implementation: Workbench `src/scientific_studies.js`, its UI module,
small integration changes to the existing goal app/shell/styles, build support,
the Python Workbench scientific-task adapter and its private service routes.
Expected tests: strict request/response/input validation; exact digest vectors;
direct authorization and draft-resolution rejection; stable retries; stale
results; evidence conflicts; save/reopen; remaining-budget interpretation;
failure separation; actual task-adapter integration; desktop/mobile interaction;
offline zero-network behavior; applicable existing Workbench regressions.

Run canonical scope-required tests and gates through current delivery policy.
Native Windows/WSL diagnostics and fixture browser runs retain that label.
Pinned JSON fixtures must preserve their canonical Git bytes: Windows CRLF
checkout transformations cannot be accepted by changing saved hashes or
weakening exact-byte verifiers. The source/admission checker remains required
for both offline and private builds. Root owns required classification,
package/isolation/Hub checks and normal tested-head delivery.

The working implementation has passed all 262 Workbench JS tests and 46 Python
source, authoring, scientific-service and HTTP tests through
`scripts/dev/workbench_science_checks.sh` in a byte-faithful LF source mirror.
This includes the 13 new scientific-study and 15 state-integrity tests.
The script preserves fixture bytes, runs regenerating tests in a disposable
copy and builds offline/private artifacts into separate output directories.
Pinned Node 24.19.0 and development-only python-docx 1.2.0 support this lane;
neither adds a runtime worker dependency. Headless desktop and mobile fixture
interactions also passed, including offline zero-network checks. A local
browser integration also imported an actual Workbench draft, obtained its
operator-registered public source definition, dispatched the existing Julia
task, displayed the 13-by-64 result, saved/reopened it and reread the same
operation with zero page errors. That run used Julia 1.13.0 on CPU with 64/128
point refinement, observed horizon coverage through 20 and accounted for
7,725 numerical milliseconds, two trajectories, two reference invocations and
22,150 retained bytes, with no provider calls. Its local host
authentication is a deterministic fixture, not production authentication;
refinement discrepancy is not a certified error bound. Canonical delivery
acceptance and broader operating-envelope scope remain open.

A separate actual Julia private-HTTP integration test passed (one test,
18.51 seconds): 8,048 numerical milliseconds, two trajectories/invocations,
22,155 retained bytes and zero provider calls. It exercised saved evidence,
replay, stale-draft rejection and completed-task cancellation without another
execution. The actual expired-grant cancellation test passed (one test,
11.79 seconds): after the owned C-04 worker reached `CONTROLS_VERIFIED`, the
fixture grant clock expired and the same authenticated owner cancelled it.
The worker ended with `reconstruction.worker.cancelled`, cleanup was
`CONFIRMED`, and the exact container was absent. Unknown consumption retained
the conservative reservation of 720,000 numerical milliseconds, two
trajectories/invocations and 402,653,184 bytes rather than fabricating a refund;
the operation remains `REQUIRES_RECONCILIATION`. This is reservation evidence,
not measured usage or authorization for another execution. These two tests run
under `tests/service/test_julia_workbench.py` and the existing
`scripts/dev/julia_worker_service.sh` acceptance entrypoint.

Operator build/composition instructions are in
`Business/Carbon_Fit/workbench/SCIENTIFIC_STUDIES_OPERATOR.md`. The exact
controller-owned public source seam imports `DOMAIN_LENGTH` and
`requested_times` from `carbon.generators.burgers_dynamics`; it grants no
protected generator access to miners, browser code or numerical workers.

## Boundaries and conditional completion

`UNASSESSED`, `NOT_QUALIFIED`, unresolved rights, request-only Challenge
preparation and no-launch states remain unchanged. This ticket does not
qualify references, choose science thresholds, enable private customer
processing, register Challenges, execute protected exams, run paid model
campaigns, allocate accelerators or authorize public deployment.

Complete only after the implemented public-source consumer, its affected
checks and Hub reconciliation pass and the unchanged tested head merges under
the current OWNER-DX-03 delivery predicate. Broader Workbench envelope studies,
private data support, multi-method qualification and all unexecuted GPU/TPU
scope remain open in the existing programme; this bounded delivery cannot
close #209 on its own.
