# C-CORE-13: exact registered scientific materials through normal MCP CLI

Programme #209/#210; owner-integrated v3 implementation authority.
Starting source `009d1bb87bd6a7fd59a9dd2b727ffccbc7a2609a`.
Status: implemented with focused local fixture diagnostics; canonical delivery pending. Primary Hub map_ref
`SYSTEM/AGENT-EXECUTION`; map_structural. The integration owner maintains the
shared programme and Hub before acceptance.

## Working decision C-CORE-13-D1

KEEP existing runtime/image loading, `_science`, stdio serving, campaign ownership,
registered material classes, operation identity and accounting. REPAIR the CLI's
assumption that any scientific-task scope selects the single Burgers material.
Normal attachment must select only absent scientific scopes (legacy), exact
single Burgers, exact ordered Burgers plus its registered envelope companion, or
exact advection with the separately bound authored-analysis Julia image.
Reject empty, mixed, unknown, reordered or extra scopes and image-role mismatch.
Recompute expected documents using current registered factories; a schema label
alone grants nothing. Do not create a generic material wrapper, engine, registry,
new task namespace or scientific authority.

Cleanup-only construction may use exact retained owner/generation/grant/runtime
proof after expiry. It cannot admit discovery work or execution through the
cleanup-only connection. Constructors receive no permanent grant bypass: every
actual material call still checks fresh grant authority, and existing cleanup
uses existing cancellation/reconciliation services. Preserve unknown claimed
consumption and HELD never-dispatched capacity semantics.

Own `carbon/miner_mcp/standard_cli.py`, narrow constructor changes in
`carbon/development_session/julia_envelope.py` and `advection_research.py`, this
ticket and focused fixture tests. Do not change `julia_analysis.py` or its pinned
runtime source identity, native numerical source, research ledger/service or
existing exact-type material allowlists. Ask the integration owner before any
other source change. Historical grants, identifiers, image-role separation and
scientific qualification remain unchanged.

## Authority and acceptance

Repository constitution, invariants, Wave, current delivery/delegation and Hub
contracts govern this bounded repair. Domain owners are
`Runtime_Julia_Truth_Oracle.md`, `Reference_and_TruthAsset_Contract.md`,
`Research_Resource_Policy_Contract.md`, `Isolated_Reconstruction_Worker.md`, and
the existing public research/MCP contracts. Build-Out and its constitutional
overlay constrain compatibility; future architecture grants no new authority.

Test valid registered discovery/composition, malformed and mismatched scope
rejection, expired owned cleanup with denied new execution, wrong owner/image
role, and the normal stdio service factory path with fixtures. Fixtures exercise
the implemented contracts without scientific/model/provider execution. Run
affected CLI/material/sequence regressions and required formatting/static checks.
Existing native solver, containment and numerical evidence retain their original
scope; no new research campaign, model call, native experiment or spending is
authorized or necessary. Canonical delivery and normal repository gates remain
required before completion; no security/scientific qualification is claimed.

## Implementation and bounded evidence

`standard_cli` recomputes the closed material combination both when verifying
the retained runtime and when composing scientific consumers. It rejects an
invalid scope before constructing a reference consumer. The normal command
remains `python -m carbon.miner_mcp.standard_cli --configuration <private-profile>`;
`--cleanup-only` retains its existing cleanup purpose. No new client envelope,
tool name, runtime grant, numerical task or image is introduced.

The envelope and advection constructors can verify exact retained ownership
when attaching for cleanup. They do not retain a bypass flag. Their callable
capability/study paths still require fresh admission, while the stdio connection
rejects fresh research registration on a cleanup-only attachment. The advection
material receives the same separately loaded authored image as the existing
research executor; public reference/practice consumers keep their worker image.

Focused local WSL diagnostics (not canonical acceptance): the new 27-case
`tests/cpu/test_standard_scientific_materials.py` suite and the affected C-08/C-04
boundary invariant files passed together (46 tests). An affected CLI/Julia/
advection/sequence regression batch passed 73 tests; its remaining existing
external stdio case required the separately provisioned optional MCP environment.
Running `tests/service/test_standard_mcp_cli.py` in that already provisioned
environment then passed all 12 cases, including actual stdio discovery/workspace
interoperability and restart. This service suite does not launch a numerical
worker or paid provider.
Strict isolated, no-cache Python 3.10 Ruff and Black checks pass for all four
changed Python files. Fixture tests assert that material discovery and denied
expired execution create no numerical operation reservations. The normal stdio
factory fixtures use the real gateway, campaign control and service composition,
with fixture host/registration/transport and no worker launch.

These are consumer-wiring diagnostics, not new native numerical, hardware,
scientific qualification or production containment evidence. Canonical CI,
programme/Hub integration and normal delivery checks remain pending.

## Engineering reference-failure diagnostics

Isolated CI run `35298266520`, job `105455282467`, stopped before judging the
candidate when public TRAIN reference preparation returned a non-SUPPORTED
result. The known parent identity is
`sha256:6ec0da17760c9ff8c8e469311677c846a7f5a9dedee67dd443c5eb62d9eccfad`.
Artifact `10528744427` retains the original JUnit failure, worker manifest and
aggregate traces. It does not contain that request, its typed failure reason,
controller journal or role entropy. Full inputs are unrecoverable from this
retained evidence; the exact cause remains unresolved. Passing PR222/224 runs
used the same relevant reference source, build recipe and dependency lock, but
fresh random cases, so they do not reproduce or resolve this failed case.

An owner-approved test-only wrapper now records a bounded failure entry into the
existing uploaded C03 trace before preserving the original result/exception.
Only request identities recomputed from this synthetic test's existing public
TRAIN records are eligible. The trace retains exact query/method settings,
parent/request/image/launch/snapshot identities, typed outcome/reason, diagnostics,
selected controls/resources and timings. It excludes private paths and unrelated
controller fields, limits nesting/counts/strings and record/file bytes, and never
redraws inputs, retries work, alters solver limits or judges the candidate.
Serialization failure preserves the original outcome and emits a bounded notice.

Six focused diagnostic tests passed in 12.08 seconds without selecting the real
numerical test; strict isolated Python 3.10 Ruff and Black passed. This is an
observability repair, not evidence that the historical reference failure is fixed.
New exact-head canonical acceptance remains required; no blind retry of the failed
head, replacement solver or outcome-selected case is authorized by this repair.
