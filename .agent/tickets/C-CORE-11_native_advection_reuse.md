# C-CORE-11: native periodic advection reuse control

Programme #209; owner v3 authority. Dependency C-CORE-07 / PR #224, starting
head `49e418d3`. Primary Hub map_ref `SYSTEM/AGENT-EXECUTION`, map_structural.
Root is shared integration owner. Status: implemented with focused CPU and actual
isolated Julia service evidence; canonical delivery and Hub reconciliation remain
required.

## Working contract and C-CORE-11-D1

KEEP shared PeriodicDefinition/template/role views and the existing Julia research
carrier, task provider, grant admission, ledger, deadline, output validation and
cleanup. Add one fixed reviewed server-owned public linear-advection material.
It requires BOTH exact prospective scientific-task scope and authorized Julia
runtime. Do not reinterpret Burgers C04 requests, accepted reference identities,
historical Julia source digests, Challenge registration or training authority.

Use periodic one-dimensional constant-speed transport, native L/T/Q units,
Float64 point values at endpoint-excluded uniform coordinates and C-order
time-by-space outputs. Start from a fixed public translating sine case with
explicit case/source identity and shared definition. Numerical requests remain
closed, byte/shape/time/work bounded. Preserve requested-time order/repetition.
Use the fixed first-order conservative upwind Euler difference method, CFL 0.75,
and two grid levels. CFL is a method stability setting, not a scientific gate.
Fine-grid initialization is declared periodic linear interpolation of supplied
nodal values. Report conservation, refinement discrepancy, analytic control
error, completed horizon and resource observations; do not certify error bounds.

This is DEVELOPMENT structural/numerical evidence. The reused carrier's
MINER_SELF_REPORTED provenance is retained, never upgraded to qualified reference
or training support. A fixed method and analytical public control do not create a
qualified Challenge population, acceptance threshold or final-exam evidence.

Root approved this smaller seam instead of widening the C04 Burgers request.
Own new `carbon/reference_runtime/julia/advection.py`, `advection.jl`, new
`carbon/development_session/advection_research.py`, focused CPU/service tests and
this ticket. Shared task/material allowlist, generic supervisor or physical-view
changes must be coordinated with root and C-CORE-08 before editing. Existing
Julia Base-only analysis image may be reused at its actual immutable identity;
server-selected source/input bytes bind the existing operation request digest.
No package installation, unrestricted evaluation in the controller, new image
authority, ledger, loop, scheduler or generic PDE language is introduced.

Alternatives rejected: embedding advection in a Burgers case, promoting an
authored script to trusted truth, adding a second public task engine, or using
language agreement as qualification. Reversal removes only this material and
prospective scope. Current advection definition-only descriptor remains historical
unless a separately versioned execution capability explicitly accompanies it.

## Acceptance and boundaries

Read constitution, invariants, Wave, current delivery/delegation and Hub contracts,
scientific definition/physical authoring owners and C-CORE-07 runtime authority.
Baseline the existing definitions and Julia protocol tests. Test positive,
negative and zero velocity, constant and translating sine controls, conservation,
refinement behavior, horizon/time-order coverage, exact layouts/units, malformed
and nonfinite requests/results, work/byte limits, scope/owner rejection and
one-charge replay. Execute the actual fixed Julia source through the existing
admitted carrier using isolated local ENGINEERING_FIXTURE_ONLY grants; retain
source/image identities, output diagnostics, accounting and exact cleanup.

No cloud calls/spending, campaign grant reuse, protected material, scientific
threshold, reference replacement, LIVE or training-support promotion is permitted.
Only normal tested expected-head merge under OWNER-DX-03 closes this bounded
implementation; broader advection qualification and additional consumers stay
open. Root batches programme/Hub integration at delivery.

## Implemented consumer and use

Trusted service composition injects `PublicAdvectionMaterial(primary,
ledger=ledger, owner=owner, image=image)` through the existing `public_material`
argument. The campaign's prospective `runtime.scientific_tasks` must contain the
exact `advection_scope(image)` in addition to existing `runtime.authored_research`
authority. Existing grants are neither widened nor retroactively edited.

The existing durable research workspace task accepts:

```json
{"kind":"workspace","action":"public_material","arguments":{"name":"julia_advection_study_v1"}}
```

Its `capabilities` material describes this fixed study and usage. The wrapper
validates native output before the carrier finishes accounting, saves the exact
arrays/diagnostics in the owned workspace, and verifies retained snapshot bytes
on replay. The existing task ID, operation identity and ledger preserve one-charge
replay. The C-CORE-08 and C-CORE-11 material allowlist additions occupy independent
exact-class branches; a future combined optional material composition is not
claimed here. No general advection Challenge, Workbench action, validator oracle,
new scheduler or additional execution interface is implied.

## Local engineering evidence, 2026-09-17

Executed seven distinct cases through the existing admitted isolated Julia
carrier: fixed public sine refinement with one-charge replay; constant controls
for negative/zero/positive velocity and unsorted/repeated requested times;
negative translating sine; malformed native units with unsuccessful operation
and cleanup; and the actual durable public workspace task with structural-only
research evidence. Tests inspect each dispatched container name and verify it no
longer exists. All grants are newly created ENGINEERING_FIXTURE_ONLY test grants.
No paid provider invocation, cloud allocation or existing campaign budget was used.

Reused immutable Julia 1.13.0 Base-only analysis image:
`sha256:14a67bdafe2fa6272e9636e7b54c1dd16880259e201873ab2b655cc64e28d4fe`.
Its existing runtime digest is
`sha256:01231f60b513a2fcfe116ed2e03e343529eb44153dadb736ee6d76bdd1aa2f97`.
This is the historical C-CORE-07 analysis image; C-CORE-11 sends its separately
bound reviewed source and request as inputs and makes no new image-build claim.

- Julia source: `sha256:fe14abc9197a58267e009581b5809a85702b5a8f3b6fe9683d9bda71f9ee75dd`.
- Shared public definition: `sha256:5ba21e4e533782f24442a816f41f9289d07d15c2c84bfba5207488b3e257721f`.
- Request: `sha256:1ad1160032426abfd6aaa7e50a3b816e985c407d8df830cf13720141aa3e2cfd`.

For the fixed sine, grids 64/128 took 16/28 updates through horizon 1.0.
Coarse/fine analytic RMS was 0.0015993885292033471 / 0.0007984030568079711;
refinement RMS/max was 0.0008028231388563174 / 0.0018985530406741002.
Observed mean drift was 4.440892098500626e-16 / 3.3306690738754696e-16.
The ledger charged one research trial, 3338 numerical milliseconds and 22011
retained bytes. Native diagnostic time 0.000010747 seconds and 11528 allocated
bytes cover only the internal timed solve; they exclude worker startup and must
not be substituted for the ledger measurement or interpreted as peak memory.

These measurements are one public analytic control. The first-order upwind
method is diffusive; two resolutions and smaller error do not establish a
general convergence order, certified error, qualified reference or scientific
threshold. Fine initialization introduces periodic interpolation error. The
public material retains MINER_SELF_REPORTED provenance and explicit ineligibility
for official evaluation and training support.

Local diagnostic commands, from an existing Linux validation environment:

```sh
python -m pytest tests/cpu/test_advection_science.py -q
CARBON_ADVECTION_JULIA_IMAGE_MANIFEST=/path/to/exact/julia-analysis-image.json \
  python -m pytest tests/service/test_advection_science_service.py -q
```

The image verifier requires its recorded exact parent/source/environment
identities. Run through the repository canonical environment for delivery;
native WSL diagnostics are not relabelled canonical CI. Baseline shared definition
and Burgers protocol tests: 119 passed, 1 skipped (standalone Julia unavailable).
Affected public research/authored Julia/definition regression: 70 passed before
the additional decoder test; focused advection tests subsequently 10 passed.
Actual service cases passed in groups 4 + 2 + 1; after tightening zero-time and
repeated-time layout validation, malformed input and public workspace task were
each rerun successfully. All five changed Python files passed isolated no-cache
Ruff (Python 3.10 target) and Black using `/dev/null` configuration.

Material lesson: Windows checkout line endings initially caused the existing
analysis image's exact source verifier to reject the local runtime wrapper.
Restoring that unchanged file's exact Git blob bytes resolved the mismatch before
any execution reservation; no source-identity check or image pin was bypassed.
Output consistency now checks exact initial rows and repeated-time rows, finite
little-endian arrays, dimensions, metadata identity and recomputed diagnostics.

All-invariant local run: 257 passed and 11 failed from CRLF checkout bytes in
unchanged `a5_fixture_v1.json` and `B-E4_recommended_design_v2.json`. After checking
that CRLF-normalized contents exactly matched their HEAD blobs and restoring
those exact bytes, `python -m pytest tests/invariants -q --lf` passed all 11
previously failing cases. No source changes, digest updates, exemptions or test
suppression were needed. Canonical CI remains the delivery authority.

## Ordered integration with C-CORE-08

The normal dependency merge retains independent exact-type branches for the
existing Julia public reference material, the two-case envelope material and this
advection material. Each admits only its own fixed names; subclasses and other
wrappers gain no additional material access. The incoming CampaignLedger remains
byte-identical, including HELD never-dispatched capacity and retained unknown
claimed-work accounting. The required Julia service script keeps authored,
Workbench, envelope and advection suites with the shared operator image root.

Focused integration checks passed: 29 advection/envelope/public-task/ledger
cases, 14 sequence-accounting cases, and three new material-dispatch cases covering
each exact type, cross-material rejection and unregistered-subclass rejection.
The new synthetic dispatch fixture initially used noncanonical JSON; correcting
its serialization to the existing canonical argument contract made all three
cases pass without changing the production parser. Isolated Ruff and Black passed
for the merged dispatch and regression test. This integration changes neither
native Julia source nor the recorded image/scientific evidence identities.
