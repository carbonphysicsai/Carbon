# C-CORE-07: isolated miner-authored Julia research

Programme #209 / MCP #210. Owner authority is the integrated v3 execution
mandate in `.agent/plans/CARBON_CORE_PLATFORM_EXECUTION.md`. Dependency:
C-CORE-02 CPU native Julia and standard research access. Working contract;
implementation and acceptance pending. Primary Hub map_ref:
`SYSTEM/AGENT-EXECUTION`, impact `map_structural`; affects Wave-C research,
resource and reconstruction worker owners. Root retains shared-contract
integration ownership and batches Hub maintenance before acceptance.

## Contract and C-CORE-07-D1

KEEP the public research workspace, durable research task provider, operation
identity, grant admission, numerical lease, controller watchdog, output parser
and ledger. WRAP the existing `research_carrier.run_script` with one closed
Julia language route and a separately identified miner analysis image. The
controller does not evaluate source and accepts no executable path, package
URL, Docker option, host mount or arbitrary command. Source executes only in
the existing admitted hostile-code worker. No second scheduler, research loop,
accounting system or evaluator is introduced.

The first image includes pinned Julia 1.13.0 and a closed prebuilt Base/standard
library environment. Bind archive checksum, project/manifest, allowed package
artifacts, image, source and bootstrap. Disable startup/history hooks, automatic
installation and uncontrolled depot/load-path lookup. Network denial and a
read-only image make attempted installation unavailable during a request;
test the effective behavior rather than treating environment variables as
containment. The Python analysis image keeps its historical interpretation.

Expose a prospective `run_julia` workspace action only when an exact trusted
runtime scope admits its language, image and environment for this owner.
Preserve all historical Python request encodings, grants and operation IDs;
use prospective versioning wherever the existing closed schema requires it.
Unsupported language or package needs become retained capability requests.
Neither discovery nor a client-selected action grants execution permission.
Direct calls, retries and restarts undergo the same identity/admission checks.

Source and explicitly named own/public input bytes are bounded and hashed.
Reserve work and cleanup through the existing ledger before dispatch; retain
the separate final-reconstruction reserve. Bind language, bootstrap, image,
source/input digests and limits in the new operation identity. Keep cached
compiled code and mutable state private to the worker, role and principal.
Validator workers never consume this image, executable or cache.

Outputs are bounded validated bytes with `MINER_SELF_REPORTED` provenance.
Preserve script, hypotheses, resource observations, failures, outputs and
capability requests. No result qualifies a reference or becomes training
support automatically; TrainingSupportContract and rights/provenance rules
continue to control any later use. Research checkpoints do not substitute for
fresh JAX reconstruction. Julia code cannot choose grader settings, hidden
cases, accepted references or scientific thresholds.

Alternative: embedding JuliaCall in the controller is rejected because it
widens controller authority and installation behavior. A dedicated JuliaCall
worker remains a future measured option. Alternative: a separate Julia job
service is unnecessary because the carrier already owns these controls.
This choice is reversible by removing the prospective action/profile/image;
old Python and fixed Julia method identities do not migrate.

## Implementation and acceptance

Starting dependency is repaired C-CORE-02 commit `4eef017e`; implementation
uses branch `agent/core-platform-07-julia-research` in its own worktree.
Baseline: existing public research workspace and task tests pass (14 tests,
native Linux diagnostic). The working sequence is pinned analysis image and
carrier route, prospective task/admission/MCP composition, then actual isolated
script execution and containment acceptance. No historical V1 task action or
Python image identity is reinterpreted; closed schemas gain a distinct version.

Expected ownership: `carbon/development_session/research_carrier.py`, a
prospective analysis-image module/recipe, public research task and admission
composition, bounded schema/transport additions, focused CPU and actual
isolated-service tests, operator guidance. Coordinate shared files before
editing. Reuse C-CORE-02's pinned Julia archive and worker image inputs; do
not restore archived Julia prototypes or alter fixed validator methods.

Before delivery run affected baseline and regression tests, all invariants,
strict quality/package checks and scope-required canonical CI. Actual service
acceptance must execute a small authored Julia numerical script and exercise
input/output bounds, malformed/nonfinite output, prohibited network/metadata,
read-only/root/credential/socket separation, denied runtime installation,
cross-owner isolation, stable retry, cancellation/deadline and exact cleanup.
Record unresolved consumption rather than granting a refund from process exit.
Containment tests establish their observed development scope only; production
security acceptance remains reserved.

No new campaign, cloud spend, host installation, protected processing, live
economic operation, public deployment or scientific promotion is authorized
by this ticket. Missing external authority blocks its affected runtime route,
not available implementation and local fixture acceptance. Completion requires
the bounded implementation, applicable checks and Hub reconciliation passing
and the unchanged tested head merging under OWNER-DX-03. The broader programme
remains open until its separately required hardware and research outcomes exist.

## Implemented prospective seam and engineering observations

`julia_analysis.py` builds a distinct image atop the existing stripped public
analysis image and admits only exact `runtime.authored_research` scope. The
closed Python carrier gains internal program/bootstrap/typed-output hooks;
historical Python request dictionaries and image semantics remain unchanged.
`DevelopmentWorkspaceTaskSpecV2` adds only `run_julia`; V1's recorded byte digest
is regression-pinned. The same durable task provider, signed gateway, standard
SDK adapter and ledger are used. The CLI reads only a fixed private
`authored-julia-image.json` operator record and verifies its parent and grant.
Absent, changed, revoked, expired and cross-owner authority rejects before work.
The existing campaign runner uses the same exact operator image and prospective
grant; its epoch loop is unchanged. A copied schema enables Julia discovery only
for that admitted campaign, preserving historical `TOOLS` and frozen plans.
Focused authored/campaign/loop diagnostics passed 33 tests in 15.56 seconds;
these exercised no paid provider calls.
After that composition change, all invariants plus the affected campaign and
protocol tests passed (330 tests, 43.08 seconds), and existing CLI/SDK regressions
passed (22 tests, 71.87 seconds). Strict Ruff and Black passed all 17 changed
Python files; `git diff --check` passed. These are native Linux diagnostics.

The source manifest is `carbon/development_session/julia_analysis.py`,
`research_carrier.py`, `research_service.py`, `research_tasks.py`,
`research_tools.py`, `research_campaign.py`, `research_loop.py`;
`carbon/research/model.py`, `canonical.py`, `records.py`;
`carbon/miner_mcp/standard.py`, `standard_cli.py`, `standard_server.py`;
`scripts/dev/build_julia_analysis_image.py`, `julia_worker_service.sh`;
`tests/cpu/test_authored_julia.py`,
`tests/cpu/test_b07a_protocol_core.py` (exact prospective enum entry),
`tests/service/test_authored_julia_service.py`; this contract and
`docs/development/AUTHORED_JULIA_RESEARCH.md`.

Native Linux diagnostics passed all invariants and affected task/MCP tests
(381 tests), plus the existing real stdio/gateway/authenticated-HTTP regression
(31 tests). Package/wheel/outside-tree checks passed (88 tests), and all 6,133
tests collected. Strict CI Ruff arguments and Black formatting are applied; canonical
CI and final Hub reconciliation remain delivery gates, not earned by these runs.

Ten actual Julia carrier/service cases passed across focused runs: numerical
execution and one-charge replay; denied runtime package installation and startup
hooks; malformed JSON, nonfinite arrays, escaping symlink and oversized output;
cancellation; external SDK stdio with signed fixture gateway and server restart;
and deadline cleanup after observing live Julia and a child process. These use
explicit `ENGINEERING_FIXTURE_ONLY` grants, no provider/chain calls, and real
local Docker/Julia. Reference/scientific/security/production qualification remain
unavailable. Existing frozen campaigns and their grants were not used.

The observed analysis image is
`sha256:14a67bdafe2fa6272e9636e7b54c1dd16880259e201873ab2b655cc64e28d4fe`.
A miner-authored sine derivative study reduced self-reported RMS discretization
error from 0.00453478104469084 (32 points) to 0.0011353355724345801 (64 points).
Its measured ledger charge was 3,266 ms, one research trial and 6,132 retained
bytes, with zero provider/reference use. The independent external stdio case
retained eight finite squares, charged 2,322 ms/one trial/3,462 bytes, and
reconnected to the identical result. This is interface and numerical execution
evidence, not a model-quality experiment or qualified physical reference.
Cancellation and deadline cases retained complete 120,000 ms and 60,000 ms
reservations after observed allocation cleanup; unknown work was not refunded.

Initial native acceptance exposed root-only project file permissions inherited
from private build staging. The image recipe now explicitly grants read/traverse
access while removing all write bits. A deadline test originally expected a
failed stdout file that the existing Docker adapter intentionally removes;
the corrected test observes the running worker/child before waiting for timeout.
These failed attempts remain failures; their repaired focused checks passed.
