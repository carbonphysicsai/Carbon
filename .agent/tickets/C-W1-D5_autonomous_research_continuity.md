# C-W1-D5 — Autonomous research continuity

Status: in_progress. Primary map_ref: WAVE-C/C-W1-D5.
Hub impact: map_structural (D4 bounded closeout, D5 selection, real outcome).
Starting main: 54f8aff742b99fd2957fede73ea814f03da49eb6. One selected ticket.

## Authority and observed problem

OWNER-C-W1-RESEARCH-PROGRAM-01 records Ryan's new finite three-campaign program.
PR #206 is merged and its unchanged first real campaign ended voluntarily before
training: the miner requested ordinary trial approval already supplied by Ryan.
The complete raw transcript is retained privately. No quality inference follows.

## Selected change and falsifiable hypothesis

Explicit execution direction and a structured stop contract will let the same
gpt-5-mini-2025-08-07 agent reach a real practice trial without another owner prompt.
Disprove this if campaign two again stops at permission-seeking, cannot reach
training, or gains no usable research continuity. This is one orchestration factor;
scientific rule, model, recipe catalog, sampling law, control and worker bounds stay
fixed. No context summarization or analysis quota expansion is included yet.

## Working contract

New explicit CLI agent-policy v2 binds prompt/tool hashes into the manifest and
epoch plan. Default v1 preserves historical plans and terminal behavior. Existing
authenticated twelve-operation research protocol is unchanged. The trusted local
supervisor adds a stop tool alongside its existing selection tool. Closed reasons:
budget, plateau, no_feasible_action, unresolved_failure, cancellation. Nonempty
supporting evidence is agent-reported, never independently verified science.

One free-text-only answer receives one deterministic, journalled clarification;
another ends the epoch. Immediate structured stop remains legal, including zero
trials. No forced minimum, automatic trial, extra authority or reward is created.
Provider attempts and costs remain metered; context/time/money guards still apply.
Restart reconstructs the same history from retained responses and clarification.
Changed policy/plan bytes fail before another dispatch. Raw transcripts persist.

## Ownership / KEEP -> REPAIR

KEEP B-07, C-03, compiler, data roles, recipe execution, C-05 v3, balanced-v2,
C-06/C-07 sources, C-10, accounting and reward simulation. REPAIR local instructions
and terminal-stop semantics in research_agent_policy.py with small loop/controller
bindings. C-MLP-02/#207 owns launchpad admission/control and adds loop checkpoint
hooks; coordinate these seams, do not implement a competing launcher or grant.

## Plan and verification

1. Preserve campaign-one evidence and record this prospective contract.
2. Implement versioned instructions/stop, replay and bounded clarification tests.
3. Reconcile board/Hub; classify honestly, run required acceptance and normal merge.
4. Freeze campaign-two implementation/images/policy, data and remaining program
   accounting before dispatch through the existing run/status/resume interface.
5. Run, report and classify observed constraints; select a further bounded change
   only from evidence. Never mutate a frozen running campaign.

Baseline canonical loop/tools/controller: 18 passed. Focused modified boundary:
30 passed. Fixtures test control flow only; these are not real miner observations.
Test malformed stop, immediate stop, one-reminder bound, replay, policy migration,
context admission and preservation of numerical/final counters. Required acceptance
is separate from campaign spend. Existing worker isolation is unchanged.

## Completion / boundaries

Engineering completes only after required acceptance and normal expected-head
merge. Experiment conclusions require the actual finite second campaign or an
exact unavailable external prerequisite. No qualification, payment or chain write.
No private key or protected final data is included in tracked records.
