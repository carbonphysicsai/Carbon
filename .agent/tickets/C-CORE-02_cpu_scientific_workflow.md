# C-CORE-02: native CPU science and standard research access

Programme: #209; MCP workstream: #210. Owner authority: the v3 mandate in
`.agent/plans/CARBON_CORE_PLATFORM_EXECUTION.md`. Status: implementation in
progress; no completion or qualification claimed. Dependency: C-CORE-01 #211.
Starting main: `689d5e0f4212c3239c33077cb7d96a19d2e7db7f`.

Primary Hub map_ref: `SYSTEM/AGENT-EXECUTION`; impact `map_structural`.
Affected owners: WAVE-C/C-02, C-03, C-04, C-08 and SYSTEM/PROTOCOL-AUTHORITY.
The scientific Wave board and separately owned campaign grants are unchanged.

## Working contract and C-CORE-02-D1

KEEP existing reconstruction profiles, ReferencePolicy/TruthAsset authority,
CampaignLedger, campaign owner lock, signed gateway, research tasks and Workbench
draft lineage. WRAP those services with typed MCP and closed native Julia tasks.
REPAIR transport replay identity without changing durable business identity.
No second admission system, task ledger, scheduler or research loop is created.

One integration owner owns ticket, packaging, shared contracts and delivery.
Independent contributors own catalogue/definition files, MCP adapter/CLI files,
and Julia/reference files respectively. Integrate consumers incrementally.

The prospective supported catalogue derives from exact implemented JAX profiles.
Preserve frozen campaign catalogues and historical Torch wrapper identifiers and
licenses. Verify actual train/export/reload/predict while Torch imports are denied.
Shared closed physical definitions reuse authoring and Burgers case contracts;
the second advection descriptor is definition-only until separately implemented.

Pin MCP SDK 2.2.0 and test its real stdio and authenticated Streamable HTTP wire
interfaces. Bind external identity to an operator-composed Carbon principal and
existing grant; callers cannot select a principal, script path or budget. Typed
objects replace external JSON-string envelopes. Resources and prompts are versioned.
Use durable start/status/cancel fallback tools while SDK durable Tasks are absent;
never advertise unsupported negotiation. Separate external client interoperability
from numerical or paid agent evidence. No public service is deployed here.

Native Julia starts with a fixed subprocess protocol and Julia 1.13.0, verified
archive hash, Base-only Project/Manifest and immutable image. Its finite-volume
study is a new DEVELOPMENT diagnostic identity. Existing accepted primary and
historical request identities remain intact. Method settings are registry bound;
no user-selected grader or acceptance threshold. Byte/shape/order/precision/unit
bounds, supervised execution, typed reference failure and observed cleanup apply.
Actual analytic controls and refinement observations do not qualify the reference.

Alternatives rejected: unrestricted controller eval, source translation, replacing
the reference from language agreement, duplicating campaign state, silently mutating
frozen discovery, accepting bearer claims without signature/audience/expiry checks.
Reversal: remove new transport/method registration prospectively; preserve recorded
attempt identities and historical evidence. Supersede this decision to change
interfaces. Scientific promotion/protected custody and new external spending remain
human-reserved; their absence does not block DEVELOPMENT implementation.

## Delivery slices and acceptance

1. Catalogue, transport identity repair, actual standard CPU research interface.
2. Pinned Julia instrument and existing reference controller integration.
3. Miner consumer; the existing Workbench consumer follows as a separately
   shippable adapter/UI slice with exact draft/evidence association.

Coherent separately shippable PRs may follow these actual consumer dependencies.
The first PR includes the usable miner/validator CPU science path and two protocol
clients. Workbench needs additional private-service and saved-draft UI contracts;
it follows this shared service dependency rather than delaying CPU availability.
Use full runtime classification for shared reference/dependency changes. Required
acceptance includes canonical CPU/invariants/quality, package and outside-tree
imports, no-Torch JAX reconstruction, actual SDK wire tests, applicable isolated
service tests and Hub regeneration/validation. Tests never select scientific
thresholds. Local WSL observations remain diagnostics unless canonical execution
is established; pinned GitHub CI owns final repository acceptance.

Evidence recorded during development (local diagnostics, canonical CI pending):
new adapter/gateway diagnostics 73 passed;
actual official Python SDK stdio fixture tests 3 passed; supported reconstruction
catalogue tests 24 passed; three actual JAX implementations reconstructed with
Torch/neuralop/physicsnemo imports blocked. Native Julia instrument: 64 protocol
tests passed, one host runtime test skipped, 3 actual local Docker service tests
passed. The actual C-04 controller ran Julia and verified cancellation cleanup
(2 service cases passed). A real Julia miner study through the external Python
MCP client passed, including reconnect/replay without another dispatch or charge.
The independent TypeScript client passed its interoperability case; a real agent
host remains open. Numerical observations live in the programme evidence, not
qualification. See `carbon/miner_mcp/README.md` for the working operator command.

Completion is conditional on applicable automated checks and normal expected-head
merge under OWNER-DX-03. #209/#210 remain open until their remaining role/backend/
interface requirements have evidence. GPU/TPU, two independent external clients
including a real agent host, arbitrary Julia containment, complete Workbench,
portable optimizer/RNG state and finite learning experiments remain open.

## Canonical build repair

CI run 35281221985 passed its canonical environment and 25 isolated C-03
service cases, then failed while extending the locally built image with Julia.
The script used a local Docker image/config ID after `@` as though it were a
registry manifest digest; the runner attempted an unauthorized registry pull.
The repair uses a verified local tag with pull disabled, checks its exact parent
ID before and after building, and verifies inherited layers, entrypoint, nonroot
user and retained source/wheel/lock provenance before writing the child manifest.
This does not authenticate a hostile build host or grant deployment authority.
Eleven focused local tests passed, including tag rebinding and child provenance
rejections. Actual corrected build and canonical delivery acceptance remain due.
