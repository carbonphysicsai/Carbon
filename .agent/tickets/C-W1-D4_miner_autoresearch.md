# C-W1-D4 — Miner autoresearch and finite DEVELOPMENT campaign

**Status:** done
**Selected authority:** OWNER-C-W1-D4-AUTORESEARCH-01 (Ryan's current direction)
**Primary Hub map_ref:** WAVE-C/C-W1
**Depends on:** B-07G, C-02, C-03, C-04, C-05, C-06, C-07, C-08, C-10, C-W1-D3, C-REWARD
**Starting main:** 4a60d85323d1945fa0ec9815340618ad87e6e619 (PR #202),
tree 44f86d881bf5ac3e0a78641fddd99690329c797a; no advancement at selection.

## Authority and outcome

Ryan prospectively authorizes a public/synthetic DEVELOPMENT research workspace,
real isolated research training and a finite recursive model-backed campaign.
Keep burgers-development-balanced-v2 mathematics and C-05 derived measurement v3.
Expanded construction, data and resource bindings receive new versions. Historical
receipts remain immutable. This is delegated development authority, not independent
scientific/security review. No wallet access, chain transaction, production,
protected/customer data, AWS, storage migration, mainnet or qualification.

## Capability matrix at selection

| Capability | Current implementation/exposure | Required bounded work |
|---|---|---|
| Research discovery/compiler/resources/tasks | B-07 v2 twelve operations; local nominal providers | authenticated requester binding; preserve v1 submission separation |
| Real practice | B-07C is fixture-only | separate honest real DEVELOPMENT provider, durable execution |
| FNO/DeepONet | C-02 reconstructs both; agent exposes only steps 32..64 | architecture-specific catalog; reject unused controls |
| Model size | lab width; FNO depth/modes; DeepONet branch sensors | domains and executed-parameter checks; DeepONet ignores depth/modes |
| Training | AdamW, cosine/warmup, microbatches, clipping, EMA, H1/PDE losses | expose implemented controls with cost/limitations and recipe identity |
| Physical parameterization | hard initial condition on; mean preservation off | selectable registered controls, physical invariant verification |
| Other installed model code / Foundax | source exists; registration/support varies | advertise actual support only; unsupported requests retained |
| Research scripts/data | absent in current miner connection | isolated scratch-only process, trusted separate reference/diagnostics |
| Final comparison | signed C-06/C-07/C-10 and balanced-v2 | fresh role-separated cohorts and prospectively frozen recipes |

## Implementation sequence

1. Register versioned executable catalog and capability disclosure; retain old v2.
2. Bind B-07 research operations to authenticated ownership and durable tasks;
   add bounded workspace/capability log and cumulative admission ledger.
3. Real isolated research provider with learning curves, public data and trusted
   diagnostics. Independently stage final inference/reference inputs only after freeze.
4. Connect existing Responses adapter and C-08 submissions; local status/HTML,
   signed-source comparison and non-paying reward scenarios.
5. Focused boundary/behavior tests, actual changed-surface service acceptance,
   policy-selected CI, Hub reconciliation and normal expected-head merge.
6. Execute finite campaign from accepted revision; retain all failures/outcomes.

## Campaign ceiling (new authorization, not a reused approval)

Two adaptive epochs; sixteen research training trials (normally eight/epoch);
twelve final replicas (control/challenger x three x two); 96 provider attempts;
USD 1.00 hard cumulative monetary admission. gpt-5-mini-2025-08-07 initially.
One numerical worker at once, two CPUs, four GiB/no swap, <=600 seconds per
worker shortened to remaining allowance; six hours cumulative numerical wall;
eight hours elapsed from first experiment operation; 512 new reference
trajectories; 2,048 solver invocations including refinements/failures; ten GiB
retained artifacts. Reserve final evidence/selection/report capacity first.
Unknown billable/worker outcomes keep conservative reservations and stop duplicate
dispatch. Repository acceptance is separate. No automatic renewal or background loop.

Before dispatch freeze campaign identity/start, score digest, public training
support, credible matched control, selection procedure, independent role roots,
parent separation, final sampling/replica policy and all budgets. Final data is
not selected by outcomes. Epoch-one feedback makes its final cohort adaptively
seen; epoch two uses fresh final families under the same declared law.

## Verification and completion predicate

Test genuine/fixture provenance; authenticated namespaces/ownership; role leakage;
executed controls; hostile scripts/process cleanup; quotas/reference/provider
accounting; cancellation/replay/restart; pre-final freeze; immutable rule;
capability logs without authority; signed-source lifecycle/quarantine. Changes to
shared runtime/isolation use their existing broader acceptance, not a new narrow
allowlist. Complete only after accepted delivery and the finite real campaign,
or identify a genuine unavailable execution input with one exact resume command.
Report actual research vs final work/costs, all gates/scores, adaptation, capability
requests, reward simulation and next evidence-backed recommendation. Indeterminate,
regression and early stop remain valid observed outcomes; never invent a winner.


## Implementation checkpoint (not delivery or campaign evidence)

Draft modules implement cumulative admission with conservative unresolved charges,
owner-bound scratch files and notes, authenticated B-07 namespace routing, durable
B-07 task snapshots, isolated script/trusted practice carriers, exact-intent worker
reconciliation, separate role roots, a provider-meter adapter and local reports.
The real practice program uses the installed JAX Trainer and the current C-05 v3
source staged immutably by the trusted service; miner scripts run separately.

Focused canonical verification: 80 tests passed across the new accounting,
workspace, report, auth, role, provider, durable-task and recovery tests plus
existing B-07 lifecycle/service regressions. Focused Ruff/Black passed. Native
Linux service diagnostics: script containment/background-child cleanup passed;
real JAX practice program passed on manufactured engineering data with three
updates, retained curve/checkpoint and physical-mean check. These diagnostics are
not real-agent inference, Burgers quality evidence or canonical service acceptance.

The owner explicitly approved the exact compiler repair on 2026-09-17.
The catalog and resolved-plan validators now permit the four exact registered
C-02 TRAIN fields only when surface, field and consumer match. Other guards stay
active. Compiler/catalog/plan/gateway regression checks passed 142 tests.

The new real service composition now binds the existing twelve research
operations, versioned workspace tasks, typed public authoring contracts, the
registered compiler, static resource inspection and explicitly unresolved
forecasts. Public TRAIN/practice reference preparation wraps C-04 with individual
reservations, replay/artifact checks and failure retention. The local supervised
reply retains nominal v2 replies and waits for tasks without model polling.
Public task/recipe/scoring descriptions contain no realized final data.

The finite controller, authenticated twelve-operation agent SDK, frozen recipe
selection, isolated prospective generation, fresh final C-08 submission and signed
comparison adapters are now implemented. The separate miner image removes
evaluator/reference/controller modules while retaining pinned numerical libraries
and allowed lab code. Active cancellation removes the exact owned worker and
verifies cleanup. Invalid numerical proposals consume trial slots; provider
ambiguity retains conservative charges and prevents resending.

The new single-practice diagnostics reuse the existing balanced-v2 arithmetic,
show actual sampled failures and retain full per-case metrics/learning curves.
They do not manufacture replicas, final admissibility or accepted improvement.
The existing non-paying reward simulation receives an eligible first comparison;
fresh-epoch/control comparisons cannot reopen its baseline or activation clock.

Additional focused checks: 121 CPU tests passed; the canonical focused controller,
research and comparison run passed. A subsequent 37-test score/controller run
passed. Actual engineering service checks passed for isolated role generation
and exact replay, minimal-image containment and cancellation. All 24 advertised
controls passed executed JAX behavior checks in one isolated engineering worker
(test elapsed 70.48 seconds). The initial probe correctly rejected an invalid
warmup >= steps; the corrected probe obeyed the existing constraint. These are
manufactured engineering controls, not agent inference or quality evidence.

The fresh-profile integration completed 96 primary references, six real C-03
reconstructions and 144 C-05 case-by-replica observations. The first comparison
correctly rejected an absent engineering quarantine journal; after explicitly
creating that fixture, resume reused the exact signed sources, completed 48
refinement trajectories and passed comparison/disclosure/replay assertions.
No numerical source was replaced to obtain a favorable result. This fixed,
two-update recipe is integration evidence only, never agent or quality evidence.
The earlier byte-identity and stale-image failures remain retained. Campaign
preflight now requires an image built from its exact accepted revision.

Hub source/impact validation and code quality passed. Owner reporting includes
current hypothesis, active operation, remaining budgets, phase accounting and
an agent-report.json alias. Invalid closed requests are retained corrective
feedback; ambiguous execution still stops without duplication.

Remaining: policy-required acceptance and normal delivery, then execute the
finite real campaign from its accepted revision. Main advanced through unrelated
Workbench and website PRs #203/#204; preserve their authority and Hub changes.
No PR or accepted D4 revision exists. No real campaign model call, reference
trajectory, training trial, final reconstruction or network transaction occurred.
The campaign clock has not started and none of its provider allowance was spent.


## Delivered finite outcome (2026-09-17)

PR #206 delivered C-W1-D4 at ce6a7e389d9f4d815fbbb45c68db2eda8784a1e8, normal merge 54f8aff742b99fd2957fede73ea814f03da49eb6; required run 35226186473 passed. Its real campaign stopped after seven model calls, USD 0.00818185, 96 public reference trajectories and 310.573 numerical seconds. The agent asked for already-granted trial approval; zero training trials or final replicas ran. This is an orchestration finding, not a measured model failure or accepted improvement. C-W1-D5 is selected for prospective autonomous continuity under OWNER-C-W1-RESEARCH-PROGRAM-01. No chain writes.
The earlier implementation checkpoints above are historical. The outcome did not
fulfill the desired learning loop; D5 owns the evidence-driven continuity repair.
