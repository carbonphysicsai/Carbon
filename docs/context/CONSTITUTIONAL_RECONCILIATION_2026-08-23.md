# Carbon Full Constitutional Reconciliation — 2026-08-23

**Status:** completed repository-wide architecture reconciliation on `main`.  
**Scope:** scientific canon, A0–A12 implementation plan, Waves B–G, long-term agentic construction plan, business canon, network/Alpha doctrine, development-agent instructions, and publication-control boundaries.  
**Purpose:** establish one coherent destination architecture without falsifying implementation maturity.

---

# 1. Executive verdict

The repository is now **constitutionally coherent in authority and direction**, with explicit maturity boundaries.

This does **not** mean every planned component is implemented.

The correct statement is:

> **Carbon now has one reconciled constitutional architecture spanning the implemented A0–A7 foundation, the remaining A8–A12 Wave-A work, later scientific/agentic waves, business architecture, and public claim discipline. Implementation maturity remains explicit and incomplete where work has not shipped.**

This reconciliation deliberately avoids the false claim:

> “Everything described in the repo is already implemented or production-qualified.”

---

# 2. New constitutional authority set

## Repository constitution

- `CONSTITUTION.md`

Defines:

- durable Carbon identity;
- authority planes;
- scientific constitution;
- runtime/domain authority;
- A0–A7 compatibility ruling;
- A8–A12 maturity boundary;
- long-term agentic construction direction;
- business/network/publication boundaries.

## Integrated scientific canon

- `docs/context/SCIENTIFIC_REFERENCE_CANON_V4_MASTER.md`

The prior `docs/context/SCIENTIFIC_REFERENCE_CANON.md` remains an evidence/bibliographic annex and historical scientific framing. It does not override v4 where architecture differs.

## Build migration guard

- `Design_Specs/Build_Out_Constitutional_Overlay.md`

Preserves `Build_Out.md` v1.4 as current detailed sequencing authority while preventing stale shorthand from reintroducing superseded assumptions.

## Long-horizon agentic plan

- `Design_Specs/Agentic_Development_Master_Plan.md`

Connects A0–A12 through qualified execution, Landscape, product qualification, commercial/private operation, frontier promotion, treasury, model-family neutrality, generalized construction, ReconstructionProtocol, lifecycle, and Physics Intelligence.

---

# 3. A0–A7 reconciliation

The completed implementation was reviewed architecturally against the integrated constitution.

## A0 — package layout

**Finding:** compatible.

No constitutional reason to replace the package layout. Future modules extend the package rather than forcing cosmetic renames.

## A1 — CI

**Finding:** compatible.

CI is retained as evidence infrastructure. Future constitutional invariant tests extend rather than replace current lanes.

## A2 — Strategy schema

**Finding:** compatible as bounded P0 subtype.

Current:

```text
TrainingStrategy
```

Future abstraction:

```text
ModelConstructionStrategy
  └── TrainingStrategy
```

No A2 migration is required before A8.

## A3 — Challenge registry / qualification hashes

**Finding:** compatible and foundational.

Future distribution, truth, measurement, frontier, and product identities extend Challenge binding rather than bypass it.

## A4 — seed domains

**Finding:** compatible.

The reconciliation adds one scientific caveat: seed separation is necessary for secrecy/decontamination but does not by itself prove semantic independence of datasets, generators, truth sources, or adaptive feedback.

## A5 — ScoreEngine

**Finding:** compatible as current bounded policy executor.

Critical migration ruling:

> A5 may not become the owner of frontier promotion, cross-Challenge economic normalization, treasury policy, customer payment, or product qualification.

Future Score Pack semantics may expand evidence eligibility/admissibility/estimand/uncertainty policies, but the engine remains an executor of registered policy.

## A6 — card store/disclosure

**Finding:** compatible.

The private/internal → public/miner projection model is retained and becomes the foundation for later customer/auditor/private projections.

## A7 — fees + submission FSM

**Finding:** compatible.

A7 remains the submission lifecycle authority. Frontier and treasury states remain downstream separate domains.

---

# 4. A8 reconciliation

A8 remains:

```text
SPECIFIED / DOCUMENTED: substantial
IMPLEMENTED: NO
TESTED: NO
PRODUCTION-QUALIFIED: NO
WAVE STATUS: todo
```

The A8 fixture execution design is compatible with the integrated architecture if implemented within the new overlay constraints.

Important interpretation:

> **A8 is the first bounded execution/reconstruction seam, not the final generalized ReconstructionProtocol.**

A8 must not implement:

- arbitrary participant construction programs;
- frontier promotion;
- treasury settlement;
- commercial rights/payment;
- product qualification;
- production sandbox claims.

Those are later authority planes.

---

# 5. A9–A12 reconciliation

## A9

Miner-facing free/practice signals remain intentionally non-oracular. Landscape/prior intelligence may later improve guidance but cannot become a reverse-engineered official exam.

## A10

Leaderboard is explicitly separated from:

- `FrontierRecord`;
- `FrontierAdvanceEvent`;
- Product Qualification;
- commercial acceptance.

## A11

Observability should preserve typed failure separation as future layers arrive:

```text
candidate / scientific
reference / truth
generator
reconstruction
infrastructure
treasury / settlement
commercial acceptance
```

## A12

Wave-A invariant suite closes only on enforceable current types. It should not fabricate tests for future frontier/treasury objects that do not yet exist.

---

# 6. Waves B–G reconciliation

The existing Build Out waves remain valid, but their meaning is now sharpened.

## B — science-ready skeletons

Must introduce/prepare explicit task-population-truth-measurement objects rather than treating “generator output” as the complete exam definition.

## C — vertical integration

Must prove one real protected candidate-evaluation path with producer-independent reconstruction and authoritative ExperimentRecord provenance.

## D — human qualification

Human/SciML authority earns the right to call the first exam LIVE.

## E — Landscape

Evidence memory and proposal layer only.

> **Landscape proposes; registered contracts decide; independent experiments adjudicate.**

## F — product qualification

Exact artifact/system/context qualification plane.

> **Rank nominates. Evidence qualifies.**

## G — commercial/private/sponsored plane

Consumes `Business/` authority for rights, privacy, deliverables, customer acceptance, and payment without moving those decisions into scientific scoring.

---

# 7. New long-horizon waves H–N

The Agentic Master Plan introduces planning waves to make the destination architecture explicit.

These are **not currently authorized implementation tickets**.

```text
H — frontier promotion + Challenge portfolio
I — treasury / event-bound network settlement
J — model-family neutrality
K — generalized Agentic Construction Discovery
L — ReconstructionProtocol / isolated construction worker
M — product/system lifecycle qualification
N — prospective Physics Intelligence
```

This closes the previous gap where the repo had good local documents for Landscape/specialists but no single authoritative end-to-end path from A0 to generalized agentic construction.

---

# 8. Generalized construction reconciliation

The long-term ontology is now explicit:

```text
parameters
→ TrainingStrategy / recipes
→ architectures / compositions
→ ModelConstructionStrategy
→ construction methods
→ ConstructionProgram
```

and:

```text
fresh validator retraining
→ broader registered ReconstructionProtocol
```

while preserving:

> **Construction and official evaluation are separate security domains.**

No current A-ticket is reinterpreted as arbitrary agent-code execution.

---

# 9. Scientific-economic reconciliation

The target long-term chain is now repository-constitutional:

```text
qualified Challenge
→ ScoreResult
→ contender nomination
→ common fresh frontier promotion when required
→ FrontierAdvanceEvent
→ SettlementObligation
→ separately governed treasury settlement
```

This prevents future code from embedding permanent-incumbent reward semantics directly into scoring merely because historical/current transport once mapped score toward weights/emissions.

Current runtime mechanics remain current runtime mechanics until explicitly migrated.

---

# 10. Business reconciliation

The `Business/` canon remains outside the scientific judge.

Company architecture:

```text
Evidence Audit / Feasibility
→ Sponsored Discovery
→ Model Development
→ Qualification
→ Lifecycle
→ Enterprise Platform
→ API / OEM / Frontier Market
→ Physics Intelligence when earned
```

Network architecture:

```text
OFFCHAIN_ONLY
→ NETWORK_ELIGIBLE
→ NETWORK_REQUIRED
```

only where science, privacy, security, economics, and participant liquidity justify the transition.

OpCo revenue does not automatically create Alpha value.

---

# 11. Agent-development reconciliation

Updated:

- `.agent/INVARIANTS.md`
- `.agent/WAVE.md`
- `agent_pack/EXECUTION_PROTOCOL.md`
- `agent_pack/README.md`

Coding agents now must read the repository constitution and migration overlay before A8 onward.

Historical A0–A7 evidence remains intact rather than being rewritten after the fact.

Future plans are visible for compatibility, but agents are explicitly prohibited from implementing later-wave freedom inside current bounded tickets without authorization.

---

# 12. Publication reconciliation

`docs/publications/` remains the public claim-control plane.

Publications must distinguish:

```text
scientific design
implementation
Carbon evidence
production qualification
```

and:

```text
business design
customer discovery
paid pilot
repeatable service
recurring revenue
platformization
network leverage
```

The papers can describe the destination architecture if they label current maturity correctly. They do not promote future architecture into implemented runtime authority.

---

# 13. Remaining work vs coherence

The repository can now be called **architecturally/constitutionally coherent**, but not “complete.”

Remaining implementation work includes:

- A8–A12;
- Waves B–D scientific execution/qualification;
- later Landscape/product/commercial/frontier/treasury/agentic waves;
- production security/network qualification;
- commercial validation.

Coherence means those future states now fit one authority model and do not contradict the completed foundation.

---

# 14. Final reconciliation statement

> **The implemented A0–A7 system is retained as a bounded P0 foundation. A8–A12 complete the current software skeleton. Waves B–D earn a qualified judge. Landscape and product layers consume evidence without becoming truth. Frontier promotion separates scientific progress from ordinary ranking. Treasury settles rather than decides. Business monetizes evidence without changing it. Agentic search eventually broadens from training recipes to construction programs while official evaluation remains independently controlled.**
