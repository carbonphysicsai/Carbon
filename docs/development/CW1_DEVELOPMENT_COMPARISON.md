# C-W1-D2: descriptive DEVELOPMENT comparison, version 1

This prospective engineering contract is frozen before new agent outcomes. Its
private session manifest binds exact material digests and the model-run proposal.
It does not retroactively rescore the historical signed baseline.

## Population, construction and reference

Use `carbon.burgers-supervised-development.v2`, Burgers Dynamics V1 1.0 and the
retained 12 TRAIN / 12 EVAL / 12 STRESS generation pin and complete case manifest.
The population, normalized coordinates and units remain those of the profile.
TRAIN alone provides labels to reconstruction. The trusted evaluator retains
EVAL/STRESS references and hidden state. Reuse the retained C-04 Cole-Hopf
reference artifacts without regeneration. Prior aggregate outcomes have been
seen; this reduced development subset is not a prospective confirmatory study.

Candidate declarations remain FNO or DeepONet with integer steps 32..64. Reuse
the accepted JAX adapter, exact worker image and original two-CPU, 4-GiB/no-swap
worker policy. All three replicas count, including failed attempts. Preserve
baseline construction and resource identities; incompatible evidence is withheld,
not normalized into compatibility. Freeze six new private seed commitments
before inference, three per permitted proposal slot. These are independent new
reconstructions; replica index is not a scientifically paired baseline seed.

## Measurements and descriptive comparison

Consume C-05's four normalized errors (field phase RMS, maximum compression,
peak dissipation and energy half-time) and six normalized physics defects
(initial condition, periodicity, conserved mean, maximum principle, energy
dissipation balance and weak local PDE). Lower values mean lower observed
error/defect on this cohort, not a qualified physics pass. Preserve the exact
measurement contract, implementation, environment, reference and scaling pins.

For each role and metric, average 12 cases within each replica and then average
the three replica means. Report baseline and challenger means, challenger minus
baseline difference, three replica means and their sample standard deviation,
and variation of paired case-mean differences across the 12 shared cases. Never
treat 36 case-by-replica cells as independent cases. Report no inferential
confidence interval or p-value: three replicas provide a limited descriptive
spread and no qualified uncertainty procedure exists. Show EVAL and STRESS
separately; no fitted weights, pooled scalar, case dropping or selective metric
reporting. Energy half-time remains horizon-clipped; retain and label its
candidate/reference censor counts rather than claiming uncensored timing.

No current authorized admissibility/improvement/equivalence rule applies.
Acceptance is `INDETERMINATE_NO_ACCEPTANCE_RULE`, accepted improvement and score
are null. An arithmetic zero difference is not a tie; absence of detected
difference is not equivalence. Scientific qualification remains separate.

## Identity, lifecycle and failures

Resolve C-08 authentication, C-07 account/dossier and C-06 signature/lifecycle from
their existing owners against frozen trusted roots and verification keys. Bind
all measurement request/result digests and full rectangular role/case/replica
membership; verify original strategy, plan, repeat, worker and resource pins.
Use the configured C-10 owner journal to check both primary and reexecution
source associations. Quarantine, pending/reconciliation, superseded/revoked,
altered, mismatched or incomplete sources cannot support an eligible comparison.
No C-10 record means no reexecution is recorded; it is not independent audit
evidence. Recheck lifecycle and C-10 before every downstream projection.

Missing, failed, censored and nonfinite observations remain distinct. Missing,
failed or nonfinite measurements withhold a complete comparison; no zero-fill,
replacement run or easier cohort. Censor flags are explicit as above. Exact
replay returns the same report; conflicting replay rejects. Preserve stopped
attempts, their actual accounting and partial evidence.

## Miner disclosure and experiment bounds

Only the existing C-07 aggregate feedback and the miner's own baseline strategy
are supplied through the authenticated local miner service. No labels, case IDs,
private state, filesystem, repository, owner wallet or credential tool is exposed.
The trusted controller consumes private credential paths; no public listener.

One new experiment: USD 1 maximum; 24 provider calls including failures/retries;
two distinct proposals including invalid attempts; two evaluations; three
replicas per evaluation; at most six training runs / 384 updates, six prediction
runs and 144 measurement runs. No new references. Freeze the exact model,
prompt/tools, per-call token reservations, 10,800-second session deadline,
7,200-second cumulative worker envelope and derived storage/service caps in the
private manifest before dispatch. Keep accounting separate from Ask Carbon.
Ambiguous provider or worker dispatch retains a conservative reservation and
stops; no automatic resend, reconstruction retry or replacement session.

## Non-paying reward seam and later prerequisite

The real report is structurally ineligible for official score, protected evidence,
settlement, payment and the existing all-burn publication profile. Its future
C-REWARD handoff names exact sources/contract/report and missing acceptance,
score-range/baseline, gain/window/allocation and finalized identity policies.
Synthetic accepted-comparison fixtures may exercise existing reward arithmetic;
they never turn a real indeterminate source into a winner. Later non-burn
testnet work needs an authorized comparison rule, eligible evidence, separately
owned reward policy, finalized miner-to-UID mapping, a winner-capable publication
profile and fresh exact transaction authorization. Multiple challenge allocations
remain separately enabled and challenge-bound. No public-network write occurs.
