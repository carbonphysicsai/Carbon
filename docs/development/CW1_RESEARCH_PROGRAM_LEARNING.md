# Finite miner research program — retained learning record

Authority: OWNER-C-W1-RESEARCH-PROGRAM-01. Primary map: WAVE-C/C-W1-D5.

## Campaign one: actual inference, no training

Accepted ce6a7e389d9f4d815fbbb45c68db2eda8784a1e8 (PR #206), merged as
54f8aff742b99fd2957fede73ea814f03da49eb6. Campaign
cw1-d4-c96cbff96bc54c638d695632548ba724 began 2026-09-17T15:24:51Z and stopped
at 2026-09-17T15:30:49.805448Z, 358.613 elapsed seconds from the retained terminal
marker filesystem timestamp. The legacy status view continues counting time since
start after completion; its later 456.864-second observation was report age, not
execution duration. This correction changes no measured usage or outcome. Model gpt-5-mini-2025-08-07, seven completed calls,
USD 0.00818185 measured provider cost. Ninety-six public references (72 TRAIN,
24 adaptive validation), 96 solver invocations, 310.573 numerical worker seconds.
Zero training trials, zero final construction replicas, zero chain transactions.
Retained bytes at terminal inspection: 7,530,017 physical (includes reports),
1,837,413 metered artifact bytes; no unresolved reservations. Reports can grow.

Expected: discover, hypothesize, practice, inspect and select or justify stopping.
Observed: discovery, public material retrieval and static recipe resource check;
then an ordinary permission request to the owner. No candidate quality, gate,
admissibility, comparison, improvement, tie or reward winner can be inferred.

Classification: C, orchestration. Model/training and evaluation defects are
untested. Seven calls completed before a voluntary stop; context admission did
not cause this outcome. No analysis operation displaced any training slot.
Public reference preparation is real numerical work, not model training.

## Ranked capability backlog (prospective, not claims of benefit)

1. Explicit autonomous execution/structured stopping. Direct transcript evidence
   of needless permission seeking; low implementation cost, no worker change.
   Hypothesis: same model reaches actual practice under unchanged resource/score
   controls. Falsifier: second campaign repeats the stall or cannot use results.
2. Auditable context compression if accumulated public envelopes truncate useful
   research. No context-stop occurred in campaign one; measure before implementing.
   Requires deterministic provenance/replay and retention of complete raw history.
3. Separate bounded analysis accounting if useful diagnostics displace training.
   Zero displaced trials observed so far. Requires accounting/admission changes,
   not an assumed grant for unmetered inner searches.

Other backbones, larger training sets, rollout and checkpoint resume remain
unranked hypotheses until a real trial identifies their benefit. A richer model
or looser scientific gates is not justified by the observed permission request.

## Iteration D5 (frozen before campaign two)

Problem/hypothesis/change/falsifier are recorded above and in the D5 ticket.
Change: agent-policy v2 only; same model, public support, control, recipe catalog,
scoring mathematics, final resource ceilings and fresh sampling procedure.
New implementation/image and exact policy hashes are frozen before execution.
Calibration evidence: campaign-one transcript. Verification: synthetic replay,
stop and budget tests, then the separately identified real campaign two.
Outcome: delivered in PR #208. Campaign two reached practice without another
owner prompt, supporting the narrow hypothesis. Sustained adaptation was not
established; the retained outcomes below supersede the pending observation.

## Incentive study: synthetic examples only

Existing C-REWARD with opening score .8, maximum 1 and half-life 24h: .99 gives
.95 opening credit and .475 after a day. Withholding that same improvement until
day one gives .95 then; a .9 day-zero followed by .99 day-one path gives .7.
Thus delay/drip feeding can retain more later credit under this example. Copies
at equal score do not reset incumbent/clock. Multi-challenge synthetic allocation
was .4875 winner, .5125 burn. These are policy examples, not agent exploitation,
competitive equilibrium, actual earned credit, wallet payment or settlement.

## Private owner inspection

Root: $HOME/.local/share/carbon-testnet/miner-autoresearch-20260917-d4.
Open research-report.html and agent-report.json. Existing tested status command:
`$HOME/.local/share/carbon-testnet/operator/run-cw1-d4-ce6a7e38.sh status`.
Raw provider responses, task requests, references and accounting remain there.
Do not run this completed campaign again; it consumes program slot one.


## Completed program: real execution, not fixture evidence

| Campaign | Exact executed revision | Calls / cost (USD) | Completed training / rejected trials | Final replicas | Numerical seconds | References / solver calls | Elapsed seconds |
|---|---|---:|---:|---:|---:|---:|---:|
| 1 | ce6a7e389d9f4d815fbbb45c68db2eda8784a1e8 | 7 / 0.00818185 | 0 / 0 | 0 | 310.573 | 96 / 96 | 358.613 |
| 2 | 4d1e3f54560c858be4ac20e51f3671f3d77900e6 | 3 / 0.00481200 | 1 / 0 | 6 | 951.365 | 168 / 168 | 1017.411 |
| 3 | 3b6d2d9b5947b139c396f755fef8077d6885fbe8 | 5 / 0.00652540 | 0 / 2 | 0 | 77.241 | 24 / 24 | 109.062 |
| Total | three distinct frozen campaigns | 15 / 0.01951925 | 1 / 2 | 6 | 1339.179 | 288 / 288 | 1485.086 |

Costs use observed provider usage and the frozen price schedule, not an
independently reconciled invoice. Tokens: 121973 input including 80000 cached;
3513 output including 1152 reasoning. Reasoning is not counted twice. There are
no unresolved monetary reservations. Engineering, image preparation and repository
CI remain separate. Legacy elapsed timestamps use the retained terminal marker or
recorded operational stop; campaign three has a durable completion timestamp.
Reports can grow after completion; approximately 60 MB was retained at closeout.
No evidence was deleted. All numerical work retained the accepted two-CPU,
four-GiB/no-swap profile, one worker at a time and 600-second productive ceiling.

Campaign two ran one 512-update FNO practice trial, then selected the same recipe
as the control: width 32, depth 3, modes 16, batch 8, learning rate .002, hard
initial-condition and mean preservation, EMA. Its TRAIN loss moved from .33331534
to .07299912 across different minibatches; this is not a validation curve or proof
of improved physics. Practice score was .72351115 with physical gate failures.
The agent's favourable-curve/budget explanation was unsupported by the available
validation and ledger evidence. No construction adaptation occurred.

Baseline and candidate were independently reconstructed three times each on the
same fresh final cohort. Both authentic sources remain COMPLETE_UNRESOLVED;
signed-source association, active lifecycle and C-10 status were checked before
comparison. The result is REJECTED_MANDATORY, not an accepted improvement or tie.

| Final measurement | Baseline | Candidate | Candidate minus baseline |
|---|---:|---:|---:|
| EVAL field time RMS | .34891526 | .34081098 | -.00810428 |
| STRESS field time RMS | .31185547 | .31001456 | -.00184091 |
| EVAL energy-path RMS | .13982118 | .13299784 | -.00682334 |
| STRESS energy-path RMS | .12994988 | .12418674 | -.00576314 |
| DEVELOPMENT score | .75135481 | .75522562 | +.00387080 |

Mandatory failures: baseline EVAL energy path and maximum principle, STRESS
energy path; candidate EVAL and STRESS energy path. Soft gains cannot compensate.
These are the same construction recipe, so the small differences describe
construction variation rather than a discovered improvement. Three replicas and
finite cases are separate dependence dimensions, not independent case-replica
samples or a population confidence interval. Reference-resolution indicators were
.0004163533 field and .00000612991 energy, below the frozen .0025 limits. Their
shared-method spatial-refinement basis is not a qualified error bound. Energy
failure is observed; undertraining versus operator/reference defects is not
resolved by this one trial. No stronger-model claim follows.

After that final comparison, legacy admission double-counted the final reserve:
six used replicas plus the full twelve-replica reserve exceeded the twelve cap.
Epoch-two inference was never dispatched. The retained failed campaign was not
resumed under modified code. PR #207 repaired remaining-reserve admission within
its existing managed control owner; campaign three received a new identity.

Campaign three rejected two workspace actions mislabeled as practice tasks. The
agent corrected the kind, obtained 24 public practice references, redundantly
polled one terminal result and called the structured stop operation. It cited
scheduling constraints despite 14 trial slots, 91 calls, all 12 final replicas
and USD .9934746 remaining. It corrected request syntax but never trained or
adapted a construction. The managed reserve repair's six-replica condition was
therefore not exercised by real campaign-three work; focused tests cover it.
The API run is terminal and its local controller was shut down. This does not
satisfy C-MLP's separate browser/multiple-iteration acceptance.

## Constraint diagnosis and next falsifiable hypothesis

The program supports B/C interface/orchestration constraints. It does not yet
separate A model/training causes, D binding numerical limits, or E measurement/
reference causes. No valid analysis operation ran, so zero useful training trials
were observed displaced by analysis accounting. Request bytes reached 59275 in
campaign one and 58567 in campaign three against a 61440 guard. Neither stop was
caused by that guard. Context growth is a measured risk, not the diagnosed cause.

1. **Task-kind guidance and recoverable errors.** Two rejected requests attempted
   already supported workspace operations using the practice kind. Recommend
   explicit valid examples and actionable error projections. Small authenticated
   interface/replay slice; no new worker or reconstruction permission. Hypothesis:
   the same model recovers and reaches useful practice. Another format stall or
   unsupported stop would contradict the proposed benefit.
2. **Remaining budgets and evidence-linked stopping.** Both later selections/stops
   cited unsupported scheduling constraints. A bounded trusted projection may
   improve decisions. Test this as a separate factor; no permissions from a request.
3. **Compact terminal observations with auditable context.** One unnecessary result
   poll and near-guard context growth justify measurement. Preserve raw transcripts,
   exact summaries and deterministic restart. Benefit is not yet established.

No agent request establishes a need for more architectures, GPUs, custom validator
code, rollout, checkpoint selection or adaptive sampling. These remain unsupported
or untested capabilities, not silently advertised implementations. Requests for
private final cases, labels, credentials or controller access remain prohibited.
The rich notebook/request history is private operator-owned evidence, not a future
external miner disclosure requirement.

## Incentive interpretation correction (synthetic only)

The earlier examples correctly show larger *later instantaneous* targets after
delay/drip-feeding. They do not establish greater cumulative reward under constant
allocation. Independent analytic integration over two days gives target-days
1.02792022 immediate, .86561702 drip-fed, .68528014 delayed. With allocation zero
until day one and one afterwards, totals are .34264007, .50494326, .68528014.
Thus a known rising allocation schedule can create a timing incentive in this
no-competitor example. Existing C-REWARD schedule execution confirmed the day-one
targets .475/.7/.95. No actual winner, payment or competitive equilibrium follows.
Copies/replay do not refresh the clock; self-improvement/takeover reuse fixed
opening score/headroom. Fresh-cohort control comparisons cannot silently reset
the incumbent lineage; a later winner path needs matched eligible evidence.

## Finite closure and proposed next request

All three slots are consumed; unused money, time, calls and storage do not
authorize a fourth campaign. No chain writes occurred. No admissible final model
or accepted improvement entered reward simulation. Source lifecycle, quarantine,
protected/official rejection and the all-burn publisher remain unchanged.

Recommend first delivering the task-kind/error repair, then one freshly authorized
same-model campaign: maximum two epochs, four trial attempts including invalid
attempts, 24 calls, USD .25, twelve final replicas, three numerical hours, four
elapsed hours, 256 reference trajectories, 1024 solver invocations, five GiB.
Existing managed admission reserves twelve replicas, 8640 numerical seconds and
two GiB for final evidence. These are ceilings/reservations, not quotas; the
observed 951.365-second complete path informs an estimate, not a guarantee.
Keep balanced-v2/C-05-v3, control/resource matching and fresh final draws; one major
interface factor. Stop on ambiguous side effects, exhausted ceilings or an
evidence-supported reason. No dispatch occurs without new authority.

The private owner report is
`$HOME/.local/share/carbon-testnet/miner-autoresearch-program-20260917/program-learning-summary.html`;
its companion safe JSON, full TRAIN curve and each campaign's existing reports
link retained evidence. Campaign three can be inspected without restarting:

```bash
cd $HOME/Carbon-cw1-program-final
PYTHONPATH=. $HOME/Carbon/.venv/bin/python -m carbon.development_session.research_campaign status --root $HOME/.local/share/carbon-testnet/miner-autoresearch-20260917-expansion-2
```

Runtime delivery and local/private reporting are complete. On 2026-09-17 Ryan
explicitly approved publication of this prepared sanitized closeout and directed
normal delivery to continue. That resolves the prior automatic-review publication
block for these aggregates, delivery identities, limitations and recommendations.
The rejection remains historical evidence. No secrets, hidden realized case
identities, labels, raw transcripts or private evidence files are published.
Publication approval adds no experiment spending or chain authority.

The coordinated corrective-feedback work is tracked in PR #212. Its engineering
acceptance and any real/browser validation remain separate; this program does
not grant that task additional execution authority or claim its results.
